{
  "id": "webhook-endpoints",
  "name": "Webhook endpoints",
  "description": "Webhook endpoints capabilities. Includes Create a webhook endpoint, Delete a webhook endpoint, List all webhook endpoints, Retrieve a webhook endpoint, Update a webhook endpoint."
}
