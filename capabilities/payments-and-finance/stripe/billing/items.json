[
  {
    "id": "GetBillingAlerts",
    "name": "List billing alerts",
    "description": "List billing alerts Lists billing active and inactive alerts GET /v1/billing/alerts",
    "context": "GET /v1/billing/alerts\nLists billing active and inactive alerts\nOptional: alert_type (query) — Filter results to only include this type of alert.; ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; meter (query) — Filter results to only include alerts with the given meter.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; body (body)"
  },
  {
    "id": "GetBillingAlertsId",
    "name": "Retrieve a billing alert",
    "description": "Retrieve a billing alert Retrieves a billing alert given an ID GET /v1/billing/alerts/{id}",
    "context": "GET /v1/billing/alerts/{id}\nRetrieves a billing alert given an ID\nRequired: id (path)\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetBillingCreditBalanceSummary",
    "name": "Retrieve the credit balance summary for a customer",
    "description": "Retrieve the credit balance summary for a customer Retrieves the credit balance summary for a customer. GET /v1/billing/credit_balance_summary",
    "context": "GET /v1/billing/credit_balance_summary\nRetrieves the credit balance summary for a customer.\nRequired: filter (query) — The filter criteria for the credit balance summary.\nOptional: customer (query) — The customer whose credit balance summary you're retrieving.; customer_account (query) — The account representing the customer whose credit balance summary you're retrieving.; expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetBillingCreditBalanceTransactions",
    "name": "List credit balance transactions",
    "description": "List credit balance transactions Retrieve a list of credit balance transactions. GET /v1/billing/credit_balance_transactions",
    "context": "GET /v1/billing/credit_balance_transactions\nRetrieve a list of credit balance transactions.\nOptional: credit_grant (query) — The credit grant for which to fetch credit balance transactions.; customer (query) — The customer whose credit balance transactions you're retrieving.; customer_account (query) — The account representing the customer whose credit balance transactions you're retrieving.; ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; body (body)"
  },
  {
    "id": "GetBillingCreditBalanceTransactionsId",
    "name": "Retrieve a credit balance transaction",
    "description": "Retrieve a credit balance transaction Retrieves a credit balance transaction. GET /v1/billing/credit_balance_transactions/{id}",
    "context": "GET /v1/billing/credit_balance_transactions/{id}\nRetrieves a credit balance transaction.\nRequired: id (path) — Unique identifier for the object.\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetBillingCreditGrants",
    "name": "List credit grants",
    "description": "List credit grants Retrieve a list of credit grants. GET /v1/billing/credit_grants",
    "context": "GET /v1/billing/credit_grants\nRetrieve a list of credit grants.\nOptional: customer (query) — Only return credit grants for this customer.; customer_account (query) — Only return credit grants for this account representing the customer.; ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; body (body)"
  },
  {
    "id": "GetBillingCreditGrantsId",
    "name": "Retrieve a credit grant",
    "description": "Retrieve a credit grant Retrieves a credit grant. GET /v1/billing/credit_grants/{id}",
    "context": "GET /v1/billing/credit_grants/{id}\nRetrieves a credit grant.\nRequired: id (path) — Unique identifier for the object.\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetBillingFeedbackOptions",
    "name": "Feedback Options List API Method",
    "description": "Feedback Options List API Method An API method for listing the feedback options model GET /v1/billing/feedback_options",
    "context": "GET /v1/billing/feedback_options\nAn API method for listing the feedback options model\nOptional: ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; status (query) — Filter results to only include feedback options with the given status.; body (body)"
  },
  {
    "id": "GetBillingFeedbackOptionsId",
    "name": "Retrieve a feedback option",
    "description": "Retrieve a feedback option Retrieves a feedback options object given an ID. GET /v1/billing/feedback_options/{id}",
    "context": "GET /v1/billing/feedback_options/{id}\nRetrieves a feedback options object given an ID.\nRequired: id (path)\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetBillingMeters",
    "name": "List billing meters",
    "description": "List billing meters Retrieve a list of billing meters. GET /v1/billing/meters",
    "context": "GET /v1/billing/meters\nRetrieve a list of billing meters.\nOptional: ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; status (query) — Filter results to only include meters with the given status.; body (body)"
  },
  {
    "id": "GetBillingMetersId",
    "name": "Retrieve a billing meter",
    "description": "Retrieve a billing meter Retrieves a billing meter given an ID. GET /v1/billing/meters/{id}",
    "context": "GET /v1/billing/meters/{id}\nRetrieves a billing meter given an ID.\nRequired: id (path)\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetBillingMetersIdEventSummaries",
    "name": "List billing meter event summaries",
    "description": "List billing meter event summaries Retrieve a list of billing meter event summaries. GET /v1/billing/meters/{id}/event_summaries",
    "context": "GET /v1/billing/meters/{id}/event_summaries\nRetrieve a list of billing meter event summaries.\nRequired: customer (query) — The customer for which to fetch event summaries.; end_time (query) — The timestamp from when to stop aggregating meter events (exclusive). Must be aligned with minute boundaries.; id (path) — Unique identifier for the object.; start_time (query) — The timestamp from when to start aggregating meter events (inclusive). Must be aligned with minute boundaries.\nOptional: ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; value_grouping_window (query) — Specifies what granularity to use when generating event summaries. If not specified, a single event summary would be returned for the specified time range. For hourly granularity, start and end times must align with hour boundaries (e.g., 00:00, 01:00, ..., 23:00). For daily granularity, start and end times must align with UTC day boundaries (00:00 UTC).; body (body)"
  },
  {
    "id": "PostBillingAlerts",
    "name": "Create a billing alert",
    "description": "Create a billing alert Creates a billing alert POST /v1/billing/alerts",
    "context": "POST /v1/billing/alerts\nCreates a billing alert\nRequired: body (body)"
  },
  {
    "id": "PostBillingAlertsIdActivate",
    "name": "Activate a billing alert",
    "description": "Activate a billing alert Reactivates this alert, allowing it to trigger again. POST /v1/billing/alerts/{id}/activate",
    "context": "POST /v1/billing/alerts/{id}/activate\nReactivates this alert, allowing it to trigger again.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "PostBillingAlertsIdArchive",
    "name": "Archive a billing alert",
    "description": "Archive a billing alert Archives this alert, removing it from the list view and APIs. POST /v1/billing/alerts/{id}/archive",
    "context": "POST /v1/billing/alerts/{id}/archive\nArchives this alert, removing it from the list view and APIs. This is non-reversible.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "PostBillingAlertsIdDeactivate",
    "name": "Deactivate a billing alert",
    "description": "Deactivate a billing alert Deactivates this alert, preventing it from triggering. POST /v1/billing/alerts/{id}/deactivate",
    "context": "POST /v1/billing/alerts/{id}/deactivate\nDeactivates this alert, preventing it from triggering.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "PostBillingCreditGrants",
    "name": "Create a credit grant",
    "description": "Create a credit grant Creates a credit grant. POST /v1/billing/credit_grants",
    "context": "POST /v1/billing/credit_grants\nCreates a credit grant.\nRequired: body (body)"
  },
  {
    "id": "PostBillingCreditGrantsId",
    "name": "Update a credit grant",
    "description": "Update a credit grant Updates a credit grant. POST /v1/billing/credit_grants/{id}",
    "context": "POST /v1/billing/credit_grants/{id}\nUpdates a credit grant.\nRequired: id (path) — Unique identifier for the object.\nOptional: body (body)"
  },
  {
    "id": "PostBillingCreditGrantsIdExpire",
    "name": "Expire a credit grant",
    "description": "Expire a credit grant Expires a credit grant. POST /v1/billing/credit_grants/{id}/expire",
    "context": "POST /v1/billing/credit_grants/{id}/expire\nExpires a credit grant.\nRequired: id (path) — Unique identifier for the object.\nOptional: body (body)"
  },
  {
    "id": "PostBillingCreditGrantsIdVoid",
    "name": "Void a credit grant",
    "description": "Void a credit grant Voids a credit grant. POST /v1/billing/credit_grants/{id}/void",
    "context": "POST /v1/billing/credit_grants/{id}/void\nVoids a credit grant.\nRequired: id (path) — Unique identifier for the object.\nOptional: body (body)"
  },
  {
    "id": "PostBillingFeedbackOptions",
    "name": "Create a feedback option",
    "description": "Create a feedback option Creates a new feedback option. POST /v1/billing/feedback_options",
    "context": "POST /v1/billing/feedback_options\nCreates a new feedback option.\nRequired: body (body)"
  },
  {
    "id": "PostBillingFeedbackOptionsId",
    "name": "Update a feedback option",
    "description": "Update a feedback option Updates the description of an existing feedback option. POST /v1/billing/feedback_options/{id}",
    "context": "POST /v1/billing/feedback_options/{id}\nUpdates the description of an existing feedback option.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "PostBillingFeedbackOptionsIdDeactivate",
    "name": "Deactivate a feedback option",
    "description": "Deactivate a feedback option Deactivates a feedback option. POST /v1/billing/feedback_options/{id}/deactivate",
    "context": "POST /v1/billing/feedback_options/{id}/deactivate\nDeactivates a feedback option. Deactivated feedback options cannot be used in portal configurations.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "PostBillingMeterEventAdjustments",
    "name": "Create a billing meter event adjustment",
    "description": "Create a billing meter event adjustment Creates a billing meter event adjustment. POST /v1/billing/meter_event_adjustments",
    "context": "POST /v1/billing/meter_event_adjustments\nCreates a billing meter event adjustment.\nRequired: body (body)"
  },
  {
    "id": "PostBillingMeterEvents",
    "name": "Create a billing meter event",
    "description": "Create a billing meter event Creates a billing meter event. POST /v1/billing/meter_events",
    "context": "POST /v1/billing/meter_events\nCreates a billing meter event.\nRequired: body (body)"
  },
  {
    "id": "PostBillingMeters",
    "name": "Create a billing meter",
    "description": "Create a billing meter Creates a billing meter. POST /v1/billing/meters",
    "context": "POST /v1/billing/meters\nCreates a billing meter.\nRequired: body (body)"
  },
  {
    "id": "PostBillingMetersId",
    "name": "Update a billing meter",
    "description": "Update a billing meter Updates a billing meter. POST /v1/billing/meters/{id}",
    "context": "POST /v1/billing/meters/{id}\nUpdates a billing meter.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "PostBillingMetersIdDeactivate",
    "name": "Deactivate a billing meter",
    "description": "Deactivate a billing meter When a meter is deactivated, no more meter events will be accepted for this meter. POST /v1/billing/meters/{id}/deactivate",
    "context": "POST /v1/billing/meters/{id}/deactivate\nWhen a meter is deactivated, no more meter events will be accepted for this meter. You can’t attach a deactivated meter to a price.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "PostBillingMetersIdReactivate",
    "name": "Reactivate a billing meter",
    "description": "Reactivate a billing meter When a meter is reactivated, events for this meter can be accepted and you can attach the meter to a price. POST /v1/billing/meters/{id}/reactivate",
    "context": "POST /v1/billing/meters/{id}/reactivate\nWhen a meter is reactivated, events for this meter can be accepted and you can attach the meter to a price.\nRequired: id (path)\nOptional: body (body)"
  }
]
