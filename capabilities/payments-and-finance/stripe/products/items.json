[
  {
    "id": "DeleteProductsId",
    "name": "Delete a product",
    "description": "Delete a product Delete a product. DELETE /v1/products/{id}",
    "context": "DELETE /v1/products/{id}\nDelete a product. Deleting a product is only possible if it has no prices associated with it. Additionally, deleting a product with type=good is only possible if it has no SKUs associated with it.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "DeleteProductsProductFeaturesId",
    "name": "Remove a feature from a product",
    "description": "Remove a feature from a product Deletes the feature attachment to a product DELETE /v1/products/{product}/features/{id}",
    "context": "DELETE /v1/products/{product}/features/{id}\nDeletes the feature attachment to a product\nRequired: id (path); product (path)\nOptional: body (body)"
  },
  {
    "id": "GetProducts",
    "name": "List all products",
    "description": "List all products Returns a list of your products. GET /v1/products",
    "context": "GET /v1/products\nReturns a list of your products. The products are returned sorted by creation date, with the most recently created products appearing first.\nOptional: active (query) — Only return products that are active or inactive (e.g., pass `false` to list all inactive products).; created (query) — Only return products that were created during the given date interval.; ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; ids (query) — Only return products with the given IDs. Cannot be used with [starting_after](https://api.stripe.com#list_products-starting_after) or [ending_before](https://api.stripe.com#list_products-ending_before).; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; shippable (query) — Only return products that can be shipped (i.e., physical, not digital products).; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; url (query) — Only return products with the given url.; body (body)"
  },
  {
    "id": "GetProductsId",
    "name": "Retrieve a product",
    "description": "Retrieve a product Retrieves the details of an existing product. GET /v1/products/{id}",
    "context": "GET /v1/products/{id}\nRetrieves the details of an existing product. Supply the unique product ID from either a product creation request or the product list, and Stripe will return the corresponding product information.\nRequired: id (path)\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetProductsProductFeatures",
    "name": "List all features attached to a product",
    "description": "List all features attached to a product Retrieve a list of features for a product GET /v1/products/{product}/features",
    "context": "GET /v1/products/{product}/features\nRetrieve a list of features for a product\nRequired: product (path)\nOptional: ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; body (body)"
  },
  {
    "id": "GetProductsProductFeaturesId",
    "name": "Retrieve a product_feature",
    "description": "Retrieve a product_feature Retrieves a product_feature, which represents a feature attachment to a product GET /v1/products/{product}/features/{id}",
    "context": "GET /v1/products/{product}/features/{id}\nRetrieves a product_feature, which represents a feature attachment to a product\nRequired: id (path) — The ID of the product_feature.; product (path) — The ID of the product.\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetProductsSearch",
    "name": "Search products",
    "description": "Search products Search for products you’ve previously created using Stripe’s Search Query Language . GET /v1/products/search",
    "context": "GET /v1/products/search\nSearch for products you’ve previously created using Stripe’s Search Query Language . Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.\nRequired: query (query) — The search query string. See [search query language](https://docs.stripe.com/search#search-query-language) and the list of supported [query fields for products](https://docs.stripe.com/search#query-fields-for-products).\nOptional: expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; page (query) — A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.; body (body)"
  },
  {
    "id": "PostProducts",
    "name": "Create a product",
    "description": "Create a product Creates a new product object. POST /v1/products",
    "context": "POST /v1/products\nCreates a new product object.\nRequired: body (body)"
  },
  {
    "id": "PostProductsId",
    "name": "Update a product",
    "description": "Update a product Updates the specific product by setting the values of the parameters passed. POST /v1/products/{id}",
    "context": "POST /v1/products/{id}\nUpdates the specific product by setting the values of the parameters passed. Any parameters not provided will be left unchanged.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "PostProductsProductFeatures",
    "name": "Attach a feature to a product",
    "description": "Attach a feature to a product Creates a product_feature, which represents a feature attachment to a product POST /v1/products/{product}/features",
    "context": "POST /v1/products/{product}/features\nCreates a product_feature, which represents a feature attachment to a product\nRequired: product (path); body (body)"
  }
]
