[
  {
    "id": "GetTransfers",
    "name": "List all transfers",
    "description": "List all transfers Returns a list of existing transfers sent to connected accounts. GET /v1/transfers",
    "context": "GET /v1/transfers\nReturns a list of existing transfers sent to connected accounts. The transfers are returned in sorted order, with the most recently created transfers appearing first.\nOptional: created (query) — Only return transfers that were created during the given date interval.; destination (query) — Only return transfers for the destination specified by this account ID.; ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; transfer_group (query) — Only return transfers with the specified transfer group.; body (body)"
  },
  {
    "id": "GetTransfersIdReversals",
    "name": "List all reversals",
    "description": "List all reversals You can see a list of the reversals belonging to a specific transfer. GET /v1/transfers/{id}/reversals",
    "context": "GET /v1/transfers/{id}/reversals\nYou can see a list of the reversals belonging to a specific transfer. Note that the 10 most recent reversals are always available by default on the transfer object. If you need more than those 10, you can use this API method and the limit and starting_after parameters to page through additional reversals.\nRequired: id (path)\nOptional: ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; body (body)"
  },
  {
    "id": "GetTransfersTransfer",
    "name": "Retrieve a transfer",
    "description": "Retrieve a transfer Retrieves the details of an existing transfer. GET /v1/transfers/{transfer}",
    "context": "GET /v1/transfers/{transfer}\nRetrieves the details of an existing transfer. Supply the unique transfer ID from either a transfer creation request or the transfer list, and Stripe will return the corresponding transfer information.\nRequired: transfer (path)\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetTransfersTransferReversalsId",
    "name": "Retrieve a reversal",
    "description": "Retrieve a reversal By default, you can see the 10 most recent reversals stored directly on the transfer object, but you can also retrieve details about a specific reversal stored on the transfer. GET /v1/transfers/{transfer}/reversals/{id}",
    "context": "GET /v1/transfers/{transfer}/reversals/{id}\nBy default, you can see the 10 most recent reversals stored directly on the transfer object, but you can also retrieve details about a specific reversal stored on the transfer.\nRequired: id (path); transfer (path)\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "PostTransfers",
    "name": "Create a transfer",
    "description": "Create a transfer To send funds from your Stripe account to a connected account, you create a new transfer object. POST /v1/transfers",
    "context": "POST /v1/transfers\nTo send funds from your Stripe account to a connected account, you create a new transfer object. Your Stripe balance must be able to cover the transfer amount, or you’ll receive an “Insufficient Funds” error.\nRequired: body (body)"
  },
  {
    "id": "PostTransfersIdReversals",
    "name": "Create a transfer reversal",
    "description": "Create a transfer reversal When you create a new reversal, you must specify a transfer to create it on. POST /v1/transfers/{id}/reversals",
    "context": "POST /v1/transfers/{id}/reversals\nWhen you create a new reversal, you must specify a transfer to create it on. When reversing transfers, you can optionally reverse part of the transfer. You can do so as many times as you wish until the entire transfer has been reversed. Once entirely reversed, a transfer can’t be reversed again. This method will return an error when called on an already-reversed transfer, or when trying to reverse more money than is left on a transfer.\nRequired: id (path)\nOptional: body (body)"
  },
  {
    "id": "PostTransfersTransfer",
    "name": "Update a transfer",
    "description": "Update a transfer Updates the specified transfer by setting the values of the parameters passed. POST /v1/transfers/{transfer}",
    "context": "POST /v1/transfers/{transfer}\nUpdates the specified transfer by setting the values of the parameters passed. Any parameters not provided will be left unchanged. This request accepts only metadata as an argument.\nRequired: transfer (path)\nOptional: body (body)"
  },
  {
    "id": "PostTransfersTransferReversalsId",
    "name": "Update a reversal",
    "description": "Update a reversal Updates the specified reversal by setting the values of the parameters passed. POST /v1/transfers/{transfer}/reversals/{id}",
    "context": "POST /v1/transfers/{transfer}/reversals/{id}\nUpdates the specified reversal by setting the values of the parameters passed. Any parameters not provided will be left unchanged. This request only accepts metadata and description as arguments.\nRequired: id (path); transfer (path)\nOptional: body (body)"
  }
]
