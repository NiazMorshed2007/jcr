{
  "id": "checkout",
  "name": "Checkout",
  "description": "Checkout capabilities. Includes Create a Checkout Session, Expire a Checkout Session, List all Checkout Sessions, Retrieve a Checkout Session, Retrieve a Checkout Session's line items, Update a Checkout Session."
}
