[
  {
    "id": "DeletePlansPlan",
    "name": "Delete a plan",
    "description": "Delete a plan Deleting plans means new subscribers can’t be added. DELETE /v1/plans/{plan}",
    "context": "DELETE /v1/plans/{plan}\nDeleting plans means new subscribers can’t be added. Existing subscribers aren’t affected.\nRequired: plan (path)\nOptional: body (body)"
  },
  {
    "id": "GetPlans",
    "name": "List all plans",
    "description": "List all plans Returns a list of your plans. GET /v1/plans",
    "context": "GET /v1/plans\nReturns a list of your plans.\nOptional: active (query) — Only return plans that are active or inactive (e.g., pass `false` to list all inactive plans).; created (query) — A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.; ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; product (query) — Only return plans for the given product.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; body (body)"
  },
  {
    "id": "GetPlansPlan",
    "name": "Retrieve a plan",
    "description": "Retrieve a plan Retrieves the plan with the given ID. GET /v1/plans/{plan}",
    "context": "GET /v1/plans/{plan}\nRetrieves the plan with the given ID.\nRequired: plan (path)\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "PostPlans",
    "name": "Create a plan",
    "description": "Create a plan You can now model subscriptions more flexibly using the Prices API . POST /v1/plans",
    "context": "POST /v1/plans\nYou can now model subscriptions more flexibly using the Prices API . It replaces the Plans API and is backwards compatible to simplify your migration.\nRequired: body (body)"
  },
  {
    "id": "PostPlansPlan",
    "name": "Update a plan",
    "description": "Update a plan Updates the specified plan by setting the values of the parameters passed. POST /v1/plans/{plan}",
    "context": "POST /v1/plans/{plan}\nUpdates the specified plan by setting the values of the parameters passed. Any parameters not provided are left unchanged. By design, you cannot change a plan’s ID, amount, currency, or billing cycle.\nRequired: plan (path)\nOptional: body (body)"
  }
]
