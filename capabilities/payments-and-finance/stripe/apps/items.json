[
  {
    "id": "GetAppsSecrets",
    "name": "List secrets",
    "description": "List secrets List all secrets stored on the given scope. GET /v1/apps/secrets",
    "context": "GET /v1/apps/secrets\nList all secrets stored on the given scope.\nRequired: scope (query) — Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user.\nOptional: ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; body (body)"
  },
  {
    "id": "GetAppsSecretsFind",
    "name": "Find a Secret",
    "description": "Find a Secret Finds a secret in the secret store by name and scope. GET /v1/apps/secrets/find",
    "context": "GET /v1/apps/secrets/find\nFinds a secret in the secret store by name and scope.\nRequired: name (query) — A name for the secret that's unique within the scope.; scope (query) — Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user.\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "PostAppsSecrets",
    "name": "Set a Secret",
    "description": "Set a Secret Create or replace a secret in the secret store. POST /v1/apps/secrets",
    "context": "POST /v1/apps/secrets\nCreate or replace a secret in the secret store.\nRequired: body (body)"
  },
  {
    "id": "PostAppsSecretsDelete",
    "name": "Delete a Secret",
    "description": "Delete a Secret Deletes a secret from the secret store by name and scope. POST /v1/apps/secrets/delete",
    "context": "POST /v1/apps/secrets/delete\nDeletes a secret from the secret store by name and scope.\nRequired: body (body)"
  }
]
