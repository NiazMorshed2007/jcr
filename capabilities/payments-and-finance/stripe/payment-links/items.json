[
  {
    "id": "GetPaymentLinks",
    "name": "List all payment links",
    "description": "List all payment links Returns a list of your payment links. GET /v1/payment_links",
    "context": "GET /v1/payment_links\nReturns a list of your payment links.\nOptional: active (query) — Only return payment links that are active or inactive (e.g., pass `false` to list all inactive payment links).; ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; body (body)"
  },
  {
    "id": "GetPaymentLinksPaymentLink",
    "name": "Retrieve payment link",
    "description": "Retrieve payment link Retrieve a payment link. GET /v1/payment_links/{payment_link}",
    "context": "GET /v1/payment_links/{payment_link}\nRetrieve a payment link.\nRequired: payment_link (path)\nOptional: expand (query) — Specifies which fields in the response should be expanded.; body (body)"
  },
  {
    "id": "GetPaymentLinksPaymentLinkLineItems",
    "name": "Retrieve a payment link's line items",
    "description": "Retrieve a payment link's line items When retrieving a payment link, there is an includable line_items property containing the first handful of those items. GET /v1/payment_links/{payment_link}/line_items",
    "context": "GET /v1/payment_links/{payment_link}/line_items\nWhen retrieving a payment link, there is an includable line_items property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.\nRequired: payment_link (path)\nOptional: ending_before (query) — A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.; expand (query) — Specifies which fields in the response should be expanded.; limit (query) — A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.; starting_after (query) — A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.; body (body)"
  },
  {
    "id": "PostPaymentLinks",
    "name": "Create a payment link",
    "description": "Create a payment link Creates a payment link. POST /v1/payment_links",
    "context": "POST /v1/payment_links\nCreates a payment link.\nRequired: body (body)"
  },
  {
    "id": "PostPaymentLinksPaymentLink",
    "name": "Update a payment link",
    "description": "Update a payment link Updates a payment link. POST /v1/payment_links/{payment_link}",
    "context": "POST /v1/payment_links/{payment_link}\nUpdates a payment link.\nRequired: payment_link (path)\nOptional: body (body)"
  }
]
