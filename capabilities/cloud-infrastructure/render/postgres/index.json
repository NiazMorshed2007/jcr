{
  "id": "postgres",
  "name": "Postgres",
  "description": "Postgres capabilities. Includes Create Postgres export, Create Postgres instance, Create PostgreSQL User, Delete Postgres instance, Delete PostgreSQL User, Failover Postgres instance, List database, table, and index sizes, List live queries, and 13 more."
}
