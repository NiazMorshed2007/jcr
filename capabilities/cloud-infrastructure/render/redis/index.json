{
  "id": "redis",
  "name": "Redis",
  "description": "Redis capabilities. Includes Create Redis instance, Delete Redis instance, List Redis instances, Resume Redis instance, Retrieve Redis connection info, Retrieve Redis instance, Suspend Redis instance, Update Redis instance."
}
