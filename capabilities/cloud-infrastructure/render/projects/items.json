[
  {
    "id": "create-project",
    "name": "Create project",
    "description": "Create project Create a new project. POST /v1/projects",
    "context": "POST /v1/projects\nCreate a new project.\nRequired: body (body)"
  },
  {
    "id": "delete-project",
    "name": "Delete project",
    "description": "Delete project Delete the project with the provided ID. DELETE /v1/projects/{projectId}",
    "context": "DELETE /v1/projects/{projectId}\nDelete the project with the provided ID. Requires _all_ of the project's environments to be empty (i.e., they must contain no services or other resources). Otherwise, deletion fails with a `409` response. To delete a non-empty project, do one of the following: - First move or delete all contained services and other resources. - Delete the project in the [Render Dashboard](https://dashboard.render.com).\nRequired: projectId (path)"
  },
  {
    "id": "list-projects",
    "name": "List projects",
    "description": "List projects List projects matching the provided filters. GET /v1/projects",
    "context": "GET /v1/projects\nList projects matching the provided filters. If no filters are provided, all projects are returned.\nOptional: name (query) — Filter by name; createdBefore (query) — Filter for resources created before a certain time (specified as an ISO 8601 timestamp); createdAfter (query) — Filter for resources created after a certain time (specified as an ISO 8601 timestamp); updatedBefore (query) — Filter for resources updated before a certain time (specified as an ISO 8601 timestamp); updatedAfter (query) — Filter for resources updated after a certain time (specified as an ISO 8601 timestamp); ownerId (query) — The ID of the workspaces to return resources for; cursor (query) — The position in the result list to start from when fetching paginated results. For details, see [Pagination](https://api-docs.render.com/reference/pagination).; limit (query) — The maximum number of items to return. For details, see [Pagination](https://api-docs.render.com/reference/pagination)."
  },
  {
    "id": "retrieve-project",
    "name": "Retrieve Project",
    "description": "Retrieve Project Retrieve the project with the provided ID. GET /v1/projects/{projectId}",
    "context": "GET /v1/projects/{projectId}\nRetrieve the project with the provided ID.\nRequired: projectId (path)"
  },
  {
    "id": "update-project",
    "name": "Update project",
    "description": "Update project Update the details of a project. PATCH /v1/projects/{projectId}",
    "context": "PATCH /v1/projects/{projectId}\nUpdate the details of a project. To update the details of a particular _environment_ in the project, instead use the [Update environment](https://api-docs.render.com/reference/update-environment) endpoint.\nRequired: projectId (path); body (body)"
  }
]
