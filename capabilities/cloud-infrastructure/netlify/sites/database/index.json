{
  "id": "database",
  "name": "Database",
  "description": "Database capabilities. Includes Creates a new database branch, Creates a new database for the specified site, Creates a point-in-time snapshot of a database branch, Deletes a database branch., Deletes a database snapshot., Deletes the database and all associated branches and snapshots for the specified site., Resets a non-production database branch by re-forking it from a source branch (defaults to the production branch), Resets project-level compute settings to tier defaults, and 11 more."
}
