{
  "id": "sites",
  "name": "Sites",
  "description": "Sites capabilities. Includes [Beta] Unlinks the repo from the site, **Note:** Environment variable keys and values have moved from `build_settings.env` and `repo.env` to a new endpoint, Agent runner hooks, Assets, Build hooks, configureDNSForSite, Database, deleteSite, and 26 more."
}
