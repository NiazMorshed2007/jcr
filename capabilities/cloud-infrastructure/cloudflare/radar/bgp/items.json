[
  {
    "id": "radar-get-bgp-hijacks-events",
    "name": "Get BGP hijack events",
    "description": "Get BGP hijack events Retrieves the BGP hijack events. GET /client/v4/radar/bgp/hijacks/events",
    "context": "GET /client/v4/radar/bgp/hijacks/events\nRetrieves the BGP hijack events.\nOptional: page (query) — Current page number, starting from 1.; per_page (query) — Number of entries per page.; eventId (query) — The unique identifier of a event.; hijackerAsn (query) — The potential hijacker AS of a BGP hijack event.; victimAsn (query) — The potential victim AS of a BGP hijack event.; involvedAsn (query) — The potential hijacker or victim AS of a BGP hijack event.; involvedCountry (query) — The country code of the potential hijacker or victim AS of a BGP hijack event.; prefix (query); minConfidence (query) — Filters events by minimum confidence score (1-4 low, 5-7 mid, 8+ high).; maxConfidence (query) — Filters events by maximum confidence score (1-4 low, 5-7 mid, 8+ high).; dateRange (query) — Filters results by a relative date range ending at the current time. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`), e.g. `7d`. Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `3dcontrol` covers days -10 to -7, `7dcontrol` covers days -14 to -7, `28dcontrol` covers days -56 to -28, and `10dcontrol` covers days -24 to -14). Mutually exclusive with `dateStart`/`dateEnd`.; dateStart (query) — Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`.; sortBy (query) — Sorts results by the specified field.; sortOrder (query) — Sort order.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-ips-timeseries",
    "name": "Get announced IP address space time series",
    "description": "Get announced IP address space time series Retrieves time series data for the announced IP space count, represented as the number of IPv4 /24s and IPv6 /48s, for a given ASN. GET /client/v4/radar/bgp/ips/timeseries",
    "context": "GET /client/v4/radar/bgp/ips/timeseries\nRetrieves time series data for the announced IP space count, represented as the number of IPv4 /24s and IPv6 /48s, for a given ASN.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; asn (query) — Filters results by Autonomous System. Specify one or more Autonomous System Numbers (ASNs) as a comma-separated list. Prefix with `-` to exclude ASNs from results. For example, `-174, 3356` excludes results from AS174, but includes results from AS3356.; location (query) — Filters results by location. Specify a comma-separated list of alpha-2 location codes.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; includeDelay (query) — Includes data delay meta information.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-ips-top-ases",
    "name": "Get top ASes by announced IP space",
    "description": "Get top ASes by announced IP space Returns the top-N autonomous systems by announced IP space at the nearest 8-hour RIB boundary at or before the requested date. GET /client/v4/radar/bgp/ips/top/ases",
    "context": "GET /client/v4/radar/bgp/ips/top/ases\nReturns the top-N autonomous systems by announced IP space at the nearest 8-hour RIB boundary at or before the requested date. The snapped boundary is returned as `anchor_ts`.\nOptional: date (query) — Filters results by the specified datetime (ISO 8601).; limit (query) — Limits the number of objects returned in the response.; metric (query) — Ranking metric: IPv4 /24 count or IPv6 /48 count.; country (query) — Optional ISO 3166-1 alpha-2 country filter. Omit for global top-N.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-pfx2as",
    "name": "Get prefix-to-ASN mapping",
    "description": "Get prefix-to-ASN mapping Retrieves the prefix-to-ASN mapping from global routing tables. GET /client/v4/radar/bgp/routes/pfx2as",
    "context": "GET /client/v4/radar/bgp/routes/pfx2as\nRetrieves the prefix-to-ASN mapping from global routing tables.\nOptional: prefix (query); origin (query) — Lookup prefixes originated by the given ASN.; rpkiStatus (query) — Return only results with matching rpki status: valid, invalid or unknown.; longestPrefixMatch (query) — Return only results with the longest prefix match for the given prefix. For example, specify a /32 prefix to lookup the origin ASN for an IPv4 address.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-pfx2as-moas",
    "name": "Get Multi-Origin AS (MOAS) prefixes",
    "description": "Get Multi-Origin AS (MOAS) prefixes Retrieves all Multi-Origin AS (MOAS) prefixes in the global routing tables. GET /client/v4/radar/bgp/routes/moas",
    "context": "GET /client/v4/radar/bgp/routes/moas\nRetrieves all Multi-Origin AS (MOAS) prefixes in the global routing tables.\nOptional: origin (query) — Lookup MOASes originated by the given ASN.; prefix (query); invalid_only (query) — Lookup only RPKI invalid MOASes.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-route-leak-events",
    "name": "Get BGP route leak events",
    "description": "Get BGP route leak events Retrieves the BGP route leak events. GET /client/v4/radar/bgp/leaks/events",
    "context": "GET /client/v4/radar/bgp/leaks/events\nRetrieves the BGP route leak events.\nOptional: page (query) — Current page number, starting from 1.; per_page (query) — Number of entries per page.; eventId (query) — The unique identifier of a event.; leakAsn (query) — The leaking AS of a route leak event.; involvedAsn (query) — ASN that is causing or affected by a route leak event.; involvedCountry (query) — Country code of a involved ASN in a route leak event.; dateRange (query) — Filters results by a relative date range ending at the current time. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`), e.g. `7d`. Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `3dcontrol` covers days -10 to -7, `7dcontrol` covers days -14 to -7, `28dcontrol` covers days -56 to -28, and `10dcontrol` covers days -24 to -14). Mutually exclusive with `dateStart`/`dateEnd`.; dateStart (query) — Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`.; sortBy (query) — Sorts results by the specified field.; sortOrder (query) — Sort order.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-routes-asns",
    "name": "List ASes from global routing tables",
    "description": "List ASes from global routing tables Retrieves all ASes in the current global routing tables with routing statistics. GET /client/v4/radar/bgp/routes/ases",
    "context": "GET /client/v4/radar/bgp/routes/ases\nRetrieves all ASes in the current global routing tables with routing statistics.\nOptional: location (query) — Filters results by location. Specify an alpha-2 location code.; limit (query) — Limits the number of objects returned in the response.; sortBy (query) — Sorts results by the specified field.; sortOrder (query) — Sort order.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-routes-paths",
    "name": "Get tier-1 path segments for an AS",
    "description": "Get tier-1 path segments for an AS Retrieves the paths an AS uses to reach the tier-1 clique, derived from RouteViews RIB snapshots. GET /client/v4/radar/bgp/routes/paths/{asn}",
    "context": "GET /client/v4/radar/bgp/routes/paths/{asn}\nRetrieves the paths an AS uses to reach the tier-1 clique, derived from RouteViews RIB snapshots. Each entry is an ordered AS-path segment (from the queried AS toward a tier-1) with the number of observed paths and peers, and the collectors that observed it. By default segments are merged across all active collectors; pass \"collector\" to scope to one. The response also includes an \"asnInfo\" map (keyed by ASN) with the name and country for every ASN in the returned segments plus the queried ASN (best-effort; null when unavailable).\nRequired: asn (path) — Single Autonomous System Number (ASN) as integer.\nOptional: ipVersion (query) — Address family of the observed paths. Defaults to IPv4.; collector (query) — Scope to a single RouteViews collector (e.g. \"route-views3\"). Omit to merge across all active collectors (identical path segments are deduplicated, observation counts summed, and every contributing collector listed).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-routes-realtime",
    "name": "Get real-time BGP routes for a prefix",
    "description": "Get real-time BGP routes for a prefix Retrieves real-time BGP routes for a prefix, using public real-time data collectors (RouteViews and RIPE RIS). GET /client/v4/radar/bgp/routes/realtime",
    "context": "GET /client/v4/radar/bgp/routes/realtime\nRetrieves real-time BGP routes for a prefix, using public real-time data collectors (RouteViews and RIPE RIS).\nOptional: prefix (query); format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-routes-stats",
    "name": "Get BGP routing table stats",
    "description": "Get BGP routing table stats Retrieves the BGP routing table stats. GET /client/v4/radar/bgp/routes/stats",
    "context": "GET /client/v4/radar/bgp/routes/stats\nRetrieves the BGP routing table stats.\nOptional: asn (query) — Filters results by Autonomous System. Specify a single Autonomous System Number (ASN) as integer.; location (query) — Filters results by location. Specify an alpha-2 location code.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-routes-upstreams-timeseries",
    "name": "Get upstream composition time series for an AS",
    "description": "Get upstream composition time series for an AS Retrieves the share of an AS’s observed paths carried by each direct upstream over time, derived from RouteViews RIB snapshots across all collectors (the combined product). GET /client/v4/radar/bgp/routes/upstreams/{asn}/timeseries",
    "context": "GET /client/v4/radar/bgp/routes/upstreams/{asn}/timeseries\nRetrieves the share of an AS’s observed paths carried by each direct upstream over time, derived from RouteViews RIB snapshots across all collectors (the combined product). Each upstream ASN is returned as its own series of shares (0–1); the least-significant upstreams beyond the requested limit are grouped into an \"OTHER\" series. Series share a common set of timestamps.\nRequired: asn (path) — Single Autonomous System Number (ASN) as integer.\nOptional: ipVersion (query) — Address family of the observed paths. Defaults to IPv4.; dateStart (query) — Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`.; limit (query) — Number of upstream ASNs to return as separate series, ranked by the first bucket. Remaining upstreams are grouped into an \"OTHER\" series. Defaults to 5.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-rpki-aspa-changes",
    "name": "Get ASPA changes over time",
    "description": "Get ASPA changes over time Retrieves ASPA (Autonomous System Provider Authorization) changes over time. GET /client/v4/radar/bgp/rpki/aspa/changes",
    "context": "GET /client/v4/radar/bgp/rpki/aspa/changes\nRetrieves ASPA (Autonomous System Provider Authorization) changes over time. Returns daily aggregated changes including additions, removals, and modifications of ASPA objects.\nOptional: dateStart (query) — Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`.; asn (query) — Filter changes involving this ASN (as customer or provider).; includeAsnInfo (query) — Include ASN metadata (name, country) in response.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-rpki-aspa-snapshot",
    "name": "Get ASPA objects snapshot",
    "description": "Get ASPA objects snapshot Retrieves current or historical ASPA (Autonomous System Provider Authorization) objects. GET /client/v4/radar/bgp/rpki/aspa/snapshot",
    "context": "GET /client/v4/radar/bgp/rpki/aspa/snapshot\nRetrieves current or historical ASPA (Autonomous System Provider Authorization) objects. ASPA objects define which ASNs are authorized upstream providers for a customer ASN.\nOptional: customerAsn (query) — Filter by customer ASN (the ASN publishing the ASPA object).; providerAsn (query) — Filter by provider ASN (an authorized upstream provider in ASPA objects).; date (query) — Filters results by the specified datetime (ISO 8601).; includeAsnInfo (query) — Include ASN metadata (name, country) in response.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-rpki-aspa-timeseries",
    "name": "Get ASPA count time series",
    "description": "Get ASPA count time series Retrieves ASPA (Autonomous System Provider Authorization) object count over time. GET /client/v4/radar/bgp/rpki/aspa/timeseries",
    "context": "GET /client/v4/radar/bgp/rpki/aspa/timeseries\nRetrieves ASPA (Autonomous System Provider Authorization) object count over time. Supports filtering by RIR or location (country code) to generate multiple named series. If no RIR or location filter is specified, returns total count.\nOptional: dateStart (query) — Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`.; name (query) — Array of names used to label the series in the response.; rir (query) — Filter by Regional Internet Registry (RIR). Multiple RIRs generate multiple series.; location (query) — Filters results by location. Specify a comma-separated list of alpha-2 location codes.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-rpki-roas-timeseries",
    "name": "Get RPKI ROA deployment time series",
    "description": "Get RPKI ROA deployment time series Retrieves RPKI ROA (Route Origin Authorization) validation ratios over time. GET /client/v4/radar/bgp/rpki/roas/timeseries",
    "context": "GET /client/v4/radar/bgp/rpki/roas/timeseries\nRetrieves RPKI ROA (Route Origin Authorization) validation ratios over time. Returns the selected metric as a time series. Supports filtering by ASN or location (country code) — multiple values of the same filter type produce one series per value. If no ASN or location is specified, returns the global aggregate.\nOptional: dateStart (query) — Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`.; metric (query) — Which RPKI ROA validation metric to return. validPfxsRatio = ratio of RPKI-valid prefixes (IPv4+IPv6 combined). validPfxsV4Ratio / validPfxsV6Ratio = same, split by IP version. validIpsRatio = ratio of RPKI-valid address space (IPv4 /24s + IPv6 /48s). validIpsV4Ratio / validIpsV6Ratio = same, split by IP version.; asn (query) — Filters results by Autonomous System Number. Specify one or more ASNs. Multiple values generate one series per ASN.; location (query) — Filters results by location. Specify a comma-separated list of alpha-2 location codes.; name (query) — Array of names used to label the series in the response.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-timeseries",
    "name": "Get BGP time series",
    "description": "Get BGP time series Retrieves BGP updates over time. GET /client/v4/radar/bgp/timeseries",
    "context": "GET /client/v4/radar/bgp/timeseries\nRetrieves BGP updates over time. When requesting updates for an autonomous system, only BGP updates of type announcement are returned.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; prefix (query) — Filters results by BGP network prefix.; updateType (query) — Filters results by BGP update type.; asn (query) — Filters results by Autonomous System. Specify one or more Autonomous System Numbers (ASNs) as a comma-separated list. Prefix with `-` to exclude ASNs from results. For example, `-174, 3356` excludes results from AS174, but includes results from AS3356.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-top-ases",
    "name": "Get top ASes by BGP updates",
    "description": "Get top ASes by BGP updates Retrieves the top autonomous systems by BGP updates (announcements only). GET /client/v4/radar/bgp/top/ases",
    "context": "GET /client/v4/radar/bgp/top/ases\nRetrieves the top autonomous systems by BGP updates (announcements only).\nOptional: limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; asn (query) — Filters results by Autonomous System. Specify one or more Autonomous System Numbers (ASNs) as a comma-separated list. Prefix with `-` to exclude ASNs from results. For example, `-174, 3356` excludes results from AS174, but includes results from AS3356.; prefix (query) — Filters results by BGP network prefix.; updateType (query) — Filters results by BGP update type.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-top-asns-by-prefixes",
    "name": "Get top ASes by prefix count",
    "description": "Get top ASes by prefix count Retrieves the full list of autonomous systems on the global routing table ordered by announced prefixes count. GET /client/v4/radar/bgp/top/ases/prefixes",
    "context": "GET /client/v4/radar/bgp/top/ases/prefixes\nRetrieves the full list of autonomous systems on the global routing table ordered by announced prefixes count. The data comes from public BGP MRT data archives and updates every 2 hours.\nOptional: country (query) — Alpha-2 country code.; limit (query) — Maximum number of ASes to return.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-bgp-top-prefixes",
    "name": "Get top prefixes by BGP updates",
    "description": "Get top prefixes by BGP updates Retrieves the top network prefixes by BGP updates. GET /client/v4/radar/bgp/top/prefixes",
    "context": "GET /client/v4/radar/bgp/top/prefixes\nRetrieves the top network prefixes by BGP updates.\nOptional: limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; asn (query) — Filters results by Autonomous System. Specify one or more Autonomous System Numbers (ASNs) as a comma-separated list. Prefix with `-` to exclude ASNs from results. For example, `-174, 3356` excludes results from AS174, but includes results from AS3356.; updateType (query) — Filters results by BGP update type.; format (query) — Format in which results will be returned."
  }
]
