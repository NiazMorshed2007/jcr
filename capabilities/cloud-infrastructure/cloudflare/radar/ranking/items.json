[
  {
    "id": "radar-get-ranking-domain-details",
    "name": "Get domain rank details",
    "description": "Get domain rank details Retrieves domain rank details. GET /client/v4/radar/ranking/domain/{domain}",
    "context": "GET /client/v4/radar/ranking/domain/{domain}\nRetrieves domain rank details. Cloudflare provides an ordered rank for the top 100 domains, but for the remainder it only provides ranking buckets like top 200 thousand, top one million, etc.. These are available through Radar datasets endpoints.\nRequired: domain (path) — Domain name.\nOptional: limit (query) — Limits the number of objects returned in the response.; rankingType (query) — The ranking type.; name (query) — Array of names used to label the series in the response.; includeTopLocations (query) — Includes top locations in the response.; date (query) — Filters results by the specified array of dates.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-ranking-domain-timeseries",
    "name": "Get domains rank time series",
    "description": "Get domains rank time series Retrieves domains rank over time. GET /client/v4/radar/ranking/timeseries_groups",
    "context": "GET /client/v4/radar/ranking/timeseries_groups\nRetrieves domains rank over time.\nOptional: limit (query) — Limits the number of objects returned in the response.; rankingType (query) — The ranking type.; name (query) — Array of names used to label the series in the response.; location (query) — Filters results by location. Specify a comma-separated list of alpha-2 location codes.; domains (query) — Filters results by domain name. Specify a comma-separated list of domain names.; domainCategory (query) — Filters results by domain category.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-ranking-internet-services-categories",
    "name": "List Internet services categories",
    "description": "List Internet services categories Retrieves the list of Internet services categories. GET /client/v4/radar/ranking/internet_services/categories",
    "context": "GET /client/v4/radar/ranking/internet_services/categories\nRetrieves the list of Internet services categories.\nOptional: limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; date (query) — Filters results by the specified array of dates.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-ranking-internet-services-timeseries",
    "name": "Get Internet services rank time series",
    "description": "Get Internet services rank time series Retrieves Internet Services rank update changes over time. GET /client/v4/radar/ranking/internet_services/timeseries_groups",
    "context": "GET /client/v4/radar/ranking/internet_services/timeseries_groups\nRetrieves Internet Services rank update changes over time.\nOptional: serviceCategory (query) — Filters results by Internet service category.; limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-ranking-top-domains",
    "name": "Get top or trending domains",
    "description": "Get top or trending domains Retrieves the top or trending domains based on their rank. GET /client/v4/radar/ranking/top",
    "context": "GET /client/v4/radar/ranking/top\nRetrieves the top or trending domains based on their rank. Popular domains are domains of broad appeal based on how people use the Internet. Trending domains are domains that are generating a surge in interest. For more information on top domains, see https://blog.cloudflare.com/radar-domain-rankings/.\nOptional: limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; location (query) — Filters results by location. Specify a comma-separated list of alpha-2 location codes.; domainCategory (query) — Filters results by domain category.; date (query) — Filters results by the specified array of dates.; rankingType (query) — The ranking type.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-ranking-top-internet-services",
    "name": "Get top Internet services",
    "description": "Get top Internet services Retrieves top Internet services based on their rank. GET /client/v4/radar/ranking/internet_services/top",
    "context": "GET /client/v4/radar/ranking/internet_services/top\nRetrieves top Internet services based on their rank.\nOptional: serviceCategory (query) — Filters results by Internet service category.; limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; date (query) — Filters results by the specified array of dates.; format (query) — Format in which results will be returned."
  }
]
