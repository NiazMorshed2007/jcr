[
  {
    "id": "radar-get-certificate-authorities",
    "name": "List certificate authorities",
    "description": "List certificate authorities Retrieves a list of certificate authorities. GET /client/v4/radar/ct/authorities",
    "context": "GET /client/v4/radar/ct/authorities\nRetrieves a list of certificate authorities.\nOptional: limit (query) — Limits the number of objects returned in the response.; offset (query) — Skips the specified number of objects before fetching the results.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-certificate-authority-details",
    "name": "Get certificate authority details",
    "description": "Get certificate authority details Retrieves the requested CA information. GET /client/v4/radar/ct/authorities/{ca_slug}",
    "context": "GET /client/v4/radar/ct/authorities/{ca_slug}\nRetrieves the requested CA information.\nRequired: ca_slug (path) — Certificate authority SHA256 fingerprint.\nOptional: format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-certificate-log-details",
    "name": "Get certificate log details",
    "description": "Get certificate log details Retrieves the requested certificate log information. GET /client/v4/radar/ct/logs/{log_slug}",
    "context": "GET /client/v4/radar/ct/logs/{log_slug}\nRetrieves the requested certificate log information.\nRequired: log_slug (path) — Certificate log slug.\nOptional: format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-certificate-logs",
    "name": "List certificate logs",
    "description": "List certificate logs Retrieves a list of certificate logs. GET /client/v4/radar/ct/logs",
    "context": "GET /client/v4/radar/ct/logs\nRetrieves a list of certificate logs.\nOptional: limit (query) — Limits the number of objects returned in the response.; offset (query) — Skips the specified number of objects before fetching the results.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-ct-summary",
    "name": "Get certificate distribution by dimension",
    "description": "Get certificate distribution by dimension Retrieves an aggregated summary of certificates grouped by the specified dimension. GET /client/v4/radar/ct/summary/{dimension}",
    "context": "GET /client/v4/radar/ct/summary/{dimension}\nRetrieves an aggregated summary of certificates grouped by the specified dimension.\nRequired: dimension (path) — Specifies the certificate attribute by which to group the results.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; limitPerGroup (query) — Limits the number of objects per group to the top items within the specified time range. When item count exceeds the limit, extra items appear grouped under an \"other\" category. Only supported on high-cardinality dimensions; otherwise the request is rejected. Minimum value is 2.; ca (query) — Filters results by certificate authority.; caOwner (query) — Filters results by certificate authority owner.; duration (query) — Filters results by certificate duration.; entryType (query) — Filters results by entry type (certificate vs. pre-certificate). Incompatible with the `tld` filter/dimension.; expirationStatus (query) — Filters results by expiration status (expired vs. valid).; hasIps (query) — Filters results based on whether the certificates are bound to specific IP addresses.; hasWildcards (query) — Filters results based on whether the certificates contain wildcard domains.; log (query) — Filters results by certificate log. Incompatible with the `tld` filter/dimension.; logApi (query) — Filters results by certificate log API (RFC6962 vs. static). Incompatible with the `tld` filter/dimension.; logOperator (query) — Filters results by certificate log operator. Incompatible with the `tld` filter/dimension.; publicKeyAlgorithm (query) — Filters results by public key algorithm.; signatureAlgorithm (query) — Filters results by signature algorithm.; tld (query) — Filters results by top-level domain. Incompatible with the `log`, `logApi`, `logOperator`, and `entryType` filters/dimensions.; validationLevel (query) — Filters results by validation level.; uniqueEntries (query) — Specifies whether to filter out duplicate certificates and pre-certificates. Set to true for unique entries only.; normalization (query) — Normalization method applied to the results. Refer to [Normalization methods](https://developers.cloudflare.com/radar/concepts/normalization/).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-ct-timeseries",
    "name": "Get certificates time series",
    "description": "Get certificates time series Retrieves certificate volume over time. GET /client/v4/radar/ct/timeseries",
    "context": "GET /client/v4/radar/ct/timeseries\nRetrieves certificate volume over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; ca (query) — Filters results by certificate authority.; caOwner (query) — Filters results by certificate authority owner.; duration (query) — Filters results by certificate duration.; entryType (query) — Filters results by entry type (certificate vs. pre-certificate). Incompatible with the `tld` filter/dimension.; expirationStatus (query) — Filters results by expiration status (expired vs. valid).; hasIps (query) — Filters results based on whether the certificates are bound to specific IP addresses.; hasWildcards (query) — Filters results based on whether the certificates contain wildcard domains.; log (query) — Filters results by certificate log. Incompatible with the `tld` filter/dimension.; logApi (query) — Filters results by certificate log API (RFC6962 vs. static). Incompatible with the `tld` filter/dimension.; logOperator (query) — Filters results by certificate log operator. Incompatible with the `tld` filter/dimension.; publicKeyAlgorithm (query) — Filters results by public key algorithm.; signatureAlgorithm (query) — Filters results by signature algorithm.; tld (query) — Filters results by top-level domain. Incompatible with the `log`, `logApi`, `logOperator`, and `entryType` filters/dimensions.; validationLevel (query) — Filters results by validation level.; uniqueEntries (query) — Specifies whether to filter out duplicate certificates and pre-certificates. Set to true for unique entries only.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-ct-timeseries-group",
    "name": "Get time series of certificate distribution by dimension",
    "description": "Get time series of certificate distribution by dimension Retrieves the distribution of certificates grouped by the specified dimension over time. GET /client/v4/radar/ct/timeseries_groups/{dimension}",
    "context": "GET /client/v4/radar/ct/timeseries_groups/{dimension}\nRetrieves the distribution of certificates grouped by the specified dimension over time.\nRequired: dimension (path) — Specifies the certificate attribute by which to group the results.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; limitPerGroup (query) — Limits the number of objects per group to the top items within the specified time range. When item count exceeds the limit, extra items appear grouped under an \"other\" category. Only supported on high-cardinality dimensions; otherwise the request is rejected. Minimum value is 2.; ca (query) — Filters results by certificate authority.; caOwner (query) — Filters results by certificate authority owner.; duration (query) — Filters results by certificate duration.; entryType (query) — Filters results by entry type (certificate vs. pre-certificate). Incompatible with the `tld` filter/dimension.; expirationStatus (query) — Filters results by expiration status (expired vs. valid).; hasIps (query) — Filters results based on whether the certificates are bound to specific IP addresses.; hasWildcards (query) — Filters results based on whether the certificates contain wildcard domains.; log (query) — Filters results by certificate log. Incompatible with the `tld` filter/dimension.; logApi (query) — Filters results by certificate log API (RFC6962 vs. static). Incompatible with the `tld` filter/dimension.; logOperator (query) — Filters results by certificate log operator. Incompatible with the `tld` filter/dimension.; publicKeyAlgorithm (query) — Filters results by public key algorithm.; signatureAlgorithm (query) — Filters results by signature algorithm.; validationLevel (query) — Filters results by validation level.; tld (query) — Filters results by top-level domain. Incompatible with the `log`, `logApi`, `logOperator`, and `entryType` filters/dimensions.; normalization (query) — Normalization method applied to the results. Refer to [Normalization methods](https://developers.cloudflare.com/radar/concepts/normalization/).; uniqueEntries (query) — Specifies whether to filter out duplicate certificates and pre-certificates. Set to true for unique entries only.; format (query) — Format in which results will be returned."
  }
]
