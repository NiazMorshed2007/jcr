[
  {
    "id": "radar-get-email-routing-summary",
    "name": "Get email routing summary by dimension",
    "description": "Get email routing summary by dimension Retrieves the distribution of email routing metrics by the specified dimension. GET /client/v4/radar/email/routing/summary/{dimension}",
    "context": "GET /client/v4/radar/email/routing/summary/{dimension}\nRetrieves the distribution of email routing metrics by the specified dimension.\nRequired: dimension (path) — Specifies the attribute by which to group the results.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; limitPerGroup (query) — Limits the number of objects per group to the top items within the specified time range. When item count exceeds the limit, extra items appear grouped under an \"other\" category. Only supported on high-cardinality dimensions; otherwise the request is rejected. Minimum value is 2.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-summary-by-arc",
    "name": "Get email ARC validation summary",
    "description": "Get email ARC validation summary Retrieves the distribution of emails by ARC (Authenticated Received Chain) validation. GET /client/v4/radar/email/routing/summary/arc",
    "context": "GET /client/v4/radar/email/routing/summary/arc\nRetrieves the distribution of emails by ARC (Authenticated Received Chain) validation.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-summary-by-dkim",
    "name": "Get email DKIM validation summary",
    "description": "Get email DKIM validation summary Retrieves the distribution of emails by DKIM (DomainKeys Identified Mail) validation. GET /client/v4/radar/email/routing/summary/dkim",
    "context": "GET /client/v4/radar/email/routing/summary/dkim\nRetrieves the distribution of emails by DKIM (DomainKeys Identified Mail) validation.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-summary-by-dmarc",
    "name": "Get email DMARC validation summary",
    "description": "Get email DMARC validation summary Retrieves the distribution of emails by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation. GET /client/v4/radar/email/routing/summary/dmarc",
    "context": "GET /client/v4/radar/email/routing/summary/dmarc\nRetrieves the distribution of emails by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-summary-by-encrypted",
    "name": "Get email encryption status summary",
    "description": "Get email encryption status summary Retrieves the distribution of emails by encryption status (encrypted vs. GET /client/v4/radar/email/routing/summary/encrypted",
    "context": "GET /client/v4/radar/email/routing/summary/encrypted\nRetrieves the distribution of emails by encryption status (encrypted vs. not-encrypted).\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-summary-by-ip-version",
    "name": "Get email IP version summary",
    "description": "Get email IP version summary Retrieves the distribution of emails by IP version. GET /client/v4/radar/email/routing/summary/ip_version",
    "context": "GET /client/v4/radar/email/routing/summary/ip_version\nRetrieves the distribution of emails by IP version.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-summary-by-spf",
    "name": "Get email SPF validation summary",
    "description": "Get email SPF validation summary Retrieves the distribution of emails by SPF (Sender Policy Framework) validation. GET /client/v4/radar/email/routing/summary/spf",
    "context": "GET /client/v4/radar/email/routing/summary/spf\nRetrieves the distribution of emails by SPF (Sender Policy Framework) validation.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-timeseries-group",
    "name": "Get email routing time series grouped by dimension",
    "description": "Get email routing time series grouped by dimension Retrieves the distribution of email routing metrics grouped by dimension over time. GET /client/v4/radar/email/routing/timeseries_groups/{dimension}",
    "context": "GET /client/v4/radar/email/routing/timeseries_groups/{dimension}\nRetrieves the distribution of email routing metrics grouped by dimension over time.\nRequired: dimension (path) — Specifies the attribute by which to group the results.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; limitPerGroup (query) — Limits the number of objects per group to the top items within the specified time range. When item count exceeds the limit, extra items appear grouped under an \"other\" category. Only supported on high-cardinality dimensions; otherwise the request is rejected. Minimum value is 2.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-timeseries-group-by-arc",
    "name": "Get email ARC validation time series",
    "description": "Get email ARC validation time series Retrieves the distribution of emails by ARC (Authenticated Received Chain) validation over time. GET /client/v4/radar/email/routing/timeseries_groups/arc",
    "context": "GET /client/v4/radar/email/routing/timeseries_groups/arc\nRetrieves the distribution of emails by ARC (Authenticated Received Chain) validation over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-timeseries-group-by-dkim",
    "name": "Get email DKIM validation time series",
    "description": "Get email DKIM validation time series Retrieves the distribution of emails by DKIM (DomainKeys Identified Mail) validation over time. GET /client/v4/radar/email/routing/timeseries_groups/dkim",
    "context": "GET /client/v4/radar/email/routing/timeseries_groups/dkim\nRetrieves the distribution of emails by DKIM (DomainKeys Identified Mail) validation over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-timeseries-group-by-dmarc",
    "name": "Get email DMARC validation time series",
    "description": "Get email DMARC validation time series Retrieves the distribution of emails by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation over time. GET /client/v4/radar/email/routing/timeseries_groups/dmarc",
    "context": "GET /client/v4/radar/email/routing/timeseries_groups/dmarc\nRetrieves the distribution of emails by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-timeseries-group-by-encrypted",
    "name": "Get email encryption status time series",
    "description": "Get email encryption status time series Retrieves the distribution of emails by encryption status (encrypted vs. GET /client/v4/radar/email/routing/timeseries_groups/encrypted",
    "context": "GET /client/v4/radar/email/routing/timeseries_groups/encrypted\nRetrieves the distribution of emails by encryption status (encrypted vs. not-encrypted) over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-timeseries-group-by-ip-version",
    "name": "Get email IP version time series",
    "description": "Get email IP version time series Retrieves the distribution of emails by IP version over time. GET /client/v4/radar/email/routing/timeseries_groups/ip_version",
    "context": "GET /client/v4/radar/email/routing/timeseries_groups/ip_version\nRetrieves the distribution of emails by IP version over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-routing-timeseries-group-by-spf",
    "name": "Get email SPF validation time series",
    "description": "Get email SPF validation time series Retrieves the distribution of emails by SPF (Sender Policy Framework) validation over time. GET /client/v4/radar/email/routing/timeseries_groups/spf",
    "context": "GET /client/v4/radar/email/routing/timeseries_groups/spf\nRetrieves the distribution of emails by SPF (Sender Policy Framework) validation over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; ipVersion (query) — Filters results by IP version (Ipv4 vs. IPv6).; encrypted (query) — Filters results by encryption status (encrypted vs. not-encrypted).; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary",
    "name": "Get email security summary by dimension",
    "description": "Get email security summary by dimension Retrieves the distribution of email security metrics by the specified dimension. GET /client/v4/radar/email/security/summary/{dimension}",
    "context": "GET /client/v4/radar/email/security/summary/{dimension}\nRetrieves the distribution of email security metrics by the specified dimension.\nRequired: dimension (path) — Specifies the attribute by which to group the results.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; limitPerGroup (query) — Limits the number of objects per group to the top items within the specified time range. When item count exceeds the limit, extra items appear grouped under an \"other\" category. Only supported on high-cardinality dimensions; otherwise the request is rejected. Minimum value is 2.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary-by-arc",
    "name": "Get email ARC validation summary",
    "description": "Get email ARC validation summary Retrieves the distribution of emails by ARC (Authenticated Received Chain) validation. GET /client/v4/radar/email/security/summary/arc",
    "context": "GET /client/v4/radar/email/security/summary/arc\nRetrieves the distribution of emails by ARC (Authenticated Received Chain) validation.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary-by-dkim",
    "name": "Get email DKIM validation summary",
    "description": "Get email DKIM validation summary Retrieves the distribution of emails by DKIM (DomainKeys Identified Mail) validation. GET /client/v4/radar/email/security/summary/dkim",
    "context": "GET /client/v4/radar/email/security/summary/dkim\nRetrieves the distribution of emails by DKIM (DomainKeys Identified Mail) validation.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary-by-dmarc",
    "name": "Get email DMARC validation summary",
    "description": "Get email DMARC validation summary Retrieves the distribution of emails by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation. GET /client/v4/radar/email/security/summary/dmarc",
    "context": "GET /client/v4/radar/email/security/summary/dmarc\nRetrieves the distribution of emails by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary-by-malicious",
    "name": "Get email malicious classification summary",
    "description": "Get email malicious classification summary Retrieves the distribution of emails by malicious classification. GET /client/v4/radar/email/security/summary/malicious",
    "context": "GET /client/v4/radar/email/security/summary/malicious\nRetrieves the distribution of emails by malicious classification.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary-by-spam",
    "name": "Get email spam classification summary",
    "description": "Get email spam classification summary Retrieves the proportion of emails by spam classification (spam vs. GET /client/v4/radar/email/security/summary/spam",
    "context": "GET /client/v4/radar/email/security/summary/spam\nRetrieves the proportion of emails by spam classification (spam vs. non-spam).\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary-by-spf",
    "name": "Get email SPF validation summary",
    "description": "Get email SPF validation summary Retrieves the distribution of emails by SPF (Sender Policy Framework) validation. GET /client/v4/radar/email/security/summary/spf",
    "context": "GET /client/v4/radar/email/security/summary/spf\nRetrieves the distribution of emails by SPF (Sender Policy Framework) validation.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary-by-spoof",
    "name": "Get email spoof classification summary",
    "description": "Get email spoof classification summary Retrieves the proportion of emails by spoof classification (spoof vs. GET /client/v4/radar/email/security/summary/spoof",
    "context": "GET /client/v4/radar/email/security/summary/spoof\nRetrieves the proportion of emails by spoof classification (spoof vs. non-spoof).\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary-by-threat-category",
    "name": "Get email threat category summary",
    "description": "Get email threat category summary Retrieves the distribution of emails by threat categories. GET /client/v4/radar/email/security/summary/threat_category",
    "context": "GET /client/v4/radar/email/security/summary/threat_category\nRetrieves the distribution of emails by threat categories.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-summary-by-tls-version",
    "name": "Get email TLS version summary",
    "description": "Get email TLS version summary Retrieves the distribution of emails by TLS version. GET /client/v4/radar/email/security/summary/tls_version",
    "context": "GET /client/v4/radar/email/security/summary/tls_version\nRetrieves the distribution of emails by TLS version.\nOptional: name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group",
    "name": "Get email security time series grouped by dimension",
    "description": "Get email security time series grouped by dimension Retrieves the distribution of email security metrics grouped by dimension over time. GET /client/v4/radar/email/security/timeseries_groups/{dimension}",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/{dimension}\nRetrieves the distribution of email security metrics grouped by dimension over time.\nRequired: dimension (path) — Specifies the attribute by which to group the results.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; limitPerGroup (query) — Limits the number of objects per group to the top items within the specified time range. When item count exceeds the limit, extra items appear grouped under an \"other\" category. Only supported on high-cardinality dimensions; otherwise the request is rejected. Minimum value is 2.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group-by-arc",
    "name": "Get email ARC validation time series",
    "description": "Get email ARC validation time series Retrieves the distribution of emails by ARC (Authenticated Received Chain) validation over time. GET /client/v4/radar/email/security/timeseries_groups/arc",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/arc\nRetrieves the distribution of emails by ARC (Authenticated Received Chain) validation over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group-by-dkim",
    "name": "Get email DKIM validation time series",
    "description": "Get email DKIM validation time series Retrieves the distribution of emails by DKIM (DomainKeys Identified Mail) validation over time. GET /client/v4/radar/email/security/timeseries_groups/dkim",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/dkim\nRetrieves the distribution of emails by DKIM (DomainKeys Identified Mail) validation over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group-by-dmarc",
    "name": "Get email DMARC validation time series",
    "description": "Get email DMARC validation time series Retrieves the distribution of emails by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation over time. GET /client/v4/radar/email/security/timeseries_groups/dmarc",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/dmarc\nRetrieves the distribution of emails by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group-by-malicious",
    "name": "Get email malicious classification time series",
    "description": "Get email malicious classification time series Retrieves the distribution of emails by malicious classification over time. GET /client/v4/radar/email/security/timeseries_groups/malicious",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/malicious\nRetrieves the distribution of emails by malicious classification over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group-by-spam",
    "name": "Get email spam classification time series",
    "description": "Get email spam classification time series Retrieves the distribution of emails by spam classification (spam vs. GET /client/v4/radar/email/security/timeseries_groups/spam",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/spam\nRetrieves the distribution of emails by spam classification (spam vs. non-spam) over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group-by-spf",
    "name": "Get email SPF validation time series",
    "description": "Get email SPF validation time series Retrieves the distribution of emails by SPF (Sender Policy Framework) validation over time. GET /client/v4/radar/email/security/timeseries_groups/spf",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/spf\nRetrieves the distribution of emails by SPF (Sender Policy Framework) validation over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group-by-spoof",
    "name": "Get email spoof classification time series",
    "description": "Get email spoof classification time series Retrieves the distribution of emails by spoof classification (spoof vs. GET /client/v4/radar/email/security/timeseries_groups/spoof",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/spoof\nRetrieves the distribution of emails by spoof classification (spoof vs. non-spoof) over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group-by-threat-category",
    "name": "Get email threat category time series",
    "description": "Get email threat category time series Retrieves the distribution of emails by threat category over time. GET /client/v4/radar/email/security/timeseries_groups/threat_category",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/threat_category\nRetrieves the distribution of emails by threat category over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-timeseries-group-by-tls-version",
    "name": "Get email TLS version time series",
    "description": "Get email TLS version time series Retrieves the distribution of emails by TLS version over time. GET /client/v4/radar/email/security/timeseries_groups/tls_version",
    "context": "GET /client/v4/radar/email/security/timeseries_groups/tls_version\nRetrieves the distribution of emails by TLS version over time.\nOptional: aggInterval (query) — Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-top-tlds-by-malicious",
    "name": "Get top TLDs by email malicious classification",
    "description": "Get top TLDs by email malicious classification Retrieves the top TLDs by emails classified as malicious or not. GET /client/v4/radar/email/security/top/tlds/malicious/{malicious}",
    "context": "GET /client/v4/radar/email/security/top/tlds/malicious/{malicious}\nRetrieves the top TLDs by emails classified as malicious or not.\nRequired: malicious (path) — Malicious classification.\nOptional: limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; tldCategory (query) — Filters results by TLD category.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-top-tlds-by-messages",
    "name": "Get top TLDs by email message volume",
    "description": "Get top TLDs by email message volume Retrieves the top TLDs by number of email messages. GET /client/v4/radar/email/security/top/tlds",
    "context": "GET /client/v4/radar/email/security/top/tlds\nRetrieves the top TLDs by number of email messages.\nOptional: limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; tldCategory (query) — Filters results by TLD category.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-top-tlds-by-spam",
    "name": "Get top TLDs by email spam classification",
    "description": "Get top TLDs by email spam classification Retrieves the top TLDs by emails classified as spam or not. GET /client/v4/radar/email/security/top/tlds/spam/{spam}",
    "context": "GET /client/v4/radar/email/security/top/tlds/spam/{spam}\nRetrieves the top TLDs by emails classified as spam or not.\nRequired: spam (path) — Spam classification.\nOptional: limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; tldCategory (query) — Filters results by TLD category.; format (query) — Format in which results will be returned."
  },
  {
    "id": "radar-get-email-security-top-tlds-by-spoof",
    "name": "Get top TLDs by email spoof classification",
    "description": "Get top TLDs by email spoof classification Retrieves the top TLDs by emails classified as spoof or not. GET /client/v4/radar/email/security/top/tlds/spoof/{spoof}",
    "context": "GET /client/v4/radar/email/security/top/tlds/spoof/{spoof}\nRetrieves the top TLDs by emails classified as spoof or not.\nRequired: spoof (path) — Spoof classification.\nOptional: limit (query) — Limits the number of objects returned in the response.; name (query) — Array of names used to label the series in the response.; dateRange (query) — Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).; dateStart (query) — Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; dateEnd (query) — End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.; arc (query) — Filters results by ARC (Authenticated Received Chain) validation.; dkim (query) — Filters results by DKIM (DomainKeys Identified Mail) validation status.; dmarc (query) — Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.; spf (query) — Filters results by SPF (Sender Policy Framework) validation status.; tlsVersion (query) — Filters results by TLS version.; tldCategory (query) — Filters results by TLD category.; format (query) — Format in which results will be returned."
  }
]
