{
  "id": "events",
  "name": "Events",
  "description": "Events capabilities. Includes Adds a tag to an event, Aggregate events by single or multiple columns with optional date filtering, Aggregate indicators by column(s), Bulk update events, Categories, Create, Create a relationship between two events, Creates event references for a event, and 23 more."
}
