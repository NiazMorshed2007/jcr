[
  {
    "id": "tags-delete",
    "name": "Delete tags from an account-level resource",
    "description": "Delete tags from an account-level resource Removes all tags from a specific account-level resource. DELETE /client/v4/accounts/{account_id}/tags",
    "context": "DELETE /client/v4/accounts/{account_id}/tags\nRemoves all tags from a specific account-level resource.\nRequired: account_id (path); body (body)\nOptional: If-Match (header) — ETag value for optimistic concurrency control. When provided, the server will verify the current resource ETag matches before applying the write. Returns 412 Precondition Failed if the resource has been modified since the ETag was obtained. Omit this header for unconditional writes."
  },
  {
    "id": "tags-get",
    "name": "Get tags for an account-level resource",
    "description": "Get tags for an account-level resource Retrieves tags for a specific account-level resource. GET /client/v4/accounts/{account_id}/tags",
    "context": "GET /client/v4/accounts/{account_id}/tags\nRetrieves tags for a specific account-level resource.\nRequired: account_id (path); resource_id (query) — The ID of the resource to retrieve tags for.; resource_type (query) — The type of the resource.\nOptional: worker_id (query) — Worker identifier. Required for worker_version resources."
  },
  {
    "id": "tags-list",
    "name": "List tagged resources",
    "description": "List tagged resources Lists all tagged resources for an account. GET /client/v4/accounts/{account_id}/tags/resources",
    "context": "GET /client/v4/accounts/{account_id}/tags/resources\nLists all tagged resources for an account.\nRequired: account_id (path)\nOptional: type (query) — Filter by resource type. Can be repeated to filter by multiple types (OR logic). Example: ?type=zone&type=worker; name (query) — Filter by resource name. Performs a case-insensitive substring match. Example: ?name=my-zone; id (query) — Filter by resource ID. Can be repeated up to 50 times to filter by multiple IDs. Example: ?id=abc&id=def; case_insensitive (query) — Match `tag` keys and values case-insensitively. Stored casing is unchanged. Example: ?tag=environment=production&case_insensitive=true; tag (query) — Filter resources by tag criteria. This parameter can be repeated multiple times, with AND logic between parameters. Supported syntax: - **Key-only**: `tag=<key>` - Resource must have the tag key (e.g., `tag=production`) - **Key-value**: `tag=<key>=<value>` - Resource must have the tag with specific value (e.g., `tag=env=prod`) - **Multiple values (OR)**: `tag=<key>=<v1>,<v2>` - Resource must have tag with any of the values (e.g., `tag=env=prod,staging`) - **Negate key-only**: `tag=!<key>` - Resource must not have the tag key (e.g., `tag=!archived`) - **Negate key-value**: `tag=<key>!=<value>` - Resource must not have the tag with specific value (e.g., `tag=region!=us-west-1`) Multiple tag parameters are combined with AND logic.; cursor (query) — Cursor for pagination."
  },
  {
    "id": "tags-list-key-summary",
    "name": "List tag key summary",
    "description": "List tag key summary Lists all distinct tag keys and their distinct values across resources in an account. GET /client/v4/accounts/{account_id}/tags/summary",
    "context": "GET /client/v4/accounts/{account_id}/tags/summary\nLists all distinct tag keys and their distinct values across resources in an account.\nRequired: account_id (path)\nOptional: cursor (query) — Cursor for pagination."
  },
  {
    "id": "tags-list-keys",
    "name": "List tag keys",
    "description": "List tag keys Lists all distinct tag keys used across resources in an account. GET /client/v4/accounts/{account_id}/tags/keys",
    "context": "GET /client/v4/accounts/{account_id}/tags/keys\nLists all distinct tag keys used across resources in an account.\nRequired: account_id (path)\nOptional: cursor (query) — Cursor for pagination."
  },
  {
    "id": "tags-list-values",
    "name": "List tag values",
    "description": "List tag values Lists all distinct values for a given tag key, optionally filtered by resource type. GET /client/v4/accounts/{account_id}/tags/values/{tag_key}",
    "context": "GET /client/v4/accounts/{account_id}/tags/values/{tag_key}\nLists all distinct values for a given tag key, optionally filtered by resource type.\nRequired: account_id (path); tag_key (path) — The tag key to retrieve values for.\nOptional: type (query) — Filter by resource type.; cursor (query) — Cursor for pagination."
  },
  {
    "id": "tags-set",
    "name": "Set tags for an account-level resource",
    "description": "Set tags for an account-level resource Creates or updates tags for a specific account-level resource. PUT /client/v4/accounts/{account_id}/tags",
    "context": "PUT /client/v4/accounts/{account_id}/tags\nCreates or updates tags for a specific account-level resource.\nRequired: account_id (path); body (body)\nOptional: If-Match (header) — ETag value for optimistic concurrency control. When provided, the server will verify the current resource ETag matches before applying the write. Returns 412 Precondition Failed if the resource has been modified since the ETag was obtained. Omit this header for unconditional writes."
  }
]
