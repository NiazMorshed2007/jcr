{
  "id": "browser-extension",
  "name": "Browser extension",
  "description": "Browser extension capabilities. Includes Create browser extension configuration, Delete browser extension configuration, Get a single browser extension inventory, Get browser extension configuration, List browser extension inventories, Search browser extension event logs, Update browser extension configuration."
}
