{
  "id": "builds",
  "name": "Builds",
  "description": "Builds capabilities. Includes Cancel a Workers build, Create a build trigger, Create a deploy hook, Create build token, Create or update a repository connection, Create Worker build configuration, Delete a build token, Delete a build trigger, and 23 more."
}
