{
  "id": "pay-per-crawl",
  "name": "Pay per crawl",
  "description": "Pay per crawl capabilities. Includes Creates the stripe config for a crawler, Creates the stripe config for a publisher, Deletes the stripe config for a crawler, Deletes the stripe config for a publisher, Get pay-per-crawl terms, Get pay-per-crawl terms signature status, Get signed pay-per-crawl terms link, Gets the can_be_enabled zone setting, and 5 more."
}
