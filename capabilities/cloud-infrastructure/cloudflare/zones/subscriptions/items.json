[
  {
    "id": "zone-subscription-create-zone-subscriptions",
    "name": "Create Zone Subscription",
    "description": "Create Zone Subscription Create a zone subscription, either plan or add-ons. POST /client/v4/zones/{zone_id}/subscriptions",
    "context": "POST /client/v4/zones/{zone_id}/subscriptions\nCreate a zone subscription, either plan or add-ons. Retained for audit-log coverage. Use the singular `/zones/{zone_id}/subscription` path instead.\nRequired: zone_id (path); body (body)"
  },
  {
    "id": "zone-subscription-delete-zone-subscriptions",
    "name": "Delete Zone Subscription",
    "description": "Delete Zone Subscription Deletes a zone's subscription. DELETE /client/v4/zones/{zone_id}/subscriptions",
    "context": "DELETE /client/v4/zones/{zone_id}/subscriptions\nDeletes a zone's subscription. Retained for audit-log coverage. Use the singular `/zones/{zone_id}/subscription` path instead.\nRequired: zone_id (path)"
  },
  {
    "id": "zone-subscription-update-zone-subscriptions",
    "name": "Update Zone Subscription",
    "description": "Update Zone Subscription Updates zone subscriptions, either plan or add-ons. PUT /client/v4/zones/{zone_id}/subscriptions",
    "context": "PUT /client/v4/zones/{zone_id}/subscriptions\nUpdates zone subscriptions, either plan or add-ons. Retained for audit-log coverage. Use the singular `/zones/{zone_id}/subscription` path instead.\nRequired: zone_id (path); body (body)"
  }
]
