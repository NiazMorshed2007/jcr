{
  "id": "rulesets",
  "name": "Rulesets",
  "description": "Rulesets capabilities. Includes Create a zone ruleset, Create a zone ruleset rule, Delete a zone ruleset, Delete a zone ruleset rule, Delete a zone ruleset version, Get a zone entry point ruleset, Get a zone entry point ruleset version, Get a zone ruleset, and 8 more."
}
