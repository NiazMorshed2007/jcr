{
  "id": "schema-validation",
  "name": "Schema validation",
  "description": "Schema validation capabilities. Includes Bulk edit per-operation schema validation settings, Delete a schema, Delete per-operation schema validation setting, Edit global schema validation settings, Get details of a schema, Get global schema validation settings, Get per-operation schema validation setting, List all uploaded schemas, and 7 more."
}
