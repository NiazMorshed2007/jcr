{
  "id": "token-validation",
  "name": "Token validation",
  "description": "Token validation capabilities. Includes Create a token validation configuration, Create a token validation rule, Create token validation rules, Delete a token validation configuration, Delete a token validation rule, Edit a token validation configuration, Edit a token validation rule, Edit token validation credentials, and 7 more."
}
