{
  "id": "ai-gateway",
  "name": "Ai gateway",
  "description": "Ai gateway capabilities. Includes Create rule, Create virtual model config, Delete rule, Delete virtual model config, Get virtual model config, List rules, List virtual model configs, Update rule, and 1 more."
}
