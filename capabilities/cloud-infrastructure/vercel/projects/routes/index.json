{
  "id": "routes",
  "name": "Routes",
  "description": "Routes capabilities. Includes Add a routing rule, Delete routing rules, Edit a routing rule, Generate a routing rule from natural language, Get project routing rules, Get routing rule version history, Promote, restore, or discard a routing rule version, Stage routing rules."
}
