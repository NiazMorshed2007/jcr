[
  {
    "id": "getRoot",
    "name": "Check registry API version support",
    "description": "Check registry API version support GET /v2/ Docker Registry v2 version check. GET /v2/",
    "context": "GET /v2/\nGET /v2/ Docker Registry v2 version check. Returns a 401 challenge when no credentials are provided, prompting the Docker client to send auth. With valid credentials, returns 200 so the client can proceed."
  },
  {
    "id": "createApiKeys",
    "name": "createApiKeys",
    "description": "createApiKeys POST /api-keys",
    "context": "POST /api-keys\ncreateApiKeys\nOptional: body (body)"
  },
  {
    "id": "createSpeedInsightsToggle",
    "name": "createSpeedInsightsToggle",
    "description": "createSpeedInsightsToggle POST /speed-insights/toggle",
    "context": "POST /speed-insights/toggle\ncreateSpeedInsightsToggle\nRequired: projectId (query)\nOptional: body (body)"
  },
  {
    "id": "createWebInsightsToggle",
    "name": "createWebInsightsToggle",
    "description": "createWebInsightsToggle POST /web/insights/toggle",
    "context": "POST /web/insights/toggle\ncreateWebInsightsToggle\nRequired: projectId (query)\nOptional: body (body)"
  },
  {
    "id": "getByTeamSlugByProjectSlugByRepositoryNameTagsList",
    "name": "List image tags",
    "description": "List image tags GET /v2/:teamSlug/:projectSlug/:repositoryName/tags/list List the tags in a repository. GET /v2/{teamSlug}/{projectSlug}/{repositoryName}/tags/list",
    "context": "GET /v2/{teamSlug}/{projectSlug}/{repositoryName}/tags/list\nGET /v2/:teamSlug/:projectSlug/:repositoryName/tags/list List the tags in a repository.\nRequired: teamSlug (path) — Single Docker repository team slug component.; projectSlug (path) — Single Docker repository project slug component.; repositoryName (path) — Single Docker repository name component.\nOptional: n (query); last (query) — Opaque pagination cursor returned by a previous list response."
  },
  {
    "id": "listEventTypes",
    "name": "List Event Types",
    "description": "List Event Types Returns the list of user-facing event types with descriptions. GET /v1/events/types",
    "context": "GET /v1/events/types\nReturns the list of user-facing event types with descriptions.\nOptional: teamId (query) — The Team identifier to perform the request on behalf of.; slug (query) — The Team slug to perform the request on behalf of."
  },
  {
    "id": "listUserEvents",
    "name": "List User Events",
    "description": "List User Events Retrieves a list of \"events\" generated by the User on Vercel. GET /v3/events",
    "context": "GET /v3/events\nRetrieves a list of \"events\" generated by the User on Vercel. Events are generated when the User performs a particular action, such as logging in, creating a deployment, and joining a Team (just to name a few). When the `teamId` parameter is supplied, then the events that are returned will be in relation to the Team that was specified.\nOptional: limit (query) — Maximum number of items which may be returned.; since (query) — Timestamp to only include items created since then.; until (query) — Timestamp to only include items created until then.; types (query) — Comma-delimited list of event \"types\" to filter the results by.; userId (query) — Deprecated. Use `principalId` instead. If `principalId` and `userId` both exist, `principalId` will be used.; principalId (query) — When retrieving events for a Team, the `principalId` parameter may be specified to filter events generated by a specific principal.; projectIds (query) — Comma-delimited list of project IDs to filter the results by.; entityId (query) — Filters events to those associated with a specific entity (matched against `payload.id`). For example, a connector ID.; withPayload (query) — When set to `true`, the response will include the `payload` field for each event.; teamId (query) — The Team identifier to perform the request on behalf of.; slug (query) — The Team slug to perform the request on behalf of."
  },
  {
    "id": "uploadFile",
    "name": "Upload Deployment Files",
    "description": "Upload Deployment Files Before you create a deployment you need to upload the required files for that deployment. POST /v2/files",
    "context": "POST /v2/files\nBefore you create a deployment you need to upload the required files for that deployment. To do it, you need to first upload each file to this endpoint. Once that's completed, you can create a new deployment with the uploaded files. The file content must be placed inside the body of the request. In the case of a successful response you'll receive a status code 200 with an empty body.\nOptional: Content-Length (header) — The file size in bytes; x-vercel-digest (header) — The file SHA1 used to check the integrity; x-now-digest (header) — The file SHA1 used to check the integrity; x-now-size (header) — The file size as an alternative to `Content-Length`; teamId (query) — The Team identifier to perform the request on behalf of.; slug (query) — The Team slug to perform the request on behalf of.; body (body)"
  }
]
