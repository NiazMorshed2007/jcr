{
  "id": "env",
  "name": "Env",
  "description": "Env capabilities. Includes Create one or more shared environment variables, Delete one or more Env Var, Disconnects a shared environment variable for a given project, Lists all Shared Environment Variables for a team, Retrieve the decrypted value of a Shared Environment Variable by id., Updates one or more shared environment variables."
}
