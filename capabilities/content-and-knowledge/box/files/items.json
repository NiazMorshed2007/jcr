[
  {
    "id": "delete_files_id",
    "name": "Delete file",
    "description": "Delete file Deletes a file, either permanently or by moving it to the trash. DELETE /2.0/files/{file_id}",
    "context": "DELETE /2.0/files/{file_id}\nDeletes a file, either permanently or by moving it to the trash. The enterprise settings determine whether the item will be permanently deleted from Box or moved to the trash.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: if-match (header) — Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
  },
  {
    "id": "delete_files_id_metadata_enterprise_securityClassification-6VMVochwUWo",
    "name": "Remove classification from file",
    "description": "Remove classification from file Removes any classifications from a file. DELETE /2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "context": "DELETE /2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo\nRemoves any classifications from a file. This API can also be called by including the enterprise ID in the URL explicitly, for example `/files/:id//enterprise_12345/securityClassification-6VMVochwUWo`.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
  },
  {
    "id": "delete_files_id_metadata_global_boxSkillsCards",
    "name": "Remove Box Skill cards from file",
    "description": "Remove Box Skill cards from file Removes any Box Skills cards metadata from a file. DELETE /2.0/files/{file_id}/metadata/global/boxSkillsCards",
    "context": "DELETE /2.0/files/{file_id}/metadata/global/boxSkillsCards\nRemoves any Box Skills cards metadata from a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
  },
  {
    "id": "delete_files_id_metadata_id_id",
    "name": "Remove metadata instance from file",
    "description": "Remove metadata instance from file Deletes a piece of file metadata. DELETE /2.0/files/{file_id}/metadata/{scope}/{template_key}",
    "context": "DELETE /2.0/files/{file_id}/metadata/{scope}/{template_key}\nDeletes a piece of file metadata.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; scope (path) — The scope of the metadata template.; template_key (path) — The name of the metadata template."
  },
  {
    "id": "delete_files_id_trash",
    "name": "Permanently remove file",
    "description": "Permanently remove file Permanently deletes a file that is in the trash. DELETE /2.0/files/{file_id}/trash",
    "context": "DELETE /2.0/files/{file_id}/trash\nPermanently deletes a file that is in the trash. This action cannot be undone.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
  },
  {
    "id": "delete_files_id_versions_id",
    "name": "Remove file version",
    "description": "Remove file version Move a file version to the trash. DELETE /2.0/files/{file_id}/versions/{file_version_id}",
    "context": "DELETE /2.0/files/{file_id}/versions/{file_version_id}\nMove a file version to the trash. Versions are only tracked for Box users with premium accounts.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; file_version_id (path) — The ID of the file version.\nOptional: if-match (header) — Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since."
  },
  {
    "id": "delete_files_id_watermark",
    "name": "Remove watermark from file",
    "description": "Remove watermark from file Removes the watermark from a file. DELETE /2.0/files/{file_id}/watermark",
    "context": "DELETE /2.0/files/{file_id}/watermark\nRemoves the watermark from a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
  },
  {
    "id": "delete_files_upload_sessions_id",
    "name": "Remove upload session",
    "description": "Remove upload session Abort an upload session and discard all data uploaded. DELETE /2.0/files/upload_sessions/{upload_session_id}",
    "context": "DELETE /2.0/files/upload_sessions/{upload_session_id}\nAbort an upload session and discard all data uploaded. This cannot be reversed. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) and [`Get upload session`](https://developer.box.com/reference/get-files-upload-sessions-id) endpoints.\nRequired: upload_session_id (path) — The ID of the upload session."
  },
  {
    "id": "get_files_id",
    "name": "Get file information",
    "description": "Get file information Retrieves the details about a file. GET /2.0/files/{file_id}",
    "context": "GET /2.0/files/{file_id}\nRetrieves the details about a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested. Additionally this field can be used to query any metadata applied to the file by specifying the `metadata` field as well as the scope and key of the template to retrieve, for example `?fields=metadata.enterprise_12345.contractTemplate`.; if-none-match (header) — Ensures an item is only returned if it has changed. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `304 Not Modified` if the item has not changed since.; boxapi (header) — The URL, and optional password, for the shared link of this item. This header can be used to access items that have not been explicitly shared with a user. Use the format `shared_link=[link]` or if a password is required then use `shared_link=[link]&shared_link_password=[password]`. This header can be used on the file or folder shared, as well as on any files or folders nested within the item.; x-rep-hints (header) — A header required to request specific `representations` of a file. Use this in combination with the `fields` query parameter to request a specific file representation. The general format for these representations is `X-Rep-Hints: [...]` where `[...]` is one or many hints in the format `[fileType?query]`. For example, to request a `png` representation in `32x32` as well as `64x64` pixel dimensions provide the following hints. `x-rep-hints: [jpg?dimensions=32x32][jpg?dimensions=64x64]` Additionally, a `text` representation is available for all document file types in Box using the `[extracted_text]` representation. `x-rep-hints: [extracted_text]`."
  },
  {
    "id": "get_files_id_app_item_associations",
    "name": "List file app item associations",
    "description": "List file app item associations **This is a beta feature, which means that its availability might be limited.** Returns all app items the file is associated with. GET /2.0/files/{file_id}/app_item_associations",
    "context": "GET /2.0/files/{file_id}/app_item_associations\n**This is a beta feature, which means that its availability might be limited.** Returns all app items the file is associated with. This includes app items associated with ancestors of the file. Assuming the context user has access to the file, the type/ids are revealed even if the context user does not have **View** permission on the app item.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: limit (query) — The maximum number of items to return per page.; marker (query) — Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`.; application_type (query) — If given, only return app items for this application type."
  },
  {
    "id": "get_files_id_collaborations",
    "name": "List file collaborations",
    "description": "List file collaborations Retrieves a list of pending and active collaborations for a file. GET /2.0/files/{file_id}/collaborations",
    "context": "GET /2.0/files/{file_id}/collaborations\nRetrieves a list of pending and active collaborations for a file. This returns all the users that have access to the file or have been invited to the file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested.; limit (query) — The maximum number of items to return per page.; marker (query) — Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`."
  },
  {
    "id": "get_files_id_comments",
    "name": "List file comments",
    "description": "List file comments Retrieves a list of comments for a file. GET /2.0/files/{file_id}/comments",
    "context": "GET /2.0/files/{file_id}/comments\nRetrieves a list of comments for a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested.; limit (query) — The maximum number of items to return per page.; offset (query) — The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
  },
  {
    "id": "get_files_id_content",
    "name": "Download file",
    "description": "Download file Returns the contents of a file in binary format. GET /2.0/files/{file_id}/content",
    "context": "GET /2.0/files/{file_id}/content\nReturns the contents of a file in binary format.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: range (header) — The byte range of the content to download. The format `bytes={start_byte}-{end_byte}` can be used to specify what section of the file to download.; boxapi (header) — The URL, and optional password, for the shared link of this item. This header can be used to access items that have not been explicitly shared with a user. Use the format `shared_link=[link]` or if a password is required then use `shared_link=[link]&shared_link_password=[password]`. This header can be used on the file or folder shared, as well as on any files or folders nested within the item.; version (query) — The file version to download.; access_token (query) — An optional access token that can be used to pre-authenticate this request, which means that a download link can be shared with a browser or a third party service without them needing to know how to handle the authentication. When using this parameter, please make sure that the access token is sufficiently scoped down to only allow read access to that file and no other files or folders."
  },
  {
    "id": "get_files_id_metadata",
    "name": "List metadata instances on file",
    "description": "List metadata instances on file Retrieves all metadata for a given file. GET /2.0/files/{file_id}/metadata",
    "context": "GET /2.0/files/{file_id}/metadata\nRetrieves all metadata for a given file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: view (query) — Taxonomy field values are returned in `API view` by default, meaning the value is represented with a taxonomy node identifier. To retrieve the `Hydrated view`, where taxonomy values are represented with the full taxonomy node information, set this parameter to `hydrated`. This is the only supported value for this parameter."
  },
  {
    "id": "get_files_id_metadata_enterprise_securityClassification-6VMVochwUWo",
    "name": "Get classification on file",
    "description": "Get classification on file Retrieves the classification metadata instance that has been applied to a file. GET /2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "context": "GET /2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo\nRetrieves the classification metadata instance that has been applied to a file. This API can also be called by including the enterprise ID in the URL explicitly, for example `/files/:id//enterprise_12345/securityClassification-6VMVochwUWo`.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
  },
  {
    "id": "get_files_id_metadata_global_boxSkillsCards",
    "name": "List Box Skill cards on file",
    "description": "List Box Skill cards on file List the Box Skills metadata cards that are attached to a file. GET /2.0/files/{file_id}/metadata/global/boxSkillsCards",
    "context": "GET /2.0/files/{file_id}/metadata/global/boxSkillsCards\nList the Box Skills metadata cards that are attached to a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
  },
  {
    "id": "get_files_id_metadata_id_id",
    "name": "Get metadata instance on file",
    "description": "Get metadata instance on file Retrieves the instance of a metadata template that has been applied to a file. GET /2.0/files/{file_id}/metadata/{scope}/{template_key}",
    "context": "GET /2.0/files/{file_id}/metadata/{scope}/{template_key}\nRetrieves the instance of a metadata template that has been applied to a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; scope (path) — The scope of the metadata template.; template_key (path) — The name of the metadata template.\nOptional: view (query) — Taxonomy field values are returned in `API view` by default, meaning the value is represented with a taxonomy node identifier. To retrieve the `Hydrated view`, where taxonomy values are represented with the full taxonomy node information, set this parameter to `hydrated`. This is the only supported value for this parameter."
  },
  {
    "id": "get_files_id_tasks",
    "name": "List tasks on file",
    "description": "List tasks on file Retrieves a list of all the tasks for a file. GET /2.0/files/{file_id}/tasks",
    "context": "GET /2.0/files/{file_id}/tasks\nRetrieves a list of all the tasks for a file. This endpoint does not support pagination.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
  },
  {
    "id": "get_files_id_thumbnail_id",
    "name": "Get file thumbnail",
    "description": "Get file thumbnail Retrieves a thumbnail, or smaller image representation, of a file. GET /2.0/files/{file_id}/thumbnail.{extension}",
    "context": "GET /2.0/files/{file_id}/thumbnail.{extension}\nRetrieves a thumbnail, or smaller image representation, of a file. Sizes of `32x32`,`64x64`, `128x128`, and `256x256` can be returned in the `.png` format and sizes of `32x32`, `160x160`, and `320x320` can be returned in the `.jpg` format. Thumbnails can be generated for the image and video file formats listed [found on our community site][1]. [1]: https://community.box.com/t5/Migrating-and-Previewing-Content/File-Types-and-Fonts-Supported-in-Box-Content-Preview/ta-p/327\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; extension (path) — The file format for the thumbnail.\nOptional: min_height (query) — The minimum height of the thumbnail.; min_width (query) — The minimum width of the thumbnail.; max_height (query) — The maximum height of the thumbnail.; max_width (query) — The maximum width of the thumbnail."
  },
  {
    "id": "get_files_id_trash",
    "name": "Get trashed file",
    "description": "Get trashed file Retrieves a file that has been moved to the trash. GET /2.0/files/{file_id}/trash",
    "context": "GET /2.0/files/{file_id}/trash\nRetrieves a file that has been moved to the trash. Please note that only if the file itself has been moved to the trash can it be retrieved with this API call. If instead one of its parent folders was moved to the trash, only that folder can be inspected using the [`GET /folders/:id/trash`](https://developer.box.com/reference/get-folders-id-trash) API. To list all items that have been moved to the trash, please use the [`GET /folders/trash/items`](https://developer.box.com/reference/get-folders-trash-items/) API.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested."
  },
  {
    "id": "get_files_id_versions",
    "name": "List all file versions",
    "description": "List all file versions Retrieve a list of the past versions for a file. GET /2.0/files/{file_id}/versions",
    "context": "GET /2.0/files/{file_id}/versions\nRetrieve a list of the past versions for a file. Versions are only tracked by Box users with premium accounts. To fetch the ID of the current version of a file, use the `GET /file/:id` API.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested.; limit (query) — The maximum number of items to return per page.; offset (query) — The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response."
  },
  {
    "id": "get_files_id_versions_id",
    "name": "Get file version",
    "description": "Get file version Retrieve a specific version of a file. GET /2.0/files/{file_id}/versions/{file_version_id}",
    "context": "GET /2.0/files/{file_id}/versions/{file_version_id}\nRetrieve a specific version of a file. Versions are only tracked for Box users with premium accounts.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; file_version_id (path) — The ID of the file version.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested."
  },
  {
    "id": "get_files_id_watermark",
    "name": "Get watermark on file",
    "description": "Get watermark on file Retrieve the watermark for a file. GET /2.0/files/{file_id}/watermark",
    "context": "GET /2.0/files/{file_id}/watermark\nRetrieve the watermark for a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`."
  },
  {
    "id": "get_files_id#get_shared_link",
    "name": "Get shared link for file",
    "description": "Get shared link for file Gets the information for a shared link on a file. GET /2.0/files/{file_id}",
    "context": "GET /2.0/files/{file_id}\nGets the information for a shared link on a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; fields (query) — Explicitly request the `shared_link` fields to be returned for this item."
  },
  {
    "id": "get_files_upload_sessions_id",
    "name": "Get upload session",
    "description": "Get upload session Return information about an upload session. GET /2.0/files/upload_sessions/{upload_session_id}",
    "context": "GET /2.0/files/upload_sessions/{upload_session_id}\nReturn information about an upload session. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) endpoint.\nRequired: upload_session_id (path) — The ID of the upload session."
  },
  {
    "id": "get_files_upload_sessions_id_parts",
    "name": "List parts",
    "description": "List parts Return a list of the chunks uploaded to the upload session so far. GET /2.0/files/upload_sessions/{upload_session_id}/parts",
    "context": "GET /2.0/files/upload_sessions/{upload_session_id}/parts\nReturn a list of the chunks uploaded to the upload session so far. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) and [`Get upload session`](https://developer.box.com/reference/get-files-upload-sessions-id) endpoints.\nRequired: upload_session_id (path) — The ID of the upload session.\nOptional: offset (query) — The offset of the item at which to begin the response. Queries with offset parameter value exceeding 10000 will be rejected with a 400 response.; limit (query) — The maximum number of items to return per page."
  },
  {
    "id": "post_files_content",
    "name": "Upload file",
    "description": "Upload file Uploads a small file to Box. POST /2.0/files/content",
    "context": "POST /2.0/files/content\nUploads a small file to Box. For file sizes over 50MB we recommend using the Chunk Upload APIs. The `attributes` part of the body must come **before** the `file` part. Requests that do not follow this format when uploading the file will receive a HTTP `400` error with a `metadata_after_file_contents` error code.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested.; content-md5 (header) — An optional header containing the SHA1 hash of the file to ensure that the file was not corrupted in transit.; body (body)"
  },
  {
    "id": "post_files_id",
    "name": "Restore file",
    "description": "Restore file Restores a file that has been moved to the trash. POST /2.0/files/{file_id}",
    "context": "POST /2.0/files/{file_id}\nRestores a file that has been moved to the trash. An optional new parent ID can be provided to restore the file to in case the original folder has been deleted.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested.; body (body)"
  },
  {
    "id": "post_files_id_content",
    "name": "Upload file version",
    "description": "Upload file version Update a file's content. POST /2.0/files/{file_id}/content",
    "context": "POST /2.0/files/{file_id}/content\nUpdate a file's content. For file sizes over 50MB we recommend using the Chunk Upload APIs. The `attributes` part of the body must come **before** the `file` part. Requests that do not follow this format when uploading the file will receive a HTTP `400` error with a `metadata_after_file_contents` error code.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: if-match (header) — Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since.; fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested.; content-md5 (header) — An optional header containing the SHA1 hash of the file to ensure that the file was not corrupted in transit.; body (body)"
  },
  {
    "id": "post_files_id_copy",
    "name": "Copy file",
    "description": "Copy file Creates a copy of a file. POST /2.0/files/{file_id}/copy",
    "context": "POST /2.0/files/{file_id}/copy\nCreates a copy of a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested.; body (body)"
  },
  {
    "id": "post_files_id_metadata_enterprise_securityClassification-6VMVochwUWo",
    "name": "Add classification to file",
    "description": "Add classification to file Adds a classification to a file by specifying the label of the classification to add. POST /2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "context": "POST /2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo\nAdds a classification to a file by specifying the label of the classification to add. This API can also be called by including the enterprise ID in the URL explicitly, for example `/files/:id//enterprise_12345/securityClassification-6VMVochwUWo`.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: body (body)"
  },
  {
    "id": "post_files_id_metadata_global_boxSkillsCards",
    "name": "Create Box Skill cards on file",
    "description": "Create Box Skill cards on file Applies one or more Box Skills metadata cards to a file. POST /2.0/files/{file_id}/metadata/global/boxSkillsCards",
    "context": "POST /2.0/files/{file_id}/metadata/global/boxSkillsCards\nApplies one or more Box Skills metadata cards to a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: body (body)"
  },
  {
    "id": "post_files_id_metadata_id_id",
    "name": "Create metadata instance on file",
    "description": "Create metadata instance on file Applies an instance of a metadata template to a file. POST /2.0/files/{file_id}/metadata/{scope}/{template_key}",
    "context": "POST /2.0/files/{file_id}/metadata/{scope}/{template_key}\nApplies an instance of a metadata template to a file. In most cases only values that are present in the metadata template will be accepted, except for the `global.properties` template which accepts any key-value pair.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; scope (path) — The scope of the metadata template.; template_key (path) — The name of the metadata template.\nOptional: body (body)"
  },
  {
    "id": "post_files_id_upload_sessions",
    "name": "Create upload session for existing file",
    "description": "Create upload session for existing file Creates an upload session for an existing file. POST /2.0/files/{file_id}/upload_sessions",
    "context": "POST /2.0/files/{file_id}/upload_sessions\nCreates an upload session for an existing file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: body (body)"
  },
  {
    "id": "post_files_id_versions_current",
    "name": "Promote file version",
    "description": "Promote file version Promote a specific version of a file. POST /2.0/files/{file_id}/versions/current",
    "context": "POST /2.0/files/{file_id}/versions/current\nPromote a specific version of a file. If previous versions exist, this method can be used to promote one of the older versions to the top of the version history. This creates a new copy of the old version and puts it at the top of the versions history. The file will have the exact same contents as the older version, with the same hash digest, `etag`, and name as the original. Other properties such as comments do not get updated to their former values. Don't use this endpoint to restore Box Notes, as it works with file formats such as PDF, DOC, PPTX or similar.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested.; body (body)"
  },
  {
    "id": "post_files_upload_sessions",
    "name": "Create upload session",
    "description": "Create upload session Creates an upload session for a new file. POST /2.0/files/upload_sessions",
    "context": "POST /2.0/files/upload_sessions\nCreates an upload session for a new file.\nOptional: body (body)"
  },
  {
    "id": "post_files_upload_sessions_id_commit",
    "name": "Commit upload session",
    "description": "Commit upload session Close an upload session and create a file from the uploaded chunks. POST /2.0/files/upload_sessions/{upload_session_id}/commit",
    "context": "POST /2.0/files/upload_sessions/{upload_session_id}/commit\nClose an upload session and create a file from the uploaded chunks. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) and [`Get upload session`](https://developer.box.com/reference/get-files-upload-sessions-id) endpoints.\nRequired: upload_session_id (path) — The ID of the upload session.; digest (header) — The [RFC3230][1] message digest of the whole file. Only SHA1 is supported. The SHA1 digest must be Base64 encoded. The format of this header is as `sha=BASE64_ENCODED_DIGEST`. [1]: https://tools.ietf.org/html/rfc3230\nOptional: if-match (header) — Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since.; if-none-match (header) — Ensures an item is only returned if it has changed. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `304 Not Modified` if the item has not changed since.; body (body)"
  },
  {
    "id": "post_files_upload_sessions_id_plan",
    "name": "Plan upload session",
    "description": "Plan upload session Plan an upload session by checking which parts already exist on the server. POST /2.0/files/upload_sessions/{upload_session_id}/plan",
    "context": "POST /2.0/files/upload_sessions/{upload_session_id}/plan\nPlan an upload session by checking which parts already exist on the server. This endpoint allows clients to optimize uploads by skipping parts that have already been uploaded (cache hits) and only uploading missing parts. The actual endpoint URL is returned by the [`Create upload session`](e://post-files-upload-sessions) and [`Get upload session`](e://get-files-upload-sessions-id) endpoints.\nRequired: upload_session_id (path) — The ID of the upload session.\nOptional: body (body)"
  },
  {
    "id": "put_files_id",
    "name": "Update file",
    "description": "Update file Updates a file. PUT /2.0/files/{file_id}",
    "context": "PUT /2.0/files/{file_id}\nUpdates a file. This can be used to rename or move a file, create a shared link, or lock a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested.; if-match (header) — Ensures this item hasn't recently changed before making changes. Pass in the item's last observed `etag` value into this header and the endpoint will fail with a `412 Precondition Failed` if it has changed since.; body (body)"
  },
  {
    "id": "put_files_id_metadata_enterprise_securityClassification-6VMVochwUWo",
    "name": "Update classification on file",
    "description": "Update classification on file Updates a classification on a file. PUT /2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo",
    "context": "PUT /2.0/files/{file_id}/metadata/enterprise/securityClassification-6VMVochwUWo\nUpdates a classification on a file. The classification can only be updated if a classification has already been applied to the file before. When editing classifications, only values are defined for the enterprise will be accepted.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: body (body)"
  },
  {
    "id": "put_files_id_metadata_global_boxSkillsCards",
    "name": "Update Box Skill cards on file",
    "description": "Update Box Skill cards on file Updates one or more Box Skills metadata cards to a file. PUT /2.0/files/{file_id}/metadata/global/boxSkillsCards",
    "context": "PUT /2.0/files/{file_id}/metadata/global/boxSkillsCards\nUpdates one or more Box Skills metadata cards to a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: body (body)"
  },
  {
    "id": "put_files_id_metadata_id_id",
    "name": "Update metadata instance on file",
    "description": "Update metadata instance on file Updates a piece of metadata on a file. PUT /2.0/files/{file_id}/metadata/{scope}/{template_key}",
    "context": "PUT /2.0/files/{file_id}/metadata/{scope}/{template_key}\nUpdates a piece of metadata on a file. The metadata instance can only be updated if the template has already been applied to the file before. When editing metadata, only values that match the metadata template schema will be accepted. The update is applied atomically. If any errors occur during the application of the operations, the metadata instance will not be changed.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; scope (path) — The scope of the metadata template.; template_key (path) — The name of the metadata template.\nOptional: body (body)"
  },
  {
    "id": "put_files_id_versions_id",
    "name": "Restore file version",
    "description": "Restore file version Restores a specific version of a file after it was deleted. PUT /2.0/files/{file_id}/versions/{file_version_id}",
    "context": "PUT /2.0/files/{file_id}/versions/{file_version_id}\nRestores a specific version of a file after it was deleted. Don't use this endpoint to restore Box Notes, as it works with file formats such as PDF, DOC, PPTX or similar.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; file_version_id (path) — The ID of the file version.\nOptional: body (body)"
  },
  {
    "id": "put_files_id_watermark",
    "name": "Apply watermark to file",
    "description": "Apply watermark to file Applies or update a watermark on a file. PUT /2.0/files/{file_id}/watermark",
    "context": "PUT /2.0/files/{file_id}/watermark\nApplies or update a watermark on a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.\nOptional: body (body)"
  },
  {
    "id": "put_files_id#add_shared_link",
    "name": "Add shared link to file",
    "description": "Add shared link to file Adds a shared link to a file. PUT /2.0/files/{file_id}",
    "context": "PUT /2.0/files/{file_id}\nAdds a shared link to a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; fields (query) — Explicitly request the `shared_link` fields to be returned for this item.\nOptional: body (body)"
  },
  {
    "id": "put_files_id#remove_shared_link",
    "name": "Remove shared link from file",
    "description": "Remove shared link from file Removes a shared link from a file. PUT /2.0/files/{file_id}",
    "context": "PUT /2.0/files/{file_id}\nRemoves a shared link from a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; fields (query) — Explicitly request the `shared_link` fields to be returned for this item.\nOptional: body (body)"
  },
  {
    "id": "put_files_id#update_shared_link",
    "name": "Update shared link on file",
    "description": "Update shared link on file Updates a shared link on a file. PUT /2.0/files/{file_id}",
    "context": "PUT /2.0/files/{file_id}\nUpdates a shared link on a file.\nRequired: file_id (path) — The unique identifier that represents a file. The ID for any file can be determined by visiting a file in the web application and copying the ID from the URL. For example, for the URL `https://*.app.box.com/files/123` the `file_id` is `123`.; fields (query) — Explicitly request the `shared_link` fields to be returned for this item.\nOptional: body (body)"
  },
  {
    "id": "put_files_upload_sessions_id",
    "name": "Upload part of file",
    "description": "Upload part of file Uploads a chunk of a file for an upload session. PUT /2.0/files/upload_sessions/{upload_session_id}",
    "context": "PUT /2.0/files/upload_sessions/{upload_session_id}\nUploads a chunk of a file for an upload session. The actual endpoint URL is returned by the [`Create upload session`](https://developer.box.com/reference/post-files-upload-sessions) and [`Get upload session`](https://developer.box.com/reference/get-files-upload-sessions-id) endpoints.\nRequired: upload_session_id (path) — The ID of the upload session.; digest (header) — The [RFC3230][1] message digest of the chunk uploaded. Only SHA1 is supported. The SHA1 digest must be base64 encoded. The format of this header is as `sha=BASE64_ENCODED_DIGEST`. To get the value for the `SHA` digest, use the openSSL command to encode the file part: `openssl sha1 -binary <FILE_PART_NAME> | base64`. [1]: https://tools.ietf.org/html/rfc3230; content-range (header) — The byte range of the chunk. Must not overlap with the range of a part already uploaded this session. Each part’s size must be exactly equal in size to the part size specified in the upload session that you created. One exception is the last part of the file, as this can be smaller. When providing the value for `content-range`, remember that: * The lower bound of each part's byte range must be a multiple of the part size. * The higher bound must be a multiple of the part size - 1.\nOptional: body (body)"
  }
]
