[
  {
    "id": "delete_legal_hold_policy_assignments_id",
    "name": "Unassign legal hold policy",
    "description": "Unassign legal hold policy Remove a legal hold from an item. DELETE /2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}",
    "context": "DELETE /2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}\nRemove a legal hold from an item. This is an asynchronous process. The policy will not be fully removed yet when the response returns.\nRequired: legal_hold_policy_assignment_id (path) — The ID of the legal hold policy assignment."
  },
  {
    "id": "get_legal_hold_policy_assignments",
    "name": "List legal hold policy assignments",
    "description": "List legal hold policy assignments Retrieves a list of items a legal hold policy has been assigned to. GET /2.0/legal_hold_policy_assignments",
    "context": "GET /2.0/legal_hold_policy_assignments\nRetrieves a list of items a legal hold policy has been assigned to.\nRequired: policy_id (query) — The ID of the legal hold policy.\nOptional: assign_to_type (query) — Filters the results by the type of item the policy was applied to.; assign_to_id (query) — Filters the results by the ID of item the policy was applied to.; marker (query) — Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`.; limit (query) — The maximum number of items to return per page.; fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested."
  },
  {
    "id": "get_legal_hold_policy_assignments_id",
    "name": "Get legal hold policy assignment",
    "description": "Get legal hold policy assignment Retrieve a legal hold policy assignment. GET /2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}",
    "context": "GET /2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}\nRetrieve a legal hold policy assignment.\nRequired: legal_hold_policy_assignment_id (path) — The ID of the legal hold policy assignment."
  },
  {
    "id": "get_legal_hold_policy_assignments_id_file_versions_on_hold",
    "name": "List previous file versions for legal hold policy assignment",
    "description": "List previous file versions for legal hold policy assignment Get a list of previous file versions for a legal hold assignment. GET /2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/file_versions_on_hold",
    "context": "GET /2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/file_versions_on_hold\nGet a list of previous file versions for a legal hold assignment. In some cases you may only need the latest file versions instead. In these cases, use the `GET /legal_hold_policy_assignments/:id/files_on_hold` API instead to return any current (latest) versions of a file for this legal hold policy assignment. Due to ongoing re-architecture efforts this API might not return all files held for this policy ID. Instead, this API will only return past file versions held in the newly developed architecture. The `GET /file_version_legal_holds` API can be used to fetch current and past versions of files held within the legacy architecture. This endpoint does not support returning any content that is on hold due to a Custodian collaborating on a Hub. The `GET /legal_hold_policy_assignments?policy_id={id}` API can be used to find a list of policy assignments for a given policy ID.\nRequired: legal_hold_policy_assignment_id (path) — The ID of the legal hold policy assignment.\nOptional: marker (query) — Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`.; limit (query) — The maximum number of items to return per page.; fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested."
  },
  {
    "id": "get_legal_hold_policy_assignments_id_files_on_hold",
    "name": "List files with current file versions for legal hold policy assignment",
    "description": "List files with current file versions for legal hold policy assignment Get a list of files with current file versions for a legal hold assignment. GET /2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/files_on_hold",
    "context": "GET /2.0/legal_hold_policy_assignments/{legal_hold_policy_assignment_id}/files_on_hold\nGet a list of files with current file versions for a legal hold assignment. In some cases you may want to get previous file versions instead. In these cases, use the `GET /legal_hold_policy_assignments/:id/file_versions_on_hold` API instead to return any previous versions of a file for this legal hold policy assignment. Due to ongoing re-architecture efforts this API might not return all file versions held for this policy ID. Instead, this API will only return the latest file version held in the newly developed architecture. The `GET /file_version_legal_holds` API can be used to fetch current and past versions of files held within the legacy architecture. This endpoint does not support returning any content that is on hold due to a Custodian collaborating on a Hub. The `GET /legal_hold_policy_assignments?policy_id={id}` API can be used to find a list of policy assignments for a given policy ID.\nRequired: legal_hold_policy_assignment_id (path) — The ID of the legal hold policy assignment.\nOptional: marker (query) — Defines the position marker at which to begin returning results. This is used when paginating using marker-based pagination. This requires `usemarker` to be set to `true`.; limit (query) — The maximum number of items to return per page.; fields (query) — A comma-separated list of attributes to include in the response. This can be used to request fields that are not normally returned in a standard response. Be aware that specifying this parameter will have the effect that none of the standard fields are returned in the response unless explicitly specified, instead only fields for the mini representation are returned, additional to the fields requested."
  },
  {
    "id": "post_legal_hold_policy_assignments",
    "name": "Assign legal hold policy",
    "description": "Assign legal hold policy Assign a legal hold to an item type of: file, file version, folder, user, ownership, or interactions. POST /2.0/legal_hold_policy_assignments",
    "context": "POST /2.0/legal_hold_policy_assignments\nAssign a legal hold to an item type of: file, file version, folder, user, ownership, or interactions.\nOptional: body (body)"
  }
]
