{
  "id": "score-configs",
  "name": "Score configs",
  "description": "Score configs capabilities. Includes Create a score configuration (config), Get a score config, Get all score configs, Update a score config."
}
