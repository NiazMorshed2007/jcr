[
  {
    "id": "organizations_deleteOrganizationMembership",
    "name": "Delete a membership from the organization associated with the API key (requires organization-scoped API key)",
    "description": "Delete a membership from the organization associated with the API key (requires organization-scoped API key) DELETE /api/public/organizations/memberships",
    "context": "DELETE /api/public/organizations/memberships\nDelete a membership from the organization associated with the API key (requires organization-scoped API key)\nRequired: body (body)"
  },
  {
    "id": "organizations_getOrganizationApiKeys",
    "name": "Get all API keys for the organization associated with the API key (requires organization-scoped API key)",
    "description": "Get all API keys for the organization associated with the API key (requires organization-scoped API key) GET /api/public/organizations/apiKeys",
    "context": "GET /api/public/organizations/apiKeys\nGet all API keys for the organization associated with the API key (requires organization-scoped API key)"
  },
  {
    "id": "organizations_getOrganizationMemberships",
    "name": "Get all memberships for the organization associated with the API key (requires organization-scoped API key)",
    "description": "Get all memberships for the organization associated with the API key (requires organization-scoped API key) GET /api/public/organizations/memberships",
    "context": "GET /api/public/organizations/memberships\nGet all memberships for the organization associated with the API key (requires organization-scoped API key)"
  },
  {
    "id": "organizations_getOrganizationProjects",
    "name": "Get all projects for the organization associated with the API key (requires organization-scoped API key)",
    "description": "Get all projects for the organization associated with the API key (requires organization-scoped API key) GET /api/public/organizations/projects",
    "context": "GET /api/public/organizations/projects\nGet all projects for the organization associated with the API key (requires organization-scoped API key)"
  },
  {
    "id": "organizations_updateOrganizationMembership",
    "name": "Create or update a membership for the organization associated with the API key (requires organization-scoped API key)",
    "description": "Create or update a membership for the organization associated with the API key (requires organization-scoped API key) PUT /api/public/organizations/memberships",
    "context": "PUT /api/public/organizations/memberships\nCreate or update a membership for the organization associated with the API key (requires organization-scoped API key)\nRequired: body (body)"
  }
]
