{
  "id": "integrations",
  "name": "Integrations",
  "description": "Integrations capabilities. Includes Create or update a blob storage integration for a specific project (requires organization-scoped API key), Delete a blob storage integration by ID (requires organization-scoped API key), Get all blob storage integrations for the organization (requires organization-scoped API key), Get the sync status of a blob storage integration by integration ID (requires organization-scoped API key)."
}
