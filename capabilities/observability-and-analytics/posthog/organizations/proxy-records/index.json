{
  "id": "proxy-records",
  "name": "Proxy records",
  "description": "Proxy records capabilities. Includes Create a new managed reverse proxy, Delete a reverse proxy, Get details of a specific reverse proxy by ID, List all reverse proxies configured for the organization, Retry provisioning a failed reverse proxy, Run a deep diagnostic on a reverse proxy, Set or clear the HTTPS redirect for requests to the managed proxy domain root.."
}
