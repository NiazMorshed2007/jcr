[
  {
    "id": "cimd_verification_tokens_create",
    "name": "Manage CIMD verification tokens for an organization",
    "description": "Manage CIMD verification tokens for an organization Manage CIMD verification tokens for an organization. POST /api/organizations/{organization_id}/cimd_verification_tokens/",
    "context": "POST /api/organizations/{organization_id}/cimd_verification_tokens/\nManage CIMD verification tokens for an organization. A partner embeds the plaintext token in their CIMD metadata document as `verification_token` inside the `com.posthog` object (the legacy top-level `posthog_verification_token` field still works as a fallback). When PostHog fetches the metadata, matching the token links the partner app to this organization and grants a higher default rate limit for account provisioning. Each token is scoped at creation to the one `cimd_url` it will be published at, and verifies nowhere else. Two organizations may name the same URL; only the one whose token is actually served there verifies, so claiming a URL cannot be used to block a partner from verifying theirs. The plaintext value is only available on creation; we store a hash.\nRequired: organization_id (path) — ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/.; body (body)"
  },
  {
    "id": "cimd_verification_tokens_destroy",
    "name": "Manage CIMD verification tokens for an organization",
    "description": "Manage CIMD verification tokens for an organization Manage CIMD verification tokens for an organization. DELETE /api/organizations/{organization_id}/cimd_verification_tokens/{id}/",
    "context": "DELETE /api/organizations/{organization_id}/cimd_verification_tokens/{id}/\nManage CIMD verification tokens for an organization. A partner embeds the plaintext token in their CIMD metadata document as `verification_token` inside the `com.posthog` object (the legacy top-level `posthog_verification_token` field still works as a fallback). When PostHog fetches the metadata, matching the token links the partner app to this organization and grants a higher default rate limit for account provisioning. Each token is scoped at creation to the one `cimd_url` it will be published at, and verifies nowhere else. Two organizations may name the same URL; only the one whose token is actually served there verifies, so claiming a URL cannot be used to block a partner from verifying theirs. The plaintext value is only available on creation; we store a hash.\nRequired: id (path) — A UUID string identifying this CIMD Verification Token.; organization_id (path) — ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/."
  },
  {
    "id": "cimd_verification_tokens_list",
    "name": "Manage CIMD verification tokens for an organization",
    "description": "Manage CIMD verification tokens for an organization Manage CIMD verification tokens for an organization. GET /api/organizations/{organization_id}/cimd_verification_tokens/",
    "context": "GET /api/organizations/{organization_id}/cimd_verification_tokens/\nManage CIMD verification tokens for an organization. A partner embeds the plaintext token in their CIMD metadata document as `verification_token` inside the `com.posthog` object (the legacy top-level `posthog_verification_token` field still works as a fallback). When PostHog fetches the metadata, matching the token links the partner app to this organization and grants a higher default rate limit for account provisioning. Each token is scoped at creation to the one `cimd_url` it will be published at, and verifies nowhere else. Two organizations may name the same URL; only the one whose token is actually served there verifies, so claiming a URL cannot be used to block a partner from verifying theirs. The plaintext value is only available on creation; we store a hash.\nRequired: organization_id (path) — ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/.\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results."
  },
  {
    "id": "cimd_verification_tokens_partial_update",
    "name": "Manage CIMD verification tokens for an organization",
    "description": "Manage CIMD verification tokens for an organization Manage CIMD verification tokens for an organization. PATCH /api/organizations/{organization_id}/cimd_verification_tokens/{id}/",
    "context": "PATCH /api/organizations/{organization_id}/cimd_verification_tokens/{id}/\nManage CIMD verification tokens for an organization. A partner embeds the plaintext token in their CIMD metadata document as `verification_token` inside the `com.posthog` object (the legacy top-level `posthog_verification_token` field still works as a fallback). When PostHog fetches the metadata, matching the token links the partner app to this organization and grants a higher default rate limit for account provisioning. Each token is scoped at creation to the one `cimd_url` it will be published at, and verifies nowhere else. Two organizations may name the same URL; only the one whose token is actually served there verifies, so claiming a URL cannot be used to block a partner from verifying theirs. The plaintext value is only available on creation; we store a hash.\nRequired: id (path) — A UUID string identifying this CIMD Verification Token.; organization_id (path) — ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/.\nOptional: body (body)"
  },
  {
    "id": "cimd_verification_tokens_retrieve",
    "name": "Manage CIMD verification tokens for an organization",
    "description": "Manage CIMD verification tokens for an organization Manage CIMD verification tokens for an organization. GET /api/organizations/{organization_id}/cimd_verification_tokens/{id}/",
    "context": "GET /api/organizations/{organization_id}/cimd_verification_tokens/{id}/\nManage CIMD verification tokens for an organization. A partner embeds the plaintext token in their CIMD metadata document as `verification_token` inside the `com.posthog` object (the legacy top-level `posthog_verification_token` field still works as a fallback). When PostHog fetches the metadata, matching the token links the partner app to this organization and grants a higher default rate limit for account provisioning. Each token is scoped at creation to the one `cimd_url` it will be published at, and verifies nowhere else. Two organizations may name the same URL; only the one whose token is actually served there verifies, so claiming a URL cannot be used to block a partner from verifying theirs. The plaintext value is only available on creation; we store a hash.\nRequired: id (path) — A UUID string identifying this CIMD Verification Token.; organization_id (path) — ID of the organization you're trying to access. To find the ID of the organization, make a call to /api/organizations/."
  }
]
