{
  "id": "mcp-registry",
  "name": "Mcp registry",
  "description": "Mcp registry capabilities. Includes Full server record: tools, measured stats, per-version scores, connection instructions., Given a task, return the MCP servers most likely to do it, each with its rank rationale, real usage signal where we measure it, and ready-to-run connection instructions, List registry servers ordered by static rank under the chosen ranking version., Rank the same index under several ranking versions side by side, Registered ranking versions and their latest completed runs., Which projects feed MCP Analytics signal into the index, and how much each contributes."
}
