{
  "id": "custom-property-sources",
  "name": "Custom property sources",
  "description": "Custom property sources capabilities. Includes custom_property_sources_create, custom_property_sources_destroy, custom_property_sources_list, custom_property_sources_partial_update, custom_property_sources_retrieve, custom_property_sources_update, Person and group sources only: run what this source reads now — an import for a table binding (a real, billable warehouse sync), a materialization for a view binding, Person and group sources only: start a backfill that reads the whole warehouse table and populates person or group properties for historical rows, and 1 more."
}
