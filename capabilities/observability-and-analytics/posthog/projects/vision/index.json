{
  "id": "vision",
  "name": "Vision",
  "description": "Vision capabilities. Includes A session's observations across every scanner the caller can read, plus the team-level semantic `search` action, which resolves its own scanner scope instead of this queryset., Affected sessions and users for this scanner over the trailing window., Aggregate counts and per-scanner-type distributions over the filtered observation set, AI prompt-rewrite suggestions for a scanner, generated from the team's thumbs up/down ratings., Apply this scanner to many sessions on demand, Apply this scanner to one specific session, on demand, Apply this suggestion: write a config to the scanner (the prompt plus any type-specific config such as classifier tags or the monitor allow_inconclusive flag), bumping the scanner version, and mark the suggestion applied, Copy a scanner into a new disabled scanner named \" (copy)\", and 45 more."
}
