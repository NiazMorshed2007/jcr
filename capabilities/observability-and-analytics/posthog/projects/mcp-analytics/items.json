[
  {
    "id": "mcp_analytics_feedback_create",
    "name": "Create a new MCP feedback submission for the current project.",
    "description": "Create a new MCP feedback submission for the current project. POST /api/projects/{project_id}/mcp_analytics/feedback/",
    "context": "POST /api/projects/{project_id}/mcp_analytics/feedback/\nCreate a new MCP feedback submission for the current project.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "mcp_analytics_feedback_list",
    "name": "List MCP feedback submissions for the current project, newest first.",
    "description": "List MCP feedback submissions for the current project, newest first. GET /api/projects/{project_id}/mcp_analytics/feedback/",
    "context": "GET /api/projects/{project_id}/mcp_analytics/feedback/\nList MCP feedback submissions for the current project, newest first.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results."
  },
  {
    "id": "mcp_analytics_intent_clusters_recompute",
    "name": "Trigger an asynchronous recompute of the intent cluster snapshot",
    "description": "Trigger an asynchronous recompute of the intent cluster snapshot Trigger an asynchronous recompute of the intent cluster snapshot. POST /api/projects/{project_id}/mcp_analytics/intent_clusters/recompute/",
    "context": "POST /api/projects/{project_id}/mcp_analytics/intent_clusters/recompute/\nTrigger an asynchronous recompute of the intent cluster snapshot. The task runs in the background; poll the GET endpoint for progress (status transitions to 'idle' or 'error').\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "mcp_analytics_intent_clusters_retrieve",
    "name": "Return the most recent intent cluster snapshot for the current project",
    "description": "Return the most recent intent cluster snapshot for the current project Return the most recent intent cluster snapshot for the current project. GET /api/projects/{project_id}/mcp_analytics/intent_clusters/",
    "context": "GET /api/projects/{project_id}/mcp_analytics/intent_clusters/\nReturn the most recent intent cluster snapshot for the current project. Returns an empty IDLE snapshot when no clustering run has happened yet.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: tool (query) — Narrow the response to one tool: its pivot entry, the clusters it serves or switches with, and the overlap pairs it belongs to. Coverage meta stays whole-snapshot. Use this for single-tool views so they don't download every cluster and pivot to render one row. An unknown tool returns empty sections, not a 404."
  },
  {
    "id": "mcp_analytics_missing_capabilities_create",
    "name": "Create a new missing capability report for the current project.",
    "description": "Create a new missing capability report for the current project. POST /api/projects/{project_id}/mcp_analytics/missing_capabilities/",
    "context": "POST /api/projects/{project_id}/mcp_analytics/missing_capabilities/\nCreate a new missing capability report for the current project.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "mcp_analytics_missing_capabilities_list",
    "name": "List missing capability reports for the current project, newest first.",
    "description": "List missing capability reports for the current project, newest first. GET /api/projects/{project_id}/mcp_analytics/missing_capabilities/",
    "context": "GET /api/projects/{project_id}/mcp_analytics/missing_capabilities/\nList missing capability reports for the current project, newest first.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results."
  },
  {
    "id": "mcp_analytics_sessions_activity_overview",
    "name": "Aggregate counters, top tools, agent clients, and the most recent tool calls for the last 30 days, computed in one request",
    "description": "Aggregate counters, top tools, agent clients, and the most recent tool calls for the last 30 days, computed in one request Aggregate counters, top tools, agent clients, and the most recent tool calls for the last 30 days, computed in one request. GET /api/projects/{project_id}/mcp_analytics/sessions/activity_overview/",
    "context": "GET /api/projects/{project_id}/mcp_analytics/sessions/activity_overview/\nAggregate counters, top tools, agent clients, and the most recent tool calls for the last 30 days, computed in one request. Powers the dashboard's activity view; always computed fresh so polling callers watch data arrive.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "mcp_analytics_sessions_generate_intent",
    "name": "Generate (or return the cached) LLM summary of the agent's goal for a session, derived from its recorded $mcp_intents",
    "description": "Generate (or return the cached) LLM summary of the agent's goal for a session, derived from its recorded $mcp_intents Generate (or return the cached) LLM summary of the agent's goal for a session, derived from its recorded $mcp_intents. POST /api/projects/{project_id}/mcp_analytics/sessions/{id}/generate_intent/",
    "context": "POST /api/projects/{project_id}/mcp_analytics/sessions/{id}/generate_intent/\nGenerate (or return the cached) LLM summary of the agent's goal for a session, derived from its recorded $mcp_intents. The first call summarises and persists the result; subsequent calls return the stored summary.\nRequired: id (path) — A UUID string identifying this mcp analytics submission.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Absolute ISO timestamp lower bound for the intent scan — pass the session's start so older sessions resolve. Defaults to a 7-day lookback when omitted."
  },
  {
    "id": "mcp_analytics_sessions_intent_digest",
    "name": "Generate (or return the cached) LLM digest of what agents are trying to do with this MCP server, derived from the most recent recorded $mcp_intents across all sessions: a one-sentence summary plus semantic themes, each sized and attributed to tools from the intents themselves",
    "description": "Generate (or return the cached) LLM digest of what agents are trying to do with this MCP server, derived from the most recent recorded $mcp_intents across all sessions: a one-sentence summary plus semantic themes, each sized and attributed to tools from the intents themselves Generate (or return the cached) LLM digest…",
    "context": "POST /api/projects/{project_id}/mcp_analytics/sessions/intent_digest/\nGenerate (or return the cached) LLM digest of what agents are trying to do with this MCP server, derived from the most recent recorded $mcp_intents across all sessions: a one-sentence summary plus semantic themes, each sized and attributed to tools from the intents themselves. Cached by intent corpus and by recency, so repeated calls are cheap and a busy server regenerates at a bounded rate. Powers the dashboard's activity tab.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "mcp_analytics_sessions_list",
    "name": "List MCP sessions for the current project, derived by grouping $mcp_tool_call events by $mcp_session_id",
    "description": "List MCP sessions for the current project, derived by grouping $mcp_tool_call events by $mcp_session_id List MCP sessions for the current project, derived by grouping $mcp_tool_call events by $mcp_session_id. GET /api/projects/{project_id}/mcp_analytics/sessions/",
    "context": "GET /api/projects/{project_id}/mcp_analytics/sessions/\nList MCP sessions for the current project, derived by grouping $mcp_tool_call events by $mcp_session_id. Ordered by newest session start first by default.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Start of the window to aggregate sessions over. PostHog date string — relative (e.g. '-7d', '-24h') or an absolute ISO timestamp. Defaults to '-7d'.; date_to (query) — End of the window. PostHog date string or absolute ISO timestamp. Defaults to now.; limit (query) — Maximum number of sessions to return per page. Defaults to 100; values above 500 are rejected.; offset (query) — Number of sessions to skip before returning results. Combine with limit to page through sessions; the response's has_next flag indicates whether more remain.; order_by (query) — Sort column. Allowed: session_id, session_start, session_end, duration_seconds, tool_call_count, mcp_client_name, distinct_id. Prefix with '-' for descending. Defaults to '-session_start' (newest sessions first).; search (query) — Case-insensitive substring filter matched against session_id, distinct_id, mcp_client_name, and tools_used."
  },
  {
    "id": "mcp_analytics_sessions_tool_calls",
    "name": "List a page of the $mcp_tool_call events that belong to a given $session_id, in chronological order.",
    "description": "List a page of the $mcp_tool_call events that belong to a given $session_id, in chronological order. GET /api/projects/{project_id}/mcp_analytics/sessions/{id}/tool_calls/",
    "context": "GET /api/projects/{project_id}/mcp_analytics/sessions/{id}/tool_calls/\nList a page of the $mcp_tool_call events that belong to a given $session_id, in chronological order.\nRequired: id (path) — A UUID string identifying this mcp analytics submission.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Absolute ISO timestamp lower bound for the event scan — pass the session's start so older sessions resolve. Defaults to a 7-day lookback when omitted or unparseable.; limit (query) — Maximum tool calls to return per page (1–500). Defaults to 500 — the whole page — so a session's calls come back in one request; pass a smaller value for a lighter response. Values above the cap are rejected.; offset (query) — Number of tool calls to skip before returning results. Combine with limit to page through a session's calls; the response's has_next flag indicates whether more remain."
  }
]
