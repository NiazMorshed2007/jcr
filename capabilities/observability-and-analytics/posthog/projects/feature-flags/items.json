[
  {
    "id": "feature_flags_activity_retrieve",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. GET /api/projects/{project_id}/feature_flags/{id}/activity/",
    "context": "GET /api/projects/{project_id}/feature_flags/{id}/activity/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: limit (query) — Number of items per page; page (query) — Page number"
  },
  {
    "id": "feature_flags_all_activity_retrieve",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. GET /api/projects/{project_id}/feature_flags/activity/",
    "context": "GET /api/projects/{project_id}/feature_flags/activity/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: limit (query) — Number of items per page; page (query) — Page number"
  },
  {
    "id": "feature_flags_archive_create",
    "name": "Archive a feature flag, hiding it from the default flag list",
    "description": "Archive a feature flag, hiding it from the default flag list Archive a feature flag, hiding it from the default flag list. POST /api/projects/{project_id}/feature_flags/{id}/archive/",
    "context": "POST /api/projects/{project_id}/feature_flags/{id}/archive/\nArchive a feature flag, hiding it from the default flag list. Sets `archived` to true. An archived flag must be disabled, so an enabled flag also gets `active` set to false in the same write. Targeting, variants and payloads are left as they are, and linked experiment and survey history is preserved. Archiving an enabled flag is refused when other active flags depend on it. An already-archived flag is returned unchanged.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_bulk_delete_create",
    "name": "Bulk delete feature flags by filter criteria or explicit IDs",
    "description": "Bulk delete feature flags by filter criteria or explicit IDs Bulk delete feature flags by filter criteria or explicit IDs. POST /api/projects/{project_id}/feature_flags/bulk_delete/",
    "context": "POST /api/projects/{project_id}/feature_flags/bulk_delete/\nBulk delete feature flags by filter criteria or explicit IDs. Accepts either: - {\"filters\": {...}} - Same filter params as list endpoint (search, active, type, etc.) - {\"ids\": [...]} - Explicit list of flag IDs (no limit) Returns same format as bulk_delete for UI compatibility. Uses bulk operations for efficiency: database updates are batched and cache invalidation happens once at the end rather than per-flag.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "feature_flags_bulk_keys_retrieve",
    "name": "Get feature flag keys by IDs",
    "description": "Get feature flag keys by IDs Get feature flag keys by IDs. POST /api/projects/{project_id}/feature_flags/bulk_keys/",
    "context": "POST /api/projects/{project_id}/feature_flags/bulk_keys/\nGet feature flag keys by IDs. Accepts a list of feature flag IDs and returns a mapping of ID to key.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "feature_flags_bulk_update_tags_create",
    "name": "Bulk update tags on multiple objects",
    "description": "Bulk update tags on multiple objects Bulk update tags on multiple objects. POST /api/projects/{project_id}/feature_flags/bulk_update_tags/",
    "context": "POST /api/projects/{project_id}/feature_flags/bulk_update_tags/\nBulk update tags on multiple objects. PAT access: this action has no ``required_scopes=`` on the decorator — inheriting viewsets must add ``\"bulk_update_tags\"`` to their ``scope_object_write_actions`` list to accept personal API keys. Without that opt-in, ``APIScopePermission`` rejects PAT requests with \"This action does not support personal API key access\". Done per-viewset so granting `` :write`` for one resource doesn't leak access to sibling resources that share this mixin. Accepts: - {\"ids\": [...], \"action\": \"add\"|\"remove\"|\"set\", \"tags\": [\"tag1\", \"tag2\"]} Actions: - \"add\": Add tags to existing tags on each object - \"remove\": Remove specific tags from each object - \"set\": Replace all tags on each object with the provided list\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "feature_flags_create",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. POST /api/projects/{project_id}/feature_flags/",
    "context": "POST /api/projects/{project_id}/feature_flags/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "feature_flags_create_static_cohort_for_flag_create",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. POST /api/projects/{project_id}/feature_flags/{id}/create_static_cohort_for_flag/",
    "context": "POST /api/projects/{project_id}/feature_flags/{id}/create_static_cohort_for_flag/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "feature_flags_dashboard_create",
    "name": "Deprecated",
    "description": "Deprecated Deprecated. POST /api/projects/{project_id}/feature_flags/{id}/dashboard/",
    "context": "POST /api/projects/{project_id}/feature_flags/{id}/dashboard/\nDeprecated. Ensures a saved usage dashboard exists for a feature flag. This endpoint will be removed after September 25, 2026; usage charts remain available on the feature flag Usage tab.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_dependent_flags_list",
    "name": "Get other active flags that depend on this flag.",
    "description": "Get other active flags that depend on this flag. GET /api/projects/{project_id}/feature_flags/{id}/dependent_flags/",
    "context": "GET /api/projects/{project_id}/feature_flags/{id}/dependent_flags/\nGet other active flags that depend on this flag.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_destroy",
    "name": "Hard delete of this model is not allowed",
    "description": "Hard delete of this model is not allowed Hard delete of this model is not allowed. DELETE /api/projects/{project_id}/feature_flags/{id}/",
    "context": "DELETE /api/projects/{project_id}/feature_flags/{id}/\nHard delete of this model is not allowed. Use a patch API call to set \"deleted\" to true\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_disable_create",
    "name": "Disable a feature flag",
    "description": "Disable a feature flag Disable a feature flag. POST /api/projects/{project_id}/feature_flags/{id}/disable/",
    "context": "POST /api/projects/{project_id}/feature_flags/{id}/disable/\nDisable a feature flag. Sets `active` to false and changes nothing else. Targeting, variants, payloads, tags and archived state are left as they are. Refused when other active flags depend on this one. An already-disabled flag is returned unchanged. A disabled flag stops evaluating for every consumer, including a linked experiment or a session replay setting. Read the full definition first to report that impact.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_enable_create",
    "name": "Enable a feature flag",
    "description": "Enable a feature flag Enable a feature flag. POST /api/projects/{project_id}/feature_flags/{id}/enable/",
    "context": "POST /api/projects/{project_id}/feature_flags/{id}/enable/\nEnable a feature flag. Sets `active` to true and changes nothing else. Targeting, variants, payloads, tags and archived state are left as they are. An archived flag is refused: unarchive it first. A flag whose own flag dependencies are disabled or use an unsupported configuration format is also refused. An already-enabled flag is returned unchanged.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_enrich_usage_dashboard_create",
    "name": "Deprecated",
    "description": "Deprecated Deprecated. POST /api/projects/{project_id}/feature_flags/{id}/enrich_usage_dashboard/",
    "context": "POST /api/projects/{project_id}/feature_flags/{id}/enrich_usage_dashboard/\nDeprecated. Adds enriched insights to an existing legacy feature flag usage dashboard. No removal date has been set; usage charts remain available on the feature flag Usage tab.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_evaluation_reasons_retrieve",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. GET /api/projects/{project_id}/feature_flags/evaluation_reasons/",
    "context": "GET /api/projects/{project_id}/feature_flags/evaluation_reasons/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: distinct_id (query) — User distinct ID; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: flag_keys (query) — Optional list of flag keys to scope the response to. When omitted, evaluation reasons are returned for every flag in the project, which can be a very large payload on projects with many flags. Pass the specific flag(s) you are debugging to keep the response small. Accepts either repeated query params (flag_keys=a&flag_keys=b) or a JSON array string (flag_keys=[\"a\",\"b\"]).; groups (query) — Groups for feature flag evaluation (JSON object string)"
  },
  {
    "id": "feature_flags_list",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. GET /api/projects/{project_id}/feature_flags/",
    "context": "GET /api/projects/{project_id}/feature_flags/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: active (query); archived (query) — Filter by archived state. When omitted, archived flags are excluded.; created_by_id (query) — Filter by the user(s) who created the feature flag. Accepts a single user ID, or a JSON-encoded / comma-separated list of user IDs to match any of them.; eligible_for_experiment (query) — When 'true', only return flags that can back an experiment: multivariate with 2-20 variants. Any other value is ignored.; evaluation_runtime (query) — Filter feature flags by their evaluation runtime.; excluded_properties (query) — JSON-encoded list of feature flag keys to exclude from the results.; excluded_tags (query) — JSON-encoded list of tag names to exclude. Flags carrying any of these tags are filtered out.; has_evaluation_contexts (query) — Filter feature flags by presence of evaluation contexts. 'true' returns only flags with at least one evaluation context, 'false' returns only flags without.; key (query) — Filter by exact feature flag key match. Case insensitive.; limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results.; search (query) — Search by feature flag key or name. Case insensitive.; tags (query) — JSON-encoded list of tag names to filter feature flags by.; type (query)"
  },
  {
    "id": "feature_flags_matching_ids_retrieve",
    "name": "Get IDs of all feature flags matching the current filters",
    "description": "Get IDs of all feature flags matching the current filters Get IDs of all feature flags matching the current filters. GET /api/projects/{project_id}/feature_flags/matching_ids/",
    "context": "GET /api/projects/{project_id}/feature_flags/matching_ids/\nGet IDs of all feature flags matching the current filters. Uses the same filtering logic as the list endpoint. Returns only IDs that the user has permission to edit.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_my_flags_retrieve",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. GET /api/projects/{project_id}/feature_flags/my_flags/",
    "context": "GET /api/projects/{project_id}/feature_flags/my_flags/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: flag_keys (query) — Optional list of flag keys to scope the response to. When omitted, every flag in the project is returned with its evaluated value, which can be a very large payload on projects with many flags. Pass the specific flag(s) you want to check to keep the response small. Accepts either repeated query params (flag_keys=a&flag_keys=b) or a JSON array string (flag_keys=[\"a\",\"b\"]).; groups (query) — Groups for feature flag evaluation (JSON object string)"
  },
  {
    "id": "feature_flags_partial_update",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. PATCH /api/projects/{project_id}/feature_flags/{id}/",
    "context": "PATCH /api/projects/{project_id}/feature_flags/{id}/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "feature_flags_remote_config_retrieve",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. GET /api/projects/{project_id}/feature_flags/{id}/remote_config/",
    "context": "GET /api/projects/{project_id}/feature_flags/{id}/remote_config/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_retrieve",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. GET /api/projects/{project_id}/feature_flags/{id}/",
    "context": "GET /api/projects/{project_id}/feature_flags/{id}/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_status_retrieve",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. GET /api/projects/{project_id}/feature_flags/{id}/status/",
    "context": "GET /api/projects/{project_id}/feature_flags/{id}/status/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_test_evaluation_create",
    "name": "Test feature flag evaluation against a specific user at an optional point in time",
    "description": "Test feature flag evaluation against a specific user at an optional point in time Test feature flag evaluation against a specific user at an optional point in time. POST /api/projects/{project_id}/feature_flags/{id}/test_evaluation/",
    "context": "POST /api/projects/{project_id}/feature_flags/{id}/test_evaluation/\nTest feature flag evaluation against a specific user at an optional point in time. This endpoint allows testing how a feature flag would evaluate for a specific user, optionally at a historical timestamp. When a timestamp is provided, both the flag conditions and person properties are evaluated as they existed at that time.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "feature_flags_unarchive_create",
    "name": "Restore an archived feature flag to the default flag list",
    "description": "Restore an archived feature flag to the default flag list Restore an archived feature flag to the default flag list. POST /api/projects/{project_id}/feature_flags/{id}/unarchive/",
    "context": "POST /api/projects/{project_id}/feature_flags/{id}/unarchive/\nRestore an archived feature flag to the default flag list. Sets `archived` to false and changes nothing else. The flag stays disabled; enable it with a separate call. An already-unarchived flag is returned unchanged.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "feature_flags_update",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. PUT /api/projects/{project_id}/feature_flags/{id}/",
    "context": "PUT /api/projects/{project_id}/feature_flags/{id}/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "feature_flags_user_blast_radius_create",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. POST /api/projects/{project_id}/feature_flags/user_blast_radius/",
    "context": "POST /api/projects/{project_id}/feature_flags/user_blast_radius/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "feature_flags_versions_retrieve",
    "name": "Create, read, update and delete feature flags",
    "description": "Create, read, update and delete feature flags Create, read, update and delete feature flags. GET /api/projects/{project_id}/feature_flags/{id}/versions/{version_number}/",
    "context": "GET /api/projects/{project_id}/feature_flags/{id}/versions/{version_number}/\nCreate, read, update and delete feature flags. [See docs](https://posthog.com/docs/feature-flags) for more information on feature flags. If you're looking to use feature flags on your application, you can either use our JavaScript Library or our dedicated endpoint to check if feature flags are enabled for a given user.\nRequired: id (path) — A unique integer value identifying this feature flag.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; version_number (path) — The version number to reconstruct."
  }
]
