[
  {
    "id": "engineering_analytics_author_workflow_costs",
    "name": "One author's estimated CI cost split by workflow over a window (date_from default -30d), highest spend first",
    "description": "One author's estimated CI cost split by workflow over a window (date_from default -30d), highest spend first One author's estimated CI cost split by workflow over a window (date_from default -30d), highest spend first. GET /api/projects/{project_id}/engineering_analytics/author_workflow_costs/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/author_workflow_costs/\nOne author's estimated CI cost split by workflow over a window (date_from default -30d), highest spend first. Runs are attributed to the author through their pull requests (attribution is by PR number). Returns an empty list when the job-level source isn't synced.\nRequired: author (query) — GitHub handle whose CI spend to break down.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_broken_tests",
    "name": "The broken-tests triage panel: live CI failures over the last 2 days grouped into distinct failures (by test id + normalized error signature) and classified by how each is behaving right now — breaking trunk, blocking the merge queue, a new failure spreading across branches, probably-resolved, flaky, or one PR's own problem — ranked with the most urgent first",
    "description": "The broken-tests triage panel: live CI failures over the last 2 days grouped into distinct failures (by test id + normalized error signature) and classified by how each is behaving right now — breaking trunk, blocking the merge queue, a new failure spreading across branches, probably-resolved, flaky, or one PR's own p…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/broken_tests/\nThe broken-tests triage panel: live CI failures over the last 2 days grouped into distinct failures (by test id + normalized error signature) and classified by how each is behaving right now — breaking trunk, blocking the merge queue, a new failure spreading across branches, probably-resolved, flaky, or one PR's own problem — ranked with the most urgent first. A blocking_merge_queue row is a failure on a merge-queue gate branch that never hit trunk: the commit had already passed the PR's own CI, so it is the semantic conflict the queue exists to catch, and it is holding up landings. Also returns breaking_master_jobs, the default-branch jobs whose latest run is red. Reach for this to answer 'what CI failures should I care about right now'; expand a row's latest_run_id via run_failure_logs for the failing lines. Fingerprinting is pytest-only for now (jest/playwright/cargo failures aren't grouped yet), and the breaking/resolved distinction needs the job-level source synced — without it those failures fall through to flaky/pr_only rather than being misreported.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_ci_cards",
    "name": "Headline counts for the open-PR backlog: open PRs, distinct repos, stuck PRs (open, non-draft, non-bot, older than 7 days), and PRs with failing CI",
    "description": "Headline counts for the open-PR backlog: open PRs, distinct repos, stuck PRs (open, non-draft, non-bot, older than 7 days), and PRs with failing CI Headline counts for the open-PR backlog: open PRs, distinct repos, stuck PRs (open, non-draft, non-bot, older than 7 days), and PRs with failing CI. GET /api/projects/{pro…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/ci_cards/\nHeadline counts for the open-PR backlog: open PRs, distinct repos, stuck PRs (open, non-draft, non-bot, older than 7 days), and PRs with failing CI. The failing-CI count rests on the head-SHA join and can lag until late CI completions settle.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_ci_failure_logs",
    "name": "The thinned CI failure logs for a pull request, grouped by failed job",
    "description": "The thinned CI failure logs for a pull request, grouped by failed job The thinned CI failure logs for a pull request, grouped by failed job. GET /api/projects/{project_id}/engineering_analytics/ci_failure_logs/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/ci_failure_logs/\nThe thinned CI failure logs for a pull request, grouped by failed job. Resolves the PR to its workflow runs via the pull_requests association (all of the PR's pushes, not just the latest commit), then reads the Logs product joined on run_id. Returns failed jobs only (the worker fetches logs for failures); logs_available is false when CI hasn't failed, the logs aged out of the short Logs retention, or a fork PR has no run association. Each line carries its original 1-based line number in the full pre-thinning log; lines are the failure region (errors plus surrounding context, with omission markers), capped per job and overall.\nRequired: pr_number (query) — Pull request number whose CI failure logs to fetch.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo (query) — 'owner/name' repository the pull request belongs to.\nOptional: source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_ci_signals_config_retrieve",
    "name": "Return the atomic CI Signals configuration and aggregate GitHub warehouse sync status.",
    "description": "Return the atomic CI Signals configuration and aggregate GitHub warehouse sync status. GET /api/projects/{project_id}/engineering_analytics/ci-signals-config/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/ci-signals-config/\nReturn the atomic CI Signals configuration and aggregate GitHub warehouse sync status.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "engineering_analytics_ci_signals_config_update",
    "name": "Enable or disable all CI signal detectors in one transaction.",
    "description": "Enable or disable all CI signal detectors in one transaction. PUT /api/projects/{project_id}/engineering_analytics/ci-signals-config/",
    "context": "PUT /api/projects/{project_id}/engineering_analytics/ci-signals-config/\nEnable or disable all CI signal detectors in one transaction.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "engineering_analytics_current_branch_health",
    "name": "Current default-branch CI verdict over the fixed last-24-hours window",
    "description": "Current default-branch CI verdict over the fixed last-24-hours window Current default-branch CI verdict over the fixed last-24-hours window. GET /api/projects/{project_id}/engineering_analytics/current_branch_health/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/current_branch_health/\nCurrent default-branch CI verdict over the fixed last-24-hours window. Counts every workflow whose latest completed run failed or timed out; failing workflow names are a bounded preview. The default branch is detected from the same window, independently of analytics date filters.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_delivery_comparison",
    "name": "One author's median ready to merged time, split at the first approval, next to the same medians for the author's own team and for the whole repository, over pull requests merged in the window (date_from default -30d)",
    "description": "One author's median ready to merged time, split at the first approval, next to the same medians for the author's own team and for the whole repository, over pull requests merged in the window (date_from default -30d) One author's median ready to merged time, split at the first approval, next to the same medians for th…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/delivery_comparison/\nOne author's median ready to merged time, split at the first approval, next to the same medians for the author's own team and for the whole repository, over pull requests merged in the window (date_from default -30d). The team is picked from the author's GitHub teams that own code: a team that pr_number asked to review, else the team the author's pull requests asked to review most often, else every team. Bots and drafts are excluded.\nRequired: author (query) — GitHub login of the author to compare with their team and the repository.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; pr_number (query) — A pull request by the author. Needs repo. A team of the author's that this pull request asked to review is the team to compare with, and the pull request stays out of the medians.; repo (query) — 'owner/name' repository. Required with pr_number; otherwise it picks the repository when the selected source syncs several.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_delivery_summary",
    "name": "Delivery and CI friction for one author or one GitHub team over a window (date_from default -30d), each figure next to the same figure over the whole repository: CI spend per merged PR, ready to merged split at the first approval, pushes after approval, merge-queue attempts, and lead time to deploy",
    "description": "Delivery and CI friction for one author or one GitHub team over a window (date_from default -30d), each figure next to the same figure over the whole repository: CI spend per merged PR, ready to merged split at the first approval, pushes after approval, merge-queue attempts, and lead time to deploy Delivery and CI fri…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/delivery_summary/\nDelivery and CI friction for one author or one GitHub team over a window (date_from default -30d), each figure next to the same figure over the whole repository: CI spend per merged PR, ready to merged split at the first approval, pushes after approval, merge-queue attempts, and lead time to deploy. Bots and drafts are excluded. Figures whose optional source isn't synced are null and flagged.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: author (query) — GitHub login: scope the read to this author's pull requests. Pass exactly one scope.; date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; github_team (query) — GitHub team slug: scope the read to pull requests authored by the team's members, through the team membership table. Pass exactly one scope.; repo (query) — 'owner/name' repository. Required with pr_number; otherwise it picks the repository when the selected source syncs several.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_dora",
    "name": "DORA-style deploy metrics over the GitHub deployments + deployment_statuses warehouse pair, each headline with its previous-window twin: deployment frequency, merge-to-deploy lead time (with a per-bucket box-plot series), and honest proxies for change failure rate and time to restore (deploy-status based — no incident data is linked)",
    "description": "DORA-style deploy metrics over the GitHub deployments + deployment_statuses warehouse pair, each headline with its previous-window twin: deployment frequency, merge-to-deploy lead time (with a per-bucket box-plot series), and honest proxies for change failure rate and time to restore (deploy-status based — no incident…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/dora/\nDORA-style deploy metrics over the GitHub deployments + deployment_statuses warehouse pair, each headline with its previous-window twin: deployment frequency, merge-to-deploy lead time (with a per-bucket box-plot series), and honest proxies for change failure rate and time to restore (deploy-status based — no incident data is linked). deploy_data_available is false when the deploy tables aren't synced.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; environment (query) — Deploy environment(s) to scope to, repeatable (from the response's `environments` list). Omit to include all persistent environments marked production or named prod/production (including regional suffixes), falling back to the busiest persistent environment when none match. Explicit names are trimmed, deduplicated, and validated against the source, including transient environments. Blank or unknown names are rejected with a 400 response.; github_team (query) — GitHub team slug (from the response's `github_teams` list) to narrow the PR-scoped merge-to-deploy figures to that team's authors. Deploy counts stay repo-wide. Needs the team-membership snapshot synced; without it the merge-to-deploy figures return empty rather than silently unfiltered.; granularity (query) — Bucket width for every series. Omit to pick one that fits the window: hour up to 48h, day up to 90 days, week beyond.; repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_flaky_tests",
    "name": "The active test-health queue: pytest and Jest tests worth acting on now, from the per-test CI spans, over a window (default -7d, maximum 30 days)",
    "description": "The active test-health queue: pytest and Jest tests worth acting on now, from the per-test CI spans, over a window (default -7d, maximum 30 days) The active test-health queue: pytest and Jest tests worth acting on now, from the per-test CI spans, over a window (default -7d, maximum 30 days). GET /api/projects/{project…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/flaky_tests/\nThe active test-health queue: pytest and Jest tests worth acting on now, from the per-test CI spans, over a window (default -7d, maximum 30 days). Evidence is counted per CI run, never per span or run attempt. A test is a 'confirmed_flake' when one commit both failed and passed it (a 'Re-run failed jobs' attempt went green, or an in-job retry recovered it); 'quarantined' when a tolerated failure is recorded while it is masked; otherwise 'suspected_regression'. It qualifies on any same-commit recovery, any master/main failure, a quarantined failure, or failures on at least min_failed_prs distinct PRs. Counts are absolute, never rates: CI emits every failure but omits ordinary passing spans, so there is no execution denominator. 'suspected_regression' means no recovery was recorded in this data, not that the test never flakes.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Window start: relative ('-7d', '-30d') or ISO8601. Defaults to -7d; the window may span at most 30 days.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; limit (query) — Maximum number of tests to return (1-200). Defaults to 50.; min_failed_prs (query) — A test with no recorded recovery qualifies once it failed on at least this many distinct pull requests in the window. Minimum 1. Defaults to 3.; repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; runner (query) — Optional test runner to return: 'pytest' or 'jest'.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_job_aggregates",
    "name": "Per-job aggregates for one workflow over a window (default -30d), one row per de-sharded job name (matrix shards aggregate together), busiest first: queue p50, duration p50/p95, failure rate, retry pressure, run share (below 1.0 = conditional job), and billable cost",
    "description": "Per-job aggregates for one workflow over a window (default -30d), one row per de-sharded job name (matrix shards aggregate together), busiest first: queue p50, duration p50/p95, failure rate, retry pressure, run share (below 1.0 = conditional job), and billable cost Per-job aggregates for one workflow over a window (d…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/job_aggregates/\nPer-job aggregates for one workflow over a window (default -30d), one row per de-sharded job name (matrix shards aggregate together), busiest first: queue p50, duration p50/p95, failure rate, retry pressure, run share (below 1.0 = conditional job), and billable cost. Optionally scope to a single git branch via `branch` or one run group via `run_scope`. Jobs always need their run as context — this is the aggregate view; use workflow_jobs for one run's jobs. Empty when the job-level source isn't synced.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; workflow_name (query) — Workflow name to aggregate jobs for.\nOptional: branch (query) — Optional exact git branch (head_branch) to scope results to, e.g. 'main'. Omit or leave blank to aggregate across all branches.; date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; run_scope (query) — Which group of runs to report on: 'all' (default) is every run; 'default_branch' is runs on master or main; 'pull_request' is runs on PR branches, excluding default-branch and merge-queue runs; 'merge_queue' is the gate runs the merge queue fired before a merge landed. Fork PRs carry no PR attribution (a GitHub limitation), so they appear only under 'all'. Any other value is a 400.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_master_failures",
    "name": "Default-branch failures over a window (default -24h), grouped error-tracking style by (workflow, de-sharded failing job) with a run count and first/last seen, newest group first",
    "description": "Default-branch failures over a window (default -24h), grouped error-tracking style by (workflow, de-sharded failing job) with a run count and first/last seen, newest group first Default-branch failures over a window (default -24h), grouped error-tracking style by (workflow, de-sharded failing job) with a run count and…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/master_failures/\nDefault-branch failures over a window (default -24h), grouped error-tracking style by (workflow, de-sharded failing job) with a run count and first/last seen, newest group first. `branch` overrides the detected default branch. PR-branch failures are deliberately excluded — at monorepo volume a flat feed is a firehose; those surface per PR. Groups degrade to workflow level (failed_job '') when the job-level source isn't synced.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: branch (query) — Optional exact git branch (head_branch) to scope results to, e.g. 'main'. Omit or leave blank to aggregate across all branches.; date_from (query) — Window start: relative ('-24h', '-7d') or ISO8601. Defaults to -24h.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_pr_cost",
    "name": "Estimated CI cost for a pull request, summed over the jobs of all its workflow runs",
    "description": "Estimated CI cost for a pull request, summed over the jobs of all its workflow runs Estimated CI cost for a pull request, summed over the jobs of all its workflow runs. GET /api/projects/{project_id}/engineering_analytics/pr_cost/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/pr_cost/\nEstimated CI cost for a pull request, summed over the jobs of all its workflow runs. Billable self-hosted Linux runners only — provider-hosted (free GitHub-hosted) and non-Linux jobs are excluded. Every figure is zero/null with `jobs_available` false when the job-level source isn't synced yet. `llm_spend` carries the agent LLM token spend attributed to the PR by git branch, or null when no `$ai_generation` event matched.\nRequired: pr_number (query) — Pull request number to estimate cost for.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo (query) — 'owner/name' repository the pull request belongs to.\nOptional: source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_pr_lifecycle",
    "name": "The timeline of a single pull request: header plus ordered events (opened, CI started/finished, merged or closed)",
    "description": "The timeline of a single pull request: header plus ordered events (opened, CI started/finished, merged or closed) The timeline of a single pull request: header plus ordered events (opened, CI started/finished, merged or closed). GET /api/projects/{project_id}/engineering_analytics/pr_lifecycle/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/pr_lifecycle/\nThe timeline of a single pull request: header plus ordered events (opened, CI started/finished, merged or closed). Use this to answer 'where is this PR stuck and what happened to it'. This is a partial view: review and comment events are not yet available.\nRequired: pr_number (query) — Pull request number to inspect.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo (query) — 'owner/name' repository the pull request belongs to.\nOptional: source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_pr_runs",
    "name": "Every workflow run attributed to a pull request, across all its commits (grouped by head SHA client-side), newest first",
    "description": "Every workflow run attributed to a pull request, across all its commits (grouped by head SHA client-side), newest first Every workflow run attributed to a pull request, across all its commits (grouped by head SHA client-side), newest first. GET /api/projects/{project_id}/engineering_analytics/pr_runs/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/pr_runs/\nEvery workflow run attributed to a pull request, across all its commits (grouped by head SHA client-side), newest first. Run-level only.\nRequired: pr_number (query) — Pull request number whose runs to list.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo (query) — 'owner/name' repository the pull request belongs to.\nOptional: source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_pull_request_timelines",
    "name": "Pull requests as timelines of what each waited on from ready for review to merge or now: review, CI, red checks by what turned them green, and the merge queue",
    "description": "Pull requests as timelines of what each waited on from ready for review to merge or now: review, CI, red checks by what turned them green, and the merge queue Pull requests as timelines of what each waited on from ready for review to merge or now: review, CI, red checks by what turned them green, and the merge queue.…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/pull_request_timelines/\nPull requests as timelines of what each waited on from ready for review to merge or now: review, CI, red checks by what turned them green, and the merge queue. Scope to one author or one GitHub team (open PRs plus PRs merged in the window, date_from default -30d), or to one pull request with pr_number and repo.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: author (query) — GitHub login: scope the read to this author's pull requests. Pass exactly one scope.; date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; github_team (query) — GitHub team slug: scope the read to pull requests authored by the team's members, through the team membership table. Pass exactly one scope.; pr_number (query) — Pull request number: scope the read to this one pull request. Needs repo. Pass exactly one scope.; repo (query) — 'owner/name' repository. Required with pr_number; otherwise it picks the repository when the selected source syncs several.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_pull_requests",
    "name": "Open pull requests plus any merged or closed since date_from (default -30d), newest first, each with its head-SHA CI rollup",
    "description": "Open pull requests plus any merged or closed since date_from (default -30d), newest first, each with its head-SHA CI rollup Open pull requests plus any merged or closed since date_from (default -30d), newest first, each with its head-SHA CI rollup. GET /api/projects/{project_id}/engineering_analytics/pull_requests/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/pull_requests/\nOpen pull requests plus any merged or closed since date_from (default -30d), newest first, each with its head-SHA CI rollup. The list is capped; when more match, `truncated` is true and the ci_cards counts can exceed it. open_to_merge_seconds is coarse — it fuses draft and ready-for-review time; CI counts can lag until late completions settle.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: author (query) — Optional GitHub login to scope the list to one author's pull requests.; date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_quarantine",
    "name": "Flaky-test quarantine file",
    "description": "Flaky-test quarantine file The repository's checked-in .test_quarantine.json: flaky tests temporarily quarantined with a hard expiry, classified by urgency (overdue, in grace, expiring soon, active). GET /api/projects/{project_id}/engineering_analytics/quarantine/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/quarantine/\nThe repository's checked-in .test_quarantine.json: flaky tests temporarily quarantined with a hard expiry, classified by urgency (overdue, in grace, expiring soon, active). `available` is false when the repo has no quarantine file — that is not an error. Parsing is fail-open: malformed entries are reported in parse_errors while well-formed ones are kept.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: repo (query) — Optional 'owner/name' repository to read the quarantine file from. Defaults to the connected GitHub source's most active repo over the last 30 days.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_quarantine_request",
    "name": "Quarantine, extend, or unquarantine a flaky test",
    "description": "Quarantine, extend, or unquarantine a flaky test Opens a pull request that edits the repository's checked-in .test_quarantine.json — and, for a new quarantine, a tracking issue the PR links but does not close. POST /api/projects/{project_id}/engineering_analytics/quarantine/request/",
    "context": "POST /api/projects/{project_id}/engineering_analytics/quarantine/request/\nOpens a pull request that edits the repository's checked-in .test_quarantine.json — and, for a new quarantine, a tracking issue the PR links but does not close. The file stays the source of truth that CI enforces; this never bypasses it. A quarantine only affects CI runs that start after the PR merges.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "engineering_analytics_repo_overview",
    "name": "Repo-level headline aggregates over a window (default -30d): run count, conclusive-run success rate, re-run cycles, merged-PR count (bots included), median PR open-to-merge (bots and drafts excluded; coarse — draft and ready time fused), median time-to-green, billable minutes + estimated cost (with the merge-queue slice of billable minutes broken out), and merge-queue landing stats (queue-landed merges, first-gate-to-merge median and p90, gate attempts, failed-gate share) — each with its equal-length previous-window twin so a caller can render honest deltas",
    "description": "Repo-level headline aggregates over a window (default -30d): run count, conclusive-run success rate, re-run cycles, merged-PR count (bots included), median PR open-to-merge (bots and drafts excluded; coarse — draft and ready time fused), median time-to-green, billable minutes + estimated cost (with the merge-queue sli…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/repo_overview/\nRepo-level headline aggregates over a window (default -30d): run count, conclusive-run success rate, re-run cycles, merged-PR count (bots included), median PR open-to-merge (bots and drafts excluded; coarse — draft and ready time fused), median time-to-green, billable minutes + estimated cost (with the merge-queue slice of billable minutes broken out), and merge-queue landing stats (queue-landed merges, first-gate-to-merge median and p90, gate attempts, failed-gate share) — each with its equal-length previous-window twin so a caller can render honest deltas. Also carries the detected default branch and its completed-run history series (skippable via include_series=false). Cost figures are null until the job-level source is synced.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; include_series (query) — Set false to skip the chart series (cost_series, time_to_green_series, success_rate_series, open_to_merge_series return empty) and their query cost — for headline-only consumers like the weekly digest. Defaults to true.; repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_repo_run_activity",
    "name": "Default-branch health as compact chart points over a window (default -30d), newest first, for the repo-hub run-activity chart",
    "description": "Default-branch health as compact chart points over a window (default -30d), newest first, for the repo-hub run-activity chart Default-branch health as compact chart points over a window (default -30d), newest first, for the repo-hub run-activity chart. GET /api/projects/{project_id}/engineering_analytics/repo_run_acti…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/repo_run_activity/\nDefault-branch health as compact chart points over a window (default -30d), newest first, for the repo-hub run-activity chart. All of a commit's workflow runs are collapsed into ONE point per commit (head SHA): its earliest workflow start, wall-clock duration until the last workflow settled (null while any is still running), and an overall conclusion that is 'failure' if any workflow decisively failed, else 'success' when at least one passed, else 'neutral'. `branch` overrides the detected default branch. `truncated` is true when more commits matched than the cap, so the chart covers only the most recent commits.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: branch (query) — Optional exact git branch (head_branch) to chart, e.g. 'main'. Omit or leave blank to use the repo's detected default branch.; date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_resolve_branch",
    "name": "Resolve a git branch to the pull request(s) it belongs to — the cross-product link seam so another product (the LLM analytics UI) can turn a git branch into a PR detail link",
    "description": "Resolve a git branch to the pull request(s) it belongs to — the cross-product link seam so another product (the LLM analytics UI) can turn a git branch into a PR detail link Resolve a git branch to the pull request(s) it belongs to — the cross-product link seam so another product (the LLM analytics UI) can turn a git…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/resolve_branch/\nResolve a git branch to the pull request(s) it belongs to — the cross-product link seam so another product (the LLM analytics UI) can turn a git branch into a PR detail link. Matches the PR's head ref, open PRs first then most recently updated. Pass `timestamp` (the trace's capture time) to prefer the PR that was active at that moment when a branch name has been reused across PRs. `branch` is required. Returns a possibly-empty, possibly-multi list — an empty list is a valid 200 (the caller renders a plain chip).\nRequired: branch (query) — Git branch (the PR's head ref) to resolve. Open PRs are returned first, then most recently updated.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: repo (query) — Optional 'owner/name' repository to narrow matching to a single repo.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one.; timestamp (query) — Optional ISO8601 timestamp, e.g. the trace's capture time. When a branch name has been reused across PRs over time, the PR whose lifetime window contains this moment is ranked first so the result matches the PR that was active when the trace was captured. A preference only, not a filter; omit to rank purely by open state then recency."
  },
  {
    "id": "engineering_analytics_run_failure_logs",
    "name": "The thinned CI failure logs of one workflow run, grouped by failed job — the run-scoped twin of ci_failure_logs for surfaces that aren't PR-scoped (default-branch failures, the run page)",
    "description": "The thinned CI failure logs of one workflow run, grouped by failed job — the run-scoped twin of ci_failure_logs for surfaces that aren't PR-scoped (default-branch failures, the run page) The thinned CI failure logs of one workflow run, grouped by failed job — the run-scoped twin of ci_failure_logs for surfaces that ar…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/run_failure_logs/\nThe thinned CI failure logs of one workflow run, grouped by failed job — the run-scoped twin of ci_failure_logs for surfaces that aren't PR-scoped (default-branch failures, the run page). logs_available is false when the run didn't fail or its logs aged out of the short Logs retention.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; run_id (query) — Workflow run id whose failure logs to fetch.\nOptional: repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_sources",
    "name": "The team's selectable GitHub repositories, oldest source first — one entry per repository a source is configured to sync, so a source syncing several repositories appears once per repo",
    "description": "The team's selectable GitHub repositories, oldest source first — one entry per repository a source is configured to sync, so a source syncing several repositories appears once per repo The team's selectable GitHub repositories, oldest source first — one entry per repository a source is configured to sync, so a source…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/sources/\nThe team's selectable GitHub repositories, oldest source first — one entry per repository a source is configured to sync, so a source syncing several repositories appears once per repo. Populate a repo picker from this and pass a chosen entry's `id` back as `source_id` and its `repo` back as `repo` to the other endpoints. Includes repositories whose tables aren't fully synced yet.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "engineering_analytics_team_ci_activity",
    "name": "One owning team's CI test activity: per-test current-vs-prior signal pairs (the before/after comparison) over the window and its equal-length prior twin",
    "description": "One owning team's CI test activity: per-test current-vs-prior signal pairs (the before/after comparison) over the window and its equal-length prior twin One owning team's CI test activity: per-test current-vs-prior signal pairs (the before/after comparison) over the window and its equal-length prior twin. GET /api/pro…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/team_ci_activity/\nOne owning team's CI test activity: per-test current-vs-prior signal pairs (the before/after comparison) over the window and its equal-length prior twin. Signal = runs where an owned test failed, errored, or a retry recovered it. Counts are absolute, never rates: CI emits every failure but omits ordinary passing spans, so there is no execution denominator. 'suspected_regression' means no recovery was recorded in this data, not that the test never flakes.\nRequired: owner_team (query) — Owning team slug to scope to (as returned by team_ci_health), e.g. 'team-replay', or the literal 'unowned' for tests with no ownership stamp.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Window start: relative ('-14d', '-7d') or ISO8601. Defaults to -14d; the window may span at most 30 days. An equal-length prior window feeds the *_prior twins; near the 30-day ceiling that prior window can reach past Traces retention, deflating *_prior counts.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one.; test_limit (query) — Maximum number of per-test signal rows to return (1-100). Defaults to 25."
  },
  {
    "id": "engineering_analytics_team_ci_health",
    "name": "Per-owning-team rollup of the CI test surfaces each team owns, over the same run evidence as flaky_tests and with the same meaning of flaky: flaky_test_count is owned tests one commit was seen both failing and passing in the window, regression_test_count is owned tests that failed with no such proof and still hit the blast-radius bar, plus failed/recovery/quarantined run counts",
    "description": "Per-owning-team rollup of the CI test surfaces each team owns, over the same run evidence as flaky_tests and with the same meaning of flaky: flaky_test_count is owned tests one commit was seen both failing and passing in the window, regression_test_count is owned tests that failed with no such proof and still hit the…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/team_ci_health/\nPer-owning-team rollup of the CI test surfaces each team owns, over the same run evidence as flaky_tests and with the same meaning of flaky: flaky_test_count is owned tests one commit was seen both failing and passing in the window, regression_test_count is owned tests that failed with no such proof and still hit the blast-radius bar, plus failed/recovery/quarantined run counts. Each has an equal-length previous-window twin for honest deltas. Ownership is stamped on the spans at CI emission time from the repo's ownership map (the distributed owners.yaml files); unstamped spans aggregate under the literal team 'unowned', and a re-stamped test lands under its latest owner only. Each row also carries test_file_count (the daily owners.yaml census denominator, with a window-start twin) and merged_pr_count (merged PRs by the team's members, bots excluded); teams with census counts but no CI signal appear with zero signal counts and a null last_seen_at. Teams are organizational owners of code surfaces, never authors. Counts are absolute, never rates: CI emits every failure but omits ordinary passing spans, so there is no execution denominator. 'suspected_regression' means no recovery was recorded in this data, not that the test never flakes.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Window start: relative ('-14d', '-7d') or ISO8601. Defaults to -14d; the window may span at most 30 days. An equal-length prior window is scanned for the *_prior twins; near the 30-day ceiling that prior window can reach past Traces retention, deflating *_prior counts and overstating deltas.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; limit (query) — Maximum number of teams to return (1-200). Defaults to 100.; min_failed_prs (query) — An unrecovered test counts toward regression_test_count once it failed on at least this many distinct pull requests in the window. Minimum 1. Defaults to 3. Does not affect flaky_test_count, which needs proof, not a threshold.; owner_team (query) — Restrict the roster to one owning team slug (or 'unowned'). The cheap way to read a single team's rollup.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_team_merge_trend",
    "name": "One team's daily time-to-merge trend: the median and average open→merge seconds over the PRs the team's members merged each day (PR author login → GitHub org team membership)",
    "description": "One team's daily time-to-merge trend: the median and average open→merge seconds over the PRs the team's members merged each day (PR author login → GitHub org team membership) One team's daily time-to-merge trend: the median and average open→merge seconds over the PRs the team's members merged each day (PR author login…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/team_merge_trend/\nOne team's daily time-to-merge trend: the median and average open→merge seconds over the PRs the team's members merged each day (PR author login → GitHub org team membership). Team-level aggregates only, never per-member figures or cross-team rankings. Timing is the coarse open→merge (draft + review time combined); bots are excluded. Requires the GitHub source's team_members snapshot; has_membership_data is false without it.\nRequired: owner_team (query) — Team slug to scope to (as returned by team_ci_health), matched against the GitHub org team slug of the source's team_members snapshot. The literal 'unowned' names an ownership gap, not an org team, and has no merge trend.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: date_from (query) — Window start: relative ('-14d', '-7d') or ISO8601. Defaults to -14d; the window may span at most 30 days.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_trunk_quarantine",
    "name": "Trunk quarantine debt by owning team",
    "description": "Trunk quarantine debt by owning team The standing Trunk quarantine debt: every test Trunk currently quarantines (failures suppressed in CI), attributed to the team that owns its file in the repository, aged against a TTL, and rolled up per team with the most indebted first. GET /api/projects/{project_id}/engineering_a…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/trunk_quarantine/\nThe standing Trunk quarantine debt: every test Trunk currently quarantines (failures suppressed in CI), attributed to the team that owns its file in the repository, aged against a TTL, and rolled up per team with the most indebted first. A quarantine only masks a test; it never fixes it, so this is the work queue of tests someone still has to repair or delete. `available` is false when no TrunkIo source has the QuarantinedTests endpoint synced — that is not an error.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_workflow_health",
    "name": "Per-workflow CI health over a window (default last 24 hours, maximum 366 days): run count, success rate, p50/p95 duration, last failure time, latest-run status, and a zero-filled run history bucketed by hour/day/week to fit the window",
    "description": "Per-workflow CI health over a window (default last 24 hours, maximum 366 days): run count, success rate, p50/p95 duration, last failure time, latest-run status, and a zero-filled run history bucketed by hour/day/week to fit the window Per-workflow CI health over a window (default last 24 hours, maximum 366 days): run…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/workflow_health/\nPer-workflow CI health over a window (default last 24 hours, maximum 366 days): run count, success rate, p50/p95 duration, last failure time, latest-run status, and a zero-filled run history bucketed by hour/day/week to fit the window. Success rate covers runs that succeeded or ended in a decisive failure. Skipped, cancelled, neutral, and action-required runs are excluded. p50/p95 are over successful runs only, so cancelled (superseded) and failed runs never bias the duration trend. Optionally scope to a single git branch via `branch`, to one workflow via `workflow_name`, or to one run group via `run_scope` (default_branch, pull_request, merge_queue). Use this for 'is CI getting slower' and 'which workflow is the long pole'; compare two windows to get a trend.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: branch (query) — Optional exact git branch (head_branch) to scope results to, e.g. 'main'. Omit or leave blank to aggregate across all branches.; date_from (query) — Window start: relative ('-24h', '-7d') or ISO8601. Defaults to -24h.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; run_scope (query) — Which group of runs to report on: 'all' (default) is every run; 'default_branch' is runs on master or main; 'pull_request' is runs on PR branches, excluding default-branch and merge-queue runs; 'merge_queue' is the gate runs the merge queue fired before a merge landed. Fork PRs carry no PR attribution (a GitHub limitation), so they appear only under 'all'. Any other value is a 400.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one.; workflow_name (query) — Optional exact workflow name to scope results to, e.g. 'Backend CI'. Omit to rank every workflow. Pass it when you want one workflow's figures over the whole window rather than the top slice."
  },
  {
    "id": "engineering_analytics_workflow_jobs",
    "name": "Jobs of a single workflow run attempt, with per-job duration, runner tier, and estimated cost",
    "description": "Jobs of a single workflow run attempt, with per-job duration, runner tier, and estimated cost Jobs of a single workflow run attempt, with per-job duration, runner tier, and estimated cost. GET /api/projects/{project_id}/engineering_analytics/workflow_jobs/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/workflow_jobs/\nJobs of a single workflow run attempt, with per-job duration, runner tier, and estimated cost. Scoped to one run_attempt (the latest unless specified) so a re-run's attempts don't merge. Returns an empty list when the job-level source isn't synced yet.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; run_id (query) — Workflow run id to list jobs for.\nOptional: repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; run_attempt (query) — Which re-run attempt to scope jobs to. Omit to use the run's latest attempt; pass an explicit attempt to avoid mixing jobs across a re-run's attempts.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_workflow_run",
    "name": "A single workflow run: status, conclusion, duration, branch, attempt, and the attributed pull request",
    "description": "A single workflow run: status, conclusion, duration, branch, attempt, and the attributed pull request A single workflow run: status, conclusion, duration, branch, attempt, and the attributed pull request. GET /api/projects/{project_id}/engineering_analytics/workflow_run/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/workflow_run/\nA single workflow run: status, conclusion, duration, branch, attempt, and the attributed pull request. Run-level only — per-job and per-step detail are not tracked yet.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; run_id (query) — GitHub Actions run id to inspect.\nOptional: repo (query) — 'owner/name' repository to scope to when the selected source syncs several repositories (from the `sources` list). Defaults to the source's first repository.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_workflow_run_activity",
    "name": "Compact per-run points for a single workflow over a window (date_from default -30d), newest first, for the run-activity chart: each run's start time, duration, conclusion, branch, and attributed PR",
    "description": "Compact per-run points for a single workflow over a window (date_from default -30d), newest first, for the run-activity chart: each run's start time, duration, conclusion, branch, and attributed PR Compact per-run points for a single workflow over a window (date_from default -30d), newest first, for the run-activity c…",
    "context": "GET /api/projects/{project_id}/engineering_analytics/workflow_run_activity/\nCompact per-run points for a single workflow over a window (date_from default -30d), newest first, for the run-activity chart: each run's start time, duration, conclusion, branch, and attributed PR. Optionally scope to a single git branch via `branch` or one run group via `run_scope`, matching workflow_runs. Leaner and higher-capped than workflow_runs so the chart spans the full window even on busy workflows; `truncated` is true when the cap is hit, so the chart covers only the most recent runs.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo (query) — 'owner/name' repository the workflow belongs to.; workflow_name (query) — Workflow name to load run activity for.\nOptional: branch (query) — Optional exact git branch (head_branch) to scope results to, e.g. 'main'. Omit or leave blank to aggregate across all branches.; date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; run_scope (query) — Which group of runs to report on: 'all' (default) is every run; 'default_branch' is runs on master or main; 'pull_request' is runs on PR branches, excluding default-branch and merge-queue runs; 'merge_queue' is the gate runs the merge queue fired before a merge landed. Fork PRs carry no PR attribution (a GitHub limitation), so they appear only under 'all'. Any other value is a 400.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_workflow_runner_costs",
    "name": "A workflow's estimated CI cost broken down by runner tier over a window (date_from default -30d), highest spend first",
    "description": "A workflow's estimated CI cost broken down by runner tier over a window (date_from default -30d), highest spend first A workflow's estimated CI cost broken down by runner tier over a window (date_from default -30d), highest spend first. GET /api/projects/{project_id}/engineering_analytics/workflow_runner_costs/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/workflow_runner_costs/\nA workflow's estimated CI cost broken down by runner tier over a window (date_from default -30d), highest spend first. Optionally scope to a single git branch via `branch` or one run group via `run_scope`. Returns an empty list when the job-level source isn't synced.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo (query) — 'owner/name' repository the workflow belongs to.; workflow_name (query) — Workflow name to break down cost for.\nOptional: branch (query) — Optional exact git branch (head_branch) to scope results to, e.g. 'main'. Omit or leave blank to aggregate across all branches.; date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; run_scope (query) — Which group of runs to report on: 'all' (default) is every run; 'default_branch' is runs on master or main; 'pull_request' is runs on PR branches, excluding default-branch and merge-queue runs; 'merge_queue' is the gate runs the merge queue fired before a merge landed. Fork PRs carry no PR attribution (a GitHub limitation), so they appear only under 'all'. Any other value is a 400.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  },
  {
    "id": "engineering_analytics_workflow_runs",
    "name": "Runs of a single workflow within a repo over a window (date_from default -30d), newest first",
    "description": "Runs of a single workflow within a repo over a window (date_from default -30d), newest first Runs of a single workflow within a repo over a window (date_from default -30d), newest first. GET /api/projects/{project_id}/engineering_analytics/workflow_runs/",
    "context": "GET /api/projects/{project_id}/engineering_analytics/workflow_runs/\nRuns of a single workflow within a repo over a window (date_from default -30d), newest first. Optionally scope to a single git branch via `branch` or to one run group via `run_scope`. Each row is run-level — per-job and per-step detail are not tracked yet. Use this as the GitHub 'workflow' page between the workflow list and a single run.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo (query) — 'owner/name' repository the workflow belongs to.; workflow_name (query) — Workflow name to list runs for.\nOptional: branch (query) — Optional exact git branch (head_branch) to scope results to, e.g. 'main'. Omit or leave blank to aggregate across all branches.; date_from (query) — Window start: relative ('-30d', '-8w') or ISO8601. Defaults to -30d.; date_to (query) — Window end: relative or ISO8601. Defaults to now.; run_scope (query) — Which group of runs to report on: 'all' (default) is every run; 'default_branch' is runs on master or main; 'pull_request' is runs on PR branches, excluding default-branch and merge-queue runs; 'merge_queue' is the gate runs the merge queue fired before a merge landed. Fork PRs carry no PR attribution (a GitHub limitation), so they appear only under 'all'. Any other value is a 400.; source_id (query) — Connected GitHub data warehouse source to read from. Defaults to the oldest connected GitHub source when the team has more than one."
  }
]
