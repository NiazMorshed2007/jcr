{
  "id": "endpoints",
  "name": "Endpoints",
  "description": "Endpoints capabilities. Includes Ask AI to rewrite the endpoint's query into a semantically equivalent form that can be materialized, Create a new endpoint., Delete an endpoint and clean up materialized query., endpoints_logs_retrieve, Execute endpoint with optional materialization, Get materialization status for an endpoint, Get OpenAPI 3.0 specification for this endpoint, Get the most recent execution time per endpoint (endpoint-level), and 7 more."
}
