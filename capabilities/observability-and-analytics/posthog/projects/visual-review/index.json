{
  "id": "visual-review",
  "name": "Visual review",
  "description": "Visual review capabilities. Includes Add a batch of snapshots to a pending run (shard-based flow)., Complete a run: detect removals, verify uploads, trigger diff processing., Create a new repo., Create a new run from a CI manifest., Deduped baseline timeline for a snapshot identity, Expire all active quarantine entries for an identifier., Finalize a fully-reviewed run: commit the approved baseline and green the gate, Get a repo by ID., and 18 more."
}
