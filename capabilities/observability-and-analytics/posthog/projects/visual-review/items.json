[
  {
    "id": "visual_review_repos_baselines_retrieve",
    "name": "Snapshots overview for a repo: every identifier with a current baseline (latest non-superseded master/main run per run_type), plus tolerate counts, active quarantine state, and a 30-day stability sparkline",
    "description": "Snapshots overview for a repo: every identifier with a current baseline (latest non-superseded master/main run per run_type), plus tolerate counts, active quarantine state, and a 30-day stability sparkline Snapshots overview for a repo: every identifier with a current baseline (latest non-superseded master/main run pe…",
    "context": "GET /api/projects/{project_id}/visual_review/repos/{id}/baselines/\nSnapshots overview for a repo: every identifier with a current baseline (latest non-superseded master/main run per run_type), plus tolerate counts, active quarantine state, and a 30-day stability sparkline. Capped at 5000 entries — sets `truncated` and returns the most recently active when exceeded. Filtering / faceting / search are all done client-side; this endpoint takes no filter query params.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "visual_review_repos_create",
    "name": "Create a new repo.",
    "description": "Create a new repo. POST /api/projects/{project_id}/visual_review/repos/",
    "context": "POST /api/projects/{project_id}/visual_review/repos/\nCreate a new repo.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "visual_review_repos_flakiness_retrieve",
    "name": "Snapshots in a repo whose rendering cannot be trusted: those that failed the gate or were absorbed by a toleration on a recent default-branch run, and those under an active quarantine",
    "description": "Snapshots in a repo whose rendering cannot be trusted: those that failed the gate or were absorbed by a toleration on a recent default-branch run, and those under an active quarantine Snapshots in a repo whose rendering cannot be trusted: those that failed the gate or were absorbed by a toleration on a recent default-…",
    "context": "GET /api/projects/{project_id}/visual_review/repos/{id}/flakiness/\nSnapshots in a repo whose rendering cannot be trusted: those that failed the gate or were absorbed by a toleration on a recent default-branch run, and those under an active quarantine. Everything else is omitted, so this is far smaller than the baselines universe; `totals.tracked` gives the full denominator. Each entry carries the share of the last 7 days of default-branch runs that failed the gate (`hard_rate`) and the share a toleration absorbed (`soft_rate`), plus `headroom`, the fraction of the diff threshold its worst absorbed run leaves free. Capped at 2000 entries, which sets `truncated`. Filtering, faceting and search are done client-side; this endpoint takes no filter query params.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "visual_review_repos_list",
    "name": "List all projects for the team.",
    "description": "List all projects for the team. GET /api/projects/{project_id}/visual_review/repos/",
    "context": "GET /api/projects/{project_id}/visual_review/repos/\nList all projects for the team.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results."
  },
  {
    "id": "visual_review_repos_partial_update",
    "name": "Update a repo's settings.",
    "description": "Update a repo's settings. PATCH /api/projects/{project_id}/visual_review/repos/{id}/",
    "context": "PATCH /api/projects/{project_id}/visual_review/repos/{id}/\nUpdate a repo's settings.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "visual_review_repos_quarantine_create",
    "name": "Quarantine a snapshot identifier for a specific run type.",
    "description": "Quarantine a snapshot identifier for a specific run type. POST /api/projects/{project_id}/visual_review/repos/{id}/quarantine/{run_type}/",
    "context": "POST /api/projects/{project_id}/visual_review/repos/{id}/quarantine/{run_type}/\nQuarantine a snapshot identifier for a specific run type.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; run_type (path); body (body)"
  },
  {
    "id": "visual_review_repos_quarantine_expire_create",
    "name": "Expire all active quarantine entries for an identifier.",
    "description": "Expire all active quarantine entries for an identifier. POST /api/projects/{project_id}/visual_review/repos/{id}/quarantine/{run_type}/expire/",
    "context": "POST /api/projects/{project_id}/visual_review/repos/{id}/quarantine/{run_type}/expire/\nExpire all active quarantine entries for an identifier.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; run_type (path); body (body)"
  },
  {
    "id": "visual_review_repos_quarantine_list",
    "name": "List quarantined identifiers",
    "description": "List quarantined identifiers List quarantined identifiers. GET /api/projects/{project_id}/visual_review/repos/{id}/quarantine/",
    "context": "GET /api/projects/{project_id}/visual_review/repos/{id}/quarantine/\nList quarantined identifiers. Without filter: active only. With identifier: full history.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: identifier (query) — Filter by identifier (returns full history); limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results.; run_type (query) — Filter by run type"
  },
  {
    "id": "visual_review_repos_retrieve",
    "name": "Get a repo by ID.",
    "description": "Get a repo by ID. GET /api/projects/{project_id}/visual_review/repos/{id}/",
    "context": "GET /api/projects/{project_id}/visual_review/repos/{id}/\nGet a repo by ID.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "visual_review_repos_runs_counts_retrieve",
    "name": "Review state counts for runs in this repo.",
    "description": "Review state counts for runs in this repo. GET /api/projects/{project_id}/visual_review/repos/{repo_id}/runs/counts/",
    "context": "GET /api/projects/{project_id}/visual_review/repos/{repo_id}/runs/counts/\nReview state counts for runs in this repo.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo_id (path)"
  },
  {
    "id": "visual_review_repos_runs_list",
    "name": "List runs in this repo, optionally filtered by review state and free-text search.",
    "description": "List runs in this repo, optionally filtered by review state and free-text search. GET /api/projects/{project_id}/visual_review/repos/{repo_id}/runs/",
    "context": "GET /api/projects/{project_id}/visual_review/repos/{repo_id}/runs/\nList runs in this repo, optionally filtered by review state and free-text search.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo_id (path)\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results.; review_state (query) — Filter by review state; search (query) — Free-text search over branch, commit SHA, run type, and PR number"
  },
  {
    "id": "visual_review_repos_snapshots_list",
    "name": "Deduped baseline timeline for a snapshot identity",
    "description": "Deduped baseline timeline for a snapshot identity Deduped baseline timeline for a snapshot identity. GET /api/projects/{project_id}/visual_review/repos/{repo_id}/snapshots/{run_type}/{identifier}/",
    "context": "GET /api/projects/{project_id}/visual_review/repos/{repo_id}/snapshots/{run_type}/{identifier}/\nDeduped baseline timeline for a snapshot identity. Newest first.\nRequired: identifier (path) — Snapshot identifier; clients must percent-encode before sending; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; repo_id (path); run_type (path) — Run type (storybook, playwright)\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results."
  },
  {
    "id": "visual_review_repos_thumbnails_retrieve",
    "name": "Serve a snapshot thumbnail by identifier",
    "description": "Serve a snapshot thumbnail by identifier Serve a snapshot thumbnail by identifier. GET /api/projects/{project_id}/visual_review/repos/{id}/thumbnails/{identifier}/",
    "context": "GET /api/projects/{project_id}/visual_review/repos/{id}/thumbnails/{identifier}/\nServe a snapshot thumbnail by identifier. Returns WebP with ETag caching.\nRequired: id (path); identifier (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: run_type (query) — Narrow the lookup to one run type. The same identifier under two run types is two different images, so omit this only when the caller shows one run type."
  },
  {
    "id": "visual_review_runs_add_snapshots_create",
    "name": "Add a batch of snapshots to a pending run (shard-based flow).",
    "description": "Add a batch of snapshots to a pending run (shard-based flow). POST /api/projects/{project_id}/visual_review/runs/{id}/add-snapshots/",
    "context": "POST /api/projects/{project_id}/visual_review/runs/{id}/add-snapshots/\nAdd a batch of snapshots to a pending run (shard-based flow).\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "visual_review_runs_approve_create",
    "name": "Mark snapshots reviewed (DB only)",
    "description": "Mark snapshots reviewed (DB only) Mark snapshots reviewed (DB only). POST /api/projects/{project_id}/visual_review/runs/{id}/approve/",
    "context": "POST /api/projects/{project_id}/visual_review/runs/{id}/approve/\nMark snapshots reviewed (DB only). Records the per-snapshot \"Accept change\" decision. Does not commit the baseline or change the GitHub gate — call finalize to ship the run. Works on a quarantined snapshot too: a quarantined NEW snapshot approved here is committed by finalize, which gives a quarantined story a baseline entry without lifting the quarantine.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "visual_review_runs_complete_create",
    "name": "Complete a run: detect removals, verify uploads, trigger diff processing.",
    "description": "Complete a run: detect removals, verify uploads, trigger diff processing. POST /api/projects/{project_id}/visual_review/runs/{id}/complete/",
    "context": "POST /api/projects/{project_id}/visual_review/runs/{id}/complete/\nComplete a run: detect removals, verify uploads, trigger diff processing.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "visual_review_runs_counts_retrieve",
    "name": "Review state counts for the runs list.",
    "description": "Review state counts for the runs list. GET /api/projects/{project_id}/visual_review/runs/counts/",
    "context": "GET /api/projects/{project_id}/visual_review/runs/counts/\nReview state counts for the runs list.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "visual_review_runs_create",
    "name": "Create a new run from a CI manifest.",
    "description": "Create a new run from a CI manifest. POST /api/projects/{project_id}/visual_review/runs/",
    "context": "POST /api/projects/{project_id}/visual_review/runs/\nCreate a new run from a CI manifest.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "visual_review_runs_finalize_create",
    "name": "Finalize a fully-reviewed run: commit the approved baseline and green the gate",
    "description": "Finalize a fully-reviewed run: commit the approved baseline and green the gate Finalize a fully-reviewed run: commit the approved baseline and green the gate. POST /api/projects/{project_id}/visual_review/runs/{id}/finalize/",
    "context": "POST /api/projects/{project_id}/visual_review/runs/{id}/finalize/\nFinalize a fully-reviewed run: commit the approved baseline and green the gate. Commits exactly the snapshots approved in the DB (tolerated ones keep their baseline) and only succeeds once every changed/new snapshot is resolved. With approve_all=true, any still-pending changed/new snapshot is approved first; quarantined snapshots are skipped, but a quarantined NEW snapshot approved by identifier is still committed. With commit_to_github=false the server returns the signed baseline YAML instead of committing it.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "visual_review_runs_list",
    "name": "List runs for the team, optionally filtered by review state, PR number, commit SHA, branch, or free-text search.",
    "description": "List runs for the team, optionally filtered by review state, PR number, commit SHA, branch, or free-text search. GET /api/projects/{project_id}/visual_review/runs/",
    "context": "GET /api/projects/{project_id}/visual_review/runs/\nList runs for the team, optionally filtered by review state, PR number, commit SHA, branch, or free-text search.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: branch (query) — Filter by branch name; commit_sha (query) — Filter by full commit SHA; limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results.; pr_number (query) — Filter by GitHub PR number; review_state (query) — Filter by review state; search (query) — Free-text search over branch, commit SHA, run type, and PR number"
  },
  {
    "id": "visual_review_runs_recompute_create",
    "name": "Re-evaluate quarantine and counts, update commit status, and optionally rerun the CI job.",
    "description": "Re-evaluate quarantine and counts, update commit status, and optionally rerun the CI job. POST /api/projects/{project_id}/visual_review/runs/{id}/recompute/",
    "context": "POST /api/projects/{project_id}/visual_review/runs/{id}/recompute/\nRe-evaluate quarantine and counts, update commit status, and optionally rerun the CI job.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "visual_review_runs_retrieve",
    "name": "Get run status and summary.",
    "description": "Get run status and summary. GET /api/projects/{project_id}/visual_review/runs/{id}/",
    "context": "GET /api/projects/{project_id}/visual_review/runs/{id}/\nGet run status and summary.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "visual_review_runs_snapshot_history_list",
    "name": "Recent change history for a snapshot identifier across runs.",
    "description": "Recent change history for a snapshot identifier across runs. GET /api/projects/{project_id}/visual_review/runs/{id}/snapshot-history/",
    "context": "GET /api/projects/{project_id}/visual_review/runs/{id}/snapshot-history/\nRecent change history for a snapshot identifier across runs.\nRequired: id (path) — UUID of the visual review run to look the snapshot up from. This is a run id, not the `id` of a snapshot inside that run. The run supplies the repo and run type to search, so the `identifier` query parameter is required alongside it.; identifier (query) — Identifier of the snapshot to look up, for example a Storybook story id plus theme. Read it from the `identifier` field of a snapshot in the run. It is a name rather than a UUID, and it is required in addition to the run id in the path.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results."
  },
  {
    "id": "visual_review_runs_snapshots_list",
    "name": "Get a run's snapshots with diff results, excluding quarantined ones by default.",
    "description": "Get a run's snapshots with diff results, excluding quarantined ones by default. GET /api/projects/{project_id}/visual_review/runs/{id}/snapshots/",
    "context": "GET /api/projects/{project_id}/visual_review/runs/{id}/snapshots/\nGet a run's snapshots with diff results, excluding quarantined ones by default.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: include_quarantined (query) — Whether to include snapshots whose identifier is currently quarantined. Defaults to false: quarantined snapshots are excluded from results and reported in quarantined_count instead, since they are noise when reviewing real changes.; limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results."
  },
  {
    "id": "visual_review_runs_tolerate_create",
    "name": "Mark a changed snapshot as a known tolerated alternate.",
    "description": "Mark a changed snapshot as a known tolerated alternate. POST /api/projects/{project_id}/visual_review/runs/{id}/tolerate/",
    "context": "POST /api/projects/{project_id}/visual_review/runs/{id}/tolerate/\nMark a changed snapshot as a known tolerated alternate.\nRequired: id (path); project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "visual_review_runs_tolerated_hashes_list",
    "name": "List known tolerated hashes for a snapshot identifier.",
    "description": "List known tolerated hashes for a snapshot identifier. GET /api/projects/{project_id}/visual_review/runs/{id}/tolerated-hashes/",
    "context": "GET /api/projects/{project_id}/visual_review/runs/{id}/tolerated-hashes/\nList known tolerated hashes for a snapshot identifier.\nRequired: id (path) — UUID of the visual review run to look the snapshot up from. This is a run id, not the `id` of a snapshot inside that run. The run supplies the repo and run type to search, so the `identifier` query parameter is required alongside it.; identifier (query) — Identifier of the snapshot to look up, for example a Storybook story id plus theme. Read it from the `identifier` field of a snapshot in the run. It is a name rather than a UUID, and it is required in addition to the run id in the path.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results."
  }
]
