{
  "id": "datasets",
  "name": "Datasets",
  "description": "Datasets capabilities. Includes Archive a dataset, Check the status of a dataset export created by the current user., Create an asynchronous JSONL export pinned to an immutable dataset revision., Create an empty dataset, Download a completed dataset JSONL export., List active datasets by default, or archived datasets when requested., List immutable dataset revisions, newest first., Restore an archived dataset without changing its item states., and 2 more."
}
