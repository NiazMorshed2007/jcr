[
  {
    "id": "event_filter_create",
    "name": "Create or update the event filter config.",
    "description": "Create or update the event filter config. POST /api/projects/{project_id}/event_filter/",
    "context": "POST /api/projects/{project_id}/event_filter/\nCreate or update the event filter config.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "event_filter_metrics_retrieve",
    "name": "Single event filter per team",
    "description": "Single event filter per team Single event filter per team. GET /api/projects/{project_id}/event_filter/metrics/",
    "context": "GET /api/projects/{project_id}/event_filter/metrics/\nSingle event filter per team. GET /event_filter/ — returns the config (or null if not yet created) POST /event_filter/ — creates or updates the config (upsert) GET /event_filter/metrics/ — time-series metrics GET /event_filter/metrics/totals/ — aggregate totals\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "event_filter_metrics_totals_retrieve",
    "name": "Single event filter per team",
    "description": "Single event filter per team Single event filter per team. GET /api/projects/{project_id}/event_filter/metrics/totals/",
    "context": "GET /api/projects/{project_id}/event_filter/metrics/totals/\nSingle event filter per team. GET /event_filter/ — returns the config (or null if not yet created) POST /event_filter/ — creates or updates the config (upsert) GET /event_filter/metrics/ — time-series metrics GET /event_filter/metrics/totals/ — aggregate totals\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "event_filter_retrieve",
    "name": "Returns the event filter config for the team, or null if not yet created.",
    "description": "Returns the event filter config for the team, or null if not yet created. GET /api/projects/{project_id}/event_filter/",
    "context": "GET /api/projects/{project_id}/event_filter/\nReturns the event filter config for the team, or null if not yet created.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  }
]
