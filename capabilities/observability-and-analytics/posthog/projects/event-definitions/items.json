[
  {
    "id": "event_definitions_bulk_update_tags_create",
    "name": "Add, remove, or replace tags across multiple event definitions in one request",
    "description": "Add, remove, or replace tags across multiple event definitions in one request Add, remove, or replace tags across multiple event definitions in one request. POST /api/projects/{project_id}/event_definitions/bulk_update_tags/",
    "context": "POST /api/projects/{project_id}/event_definitions/bulk_update_tags/\nAdd, remove, or replace tags across multiple event definitions in one request. Overrides ``TaggedItemViewSetMixin.bulk_update_tags``, which assumes integer PKs and runs object-level access-control filtering. Event definitions use UUID PKs and are not an object-level access-controlled resource — project membership (enforced by the viewset) is the only boundary, matching the single-object update path — so this scopes by project and skips the per-object editor check. Tags live on the base ``EventDefinition`` row, so it operates there regardless of the enterprise extension.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "event_definitions_bulk_update_verified_create",
    "name": "Mark multiple event definitions as verified or unverified in one request",
    "description": "Mark multiple event definitions as verified or unverified in one request Mark multiple event definitions as verified or unverified in one request. POST /api/projects/{project_id}/event_definitions/bulk_update_verified/",
    "context": "POST /api/projects/{project_id}/event_definitions/bulk_update_verified/\nMark multiple event definitions as verified or unverified in one request. In the same vein as ``bulk_update_tags``, but ``verified`` lives on the enterprise ``EnterpriseEventDefinition`` extension rather than the base row, so this action: - requires an enterprise license; - scopes by project (``team__project_id``) and relies on project membership — the same boundary the single-object update path uses — rather than object-level RBAC; - lazily promotes ingestion-created base rows to ``EnterpriseEventDefinition`` (mirroring ``_get_event_definition``) before setting ``verified``; - mirrors the single-object semantics: verifying stamps ``verified_by``/``verified_at`` and unhides the event (an event cannot be both hidden and verified); unverifying clears them; - logs a \"changed\" activity per event so the History tab matches the single-object path. Events already in the target state are skipped (not re-written, not logged).\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "event_definitions_by_name_retrieve",
    "name": "Get event definition by exact name",
    "description": "Get event definition by exact name GET /api/projects/{project_id}/event_definitions/by_name/",
    "context": "GET /api/projects/{project_id}/event_definitions/by_name/\nGet event definition by exact name\nRequired: name (query) — The exact event name to look up; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "event_definitions_create",
    "name": "event_definitions_create",
    "description": "event_definitions_create POST /api/projects/{project_id}/event_definitions/",
    "context": "POST /api/projects/{project_id}/event_definitions/\nevent_definitions_create\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "event_definitions_destroy",
    "name": "event_definitions_destroy",
    "description": "event_definitions_destroy DELETE /api/projects/{project_id}/event_definitions/{id}/",
    "context": "DELETE /api/projects/{project_id}/event_definitions/{id}/\nevent_definitions_destroy\nRequired: id (path) — A UUID string identifying this event definition.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "event_definitions_golang_retrieve",
    "name": "event_definitions_golang_retrieve",
    "description": "event_definitions_golang_retrieve GET /api/projects/{project_id}/event_definitions/golang/",
    "context": "GET /api/projects/{project_id}/event_definitions/golang/\nevent_definitions_golang_retrieve\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "event_definitions_list",
    "name": "event_definitions_list",
    "description": "event_definitions_list GET /api/projects/{project_id}/event_definitions/",
    "context": "GET /api/projects/{project_id}/event_definitions/\nevent_definitions_list\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: exclude_hidden (query) — When true, omit events that have been explicitly hidden by a team admin (Enterprise only).; exclude_stale (query) — When true, omit events whose last ingested occurrence is older than 30 days. Events that have never been seen (`last_seen_at` is null) are kept so newly-defined events remain discoverable. Default false. If a search returns zero results with this filter on, retry with `exclude_stale=false` and tell the user the matches are stale.; limit (query) — Number of results to return per page.; names (query) — Return exact matches for these event names. Pass names as repeated or comma-separated values.; offset (query) — The initial index from which to return the results."
  },
  {
    "id": "event_definitions_metrics_retrieve",
    "name": "event_definitions_metrics_retrieve",
    "description": "event_definitions_metrics_retrieve GET /api/projects/{project_id}/event_definitions/{id}/metrics/",
    "context": "GET /api/projects/{project_id}/event_definitions/{id}/metrics/\nevent_definitions_metrics_retrieve\nRequired: id (path) — A UUID string identifying this event definition.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "event_definitions_partial_update",
    "name": "event_definitions_partial_update",
    "description": "event_definitions_partial_update PATCH /api/projects/{project_id}/event_definitions/{id}/",
    "context": "PATCH /api/projects/{project_id}/event_definitions/{id}/\nevent_definitions_partial_update\nRequired: id (path) — A UUID string identifying this event definition.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "event_definitions_primary_properties_retrieve",
    "name": "Resolve team-configured primary properties for event definitions",
    "description": "Resolve team-configured primary properties for event definitions Resolve team-configured primary properties for event definitions. GET /api/projects/{project_id}/event_definitions/primary_properties/",
    "context": "GET /api/projects/{project_id}/event_definitions/primary_properties/\nResolve team-configured primary properties for event definitions. The response only contains entries where a non-null primary_property is set on the EventDefinition. Callers should fall back to the core taxonomy defaults client-side for names not present in the response.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: names (query) — Optional: restrict the response to these event names. Repeat the parameter for multiple names (e.g. `?names=a&names=b`). When omitted, returns every team-configured primary property."
  },
  {
    "id": "event_definitions_python_retrieve",
    "name": "event_definitions_python_retrieve",
    "description": "event_definitions_python_retrieve GET /api/projects/{project_id}/event_definitions/python/",
    "context": "GET /api/projects/{project_id}/event_definitions/python/\nevent_definitions_python_retrieve\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "event_definitions_retrieve",
    "name": "event_definitions_retrieve",
    "description": "event_definitions_retrieve GET /api/projects/{project_id}/event_definitions/{id}/",
    "context": "GET /api/projects/{project_id}/event_definitions/{id}/\nevent_definitions_retrieve\nRequired: id (path) — A UUID string identifying this event definition.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "event_definitions_typescript_retrieve",
    "name": "event_definitions_typescript_retrieve",
    "description": "event_definitions_typescript_retrieve GET /api/projects/{project_id}/event_definitions/typescript/",
    "context": "GET /api/projects/{project_id}/event_definitions/typescript/\nevent_definitions_typescript_retrieve\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "event_definitions_update",
    "name": "event_definitions_update",
    "description": "event_definitions_update PUT /api/projects/{project_id}/event_definitions/{id}/",
    "context": "PUT /api/projects/{project_id}/event_definitions/{id}/\nevent_definitions_update\nRequired: id (path) — A UUID string identifying this event definition.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  }
]
