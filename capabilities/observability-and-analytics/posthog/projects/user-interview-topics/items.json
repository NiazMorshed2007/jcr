[
  {
    "id": "user_interview_topics_add_interviewee_create",
    "name": "Add a single interviewee to this topic",
    "description": "Add a single interviewee to this topic Add a single interviewee to this topic. POST /api/projects/{project_id}/user_interview_topics/{id}/add_interviewee/",
    "context": "POST /api/projects/{project_id}/user_interview_topics/{id}/add_interviewee/\nAdd a single interviewee to this topic. Email-shaped identifiers (including the `Display Name ` form) are appended to `interviewee_emails`; everything else is appended to `interviewee_distinct_ids`. Idempotent — adding an identifier that's already present leaves the topic unchanged. Returns the updated topic.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "user_interview_topics_create",
    "name": "Planned user interview topics: who we want to target and what we want to ask about.",
    "description": "Planned user interview topics: who we want to target and what we want to ask about. POST /api/projects/{project_id}/user_interview_topics/",
    "context": "POST /api/projects/{project_id}/user_interview_topics/\nPlanned user interview topics: who we want to target and what we want to ask about.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "user_interview_topics_destroy",
    "name": "Planned user interview topics: who we want to target and what we want to ask about.",
    "description": "Planned user interview topics: who we want to target and what we want to ask about. DELETE /api/projects/{project_id}/user_interview_topics/{id}/",
    "context": "DELETE /api/projects/{project_id}/user_interview_topics/{id}/\nPlanned user interview topics: who we want to target and what we want to ask about.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "user_interview_topics_generate_links_create",
    "name": "Generate one public interview link per targeted interviewee",
    "description": "Generate one public interview link per targeted interviewee Generate one public interview link per targeted interviewee. POST /api/projects/{project_id}/user_interview_topics/{id}/generate_links/",
    "context": "POST /api/projects/{project_id}/user_interview_topics/{id}/generate_links/\nGenerate one public interview link per targeted interviewee. Materializes an IntervieweeContext row for every identifier on the topic (without overwriting existing per-person context), and an enabled SharingConfiguration with a unique access token. The URL resolves to the public interview viewer with no PostHog auth required.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "user_interview_topics_interviewees_bulk_create",
    "name": "Create up to 500 interviewee context rows for a topic in a single request",
    "description": "Create up to 500 interviewee context rows for a topic in a single request Create up to 500 interviewee context rows for a topic in a single request. POST /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/bulk/",
    "context": "POST /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/bulk/\nCreate up to 500 interviewee context rows for a topic in a single request. Rows whose (topic, interviewee_identifier) already exists are skipped — the response surfaces an `inserted_count`, a `skipped_count`, and the `skipped_identifiers` so the caller can reconcile. Items must have unique `interviewee_identifier` values within the batch.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; topic_id (path); body (body)"
  },
  {
    "id": "user_interview_topics_interviewees_create",
    "name": "Per-interviewee extra context for a user interview topic",
    "description": "Per-interviewee extra context for a user interview topic Per-interviewee extra context for a user interview topic. POST /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/",
    "context": "POST /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/\nPer-interviewee extra context for a user interview topic. At most one row per (topic, interviewee_identifier).\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; topic_id (path); body (body)"
  },
  {
    "id": "user_interview_topics_interviewees_destroy",
    "name": "Per-interviewee extra context for a user interview topic",
    "description": "Per-interviewee extra context for a user interview topic Per-interviewee extra context for a user interview topic. DELETE /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/{id}/",
    "context": "DELETE /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/{id}/\nPer-interviewee extra context for a user interview topic. At most one row per (topic, interviewee_identifier).\nRequired: id (path) — A UUID string identifying this interviewee context.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; topic_id (path)"
  },
  {
    "id": "user_interview_topics_interviewees_list",
    "name": "Per-interviewee extra context for a user interview topic",
    "description": "Per-interviewee extra context for a user interview topic Per-interviewee extra context for a user interview topic. GET /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/",
    "context": "GET /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/\nPer-interviewee extra context for a user interview topic. At most one row per (topic, interviewee_identifier).\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; topic_id (path)\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results."
  },
  {
    "id": "user_interview_topics_interviewees_partial_update",
    "name": "Per-interviewee extra context for a user interview topic",
    "description": "Per-interviewee extra context for a user interview topic Per-interviewee extra context for a user interview topic. PATCH /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/{id}/",
    "context": "PATCH /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/{id}/\nPer-interviewee extra context for a user interview topic. At most one row per (topic, interviewee_identifier).\nRequired: id (path) — A UUID string identifying this interviewee context.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; topic_id (path)\nOptional: body (body)"
  },
  {
    "id": "user_interview_topics_interviewees_retrieve",
    "name": "Per-interviewee extra context for a user interview topic",
    "description": "Per-interviewee extra context for a user interview topic Per-interviewee extra context for a user interview topic. GET /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/{id}/",
    "context": "GET /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/{id}/\nPer-interviewee extra context for a user interview topic. At most one row per (topic, interviewee_identifier).\nRequired: id (path) — A UUID string identifying this interviewee context.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; topic_id (path)"
  },
  {
    "id": "user_interview_topics_interviewees_update",
    "name": "Per-interviewee extra context for a user interview topic",
    "description": "Per-interviewee extra context for a user interview topic Per-interviewee extra context for a user interview topic. PUT /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/{id}/",
    "context": "PUT /api/projects/{project_id}/user_interview_topics/{topic_id}/interviewees/{id}/\nPer-interviewee extra context for a user interview topic. At most one row per (topic, interviewee_identifier).\nRequired: id (path) — A UUID string identifying this interviewee context.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; topic_id (path); body (body)"
  },
  {
    "id": "user_interview_topics_links_csv_create",
    "name": "Same materialization as generate_links, returned as a downloadable CSV",
    "description": "Same materialization as generate_links, returned as a downloadable CSV Same materialization as generate_links, returned as a downloadable CSV. POST /api/projects/{project_id}/user_interview_topics/{id}/links_csv/",
    "context": "POST /api/projects/{project_id}/user_interview_topics/{id}/links_csv/\nSame materialization as generate_links, returned as a downloadable CSV. Intended for users who want to mail-merge the per-person interview links into their own email tooling.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "user_interview_topics_list",
    "name": "Planned user interview topics: who we want to target and what we want to ask about.",
    "description": "Planned user interview topics: who we want to target and what we want to ask about. GET /api/projects/{project_id}/user_interview_topics/",
    "context": "GET /api/projects/{project_id}/user_interview_topics/\nPlanned user interview topics: who we want to target and what we want to ask about.\nRequired: project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: limit (query) — Number of results to return per page.; offset (query) — The initial index from which to return the results.; search (query) — A search term."
  },
  {
    "id": "user_interview_topics_partial_update",
    "name": "Planned user interview topics: who we want to target and what we want to ask about.",
    "description": "Planned user interview topics: who we want to target and what we want to ask about. PATCH /api/projects/{project_id}/user_interview_topics/{id}/",
    "context": "PATCH /api/projects/{project_id}/user_interview_topics/{id}/\nPlanned user interview topics: who we want to target and what we want to ask about.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "user_interview_topics_preview_invite_create",
    "name": "Render the invite email exactly as a specific targeted interviewee would receive it — personalized subject and body — without sending anything and without creating or reading any share links",
    "description": "Render the invite email exactly as a specific targeted interviewee would receive it — personalized subject and body — without sending anything and without creating or reading any share links Render the invite email exactly as a specific targeted interviewee would receive it — personalized subject and body — without se…",
    "context": "POST /api/projects/{project_id}/user_interview_topics/{id}/preview_invite/\nRender the invite email exactly as a specific targeted interviewee would receive it — personalized subject and body — without sending anything and without creating or reading any share links. Pass `interviewee_identifier` to preview for a particular person, or omit it to preview for the first targeted interviewee. The body always shows an illustrative placeholder link (`is_preview_link: true`), never a live interview URL.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "user_interview_topics_remove_interviewee_create",
    "name": "Remove an interviewee from this topic",
    "description": "Remove an interviewee from this topic Remove an interviewee from this topic. POST /api/projects/{project_id}/user_interview_topics/{id}/remove_interviewee/",
    "context": "POST /api/projects/{project_id}/user_interview_topics/{id}/remove_interviewee/\nRemove an interviewee from this topic. Drops the identifier from both `interviewee_emails` and `interviewee_distinct_ids`, and disables any active SharingConfiguration linked to an IntervieweeContext for that identifier on this topic so the removed person can no longer open their interview link. Idempotent — removing an identifier that isn't present is a no-op. Returns the updated topic.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  },
  {
    "id": "user_interview_topics_retrieve",
    "name": "Planned user interview topics: who we want to target and what we want to ask about.",
    "description": "Planned user interview topics: who we want to target and what we want to ask about. GET /api/projects/{project_id}/user_interview_topics/{id}/",
    "context": "GET /api/projects/{project_id}/user_interview_topics/{id}/\nPlanned user interview topics: who we want to target and what we want to ask about.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "user_interview_topics_send_invites_create",
    "name": "Generate (if needed) and email a personalized public interview link to every targeted interviewee on this topic whose identifier is an email address",
    "description": "Generate (if needed) and email a personalized public interview link to every targeted interviewee on this topic whose identifier is an email address Generate (if needed) and email a personalized public interview link to every targeted interviewee on this topic whose identifier is an email address. POST /api/projects/{…",
    "context": "POST /api/projects/{project_id}/user_interview_topics/{id}/send_invites/\nGenerate (if needed) and email a personalized public interview link to every targeted interviewee on this topic whose identifier is an email address. Distinct-ID-only interviewees are skipped and surfaced in the response. Each invite is keyed on the underlying SharingConfiguration so re-runs after token rotation produce a fresh send.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.\nOptional: body (body)"
  },
  {
    "id": "user_interview_topics_shared_link_create",
    "name": "Get-or-create a single non-personalised (shared) interview link for this topic",
    "description": "Get-or-create a single non-personalised (shared) interview link for this topic Get-or-create a single non-personalised (shared) interview link for this topic. POST /api/projects/{project_id}/user_interview_topics/{id}/shared_link/",
    "context": "POST /api/projects/{project_id}/user_interview_topics/{id}/shared_link/\nGet-or-create a single non-personalised (shared) interview link for this topic. Unlike generate_links, the returned URL is not tied to a specific interviewee — every visitor becomes a new anonymous respondent who self-identifies with a name. Idempotent: repeated calls return the same active link. `distinct_id` and `session_id` query params appended to the URL are captured as best-effort person/session linkage.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "user_interview_topics_shared_link_destroy",
    "name": "Revoke this topic's shared (non-personalised) interview link so an already-distributed URL can no longer start interviews",
    "description": "Revoke this topic's shared (non-personalised) interview link so an already-distributed URL can no longer start interviews Revoke this topic's shared (non-personalised) interview link so an already-distributed URL can no longer start interviews. DELETE /api/projects/{project_id}/user_interview_topics/{id}/shared_link/",
    "context": "DELETE /api/projects/{project_id}/user_interview_topics/{id}/shared_link/\nRevoke this topic's shared (non-personalised) interview link so an already-distributed URL can no longer start interviews. Idempotent — a no-op when no active shared link exists. A subsequent shared_link POST mints a fresh link (rotation).\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "user_interview_topics_test_link_retrieve",
    "name": "Return the calling user's personal dogfood interview link for this topic, plus the latest test interview they have recorded against it",
    "description": "Return the calling user's personal dogfood interview link for this topic, plus the latest test interview they have recorded against it Return the calling user's personal dogfood interview link for this topic, plus the latest test interview they have recorded against it. GET /api/projects/{project_id}/user_interview_to…",
    "context": "GET /api/projects/{project_id}/user_interview_topics/{id}/test_link/\nReturn the calling user's personal dogfood interview link for this topic, plus the latest test interview they have recorded against it. Lazily get-or-creates a per-caller IntervieweeContext + enabled SharingConfiguration the first time it's called, then returns the same stable URL on subsequent calls. The caller's identifier is intentionally not added to the topic's targeting arrays — each user dogfoods under their own row, so test calls never mint a public share token on someone else's behalf.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/."
  },
  {
    "id": "user_interview_topics_update",
    "name": "Planned user interview topics: who we want to target and what we want to ask about.",
    "description": "Planned user interview topics: who we want to target and what we want to ask about. PUT /api/projects/{project_id}/user_interview_topics/{id}/",
    "context": "PUT /api/projects/{project_id}/user_interview_topics/{id}/\nPlanned user interview topics: who we want to target and what we want to ask about.\nRequired: id (path) — A UUID string identifying this user interview topic.; project_id (path) — Project ID of the project you're trying to access. To find the ID of the project, make a call to /api/projects/.; body (body)"
  }
]
