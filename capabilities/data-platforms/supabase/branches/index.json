{
  "id": "branches",
  "name": "Branches",
  "description": "Branches capabilities. Includes Delete a database branch, Diffs a database branch, Get database branch config, Merges a database branch, Pushes a database branch, Resets a database branch, Restore a scheduled branch deletion, Update database branch config."
}
