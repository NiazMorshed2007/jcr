{
  "id": "analytics",
  "name": "Analytics",
  "description": "Analytics capabilities. Includes Gets a project's function combined statistics, Gets all project's logs in a single log stream, Gets project's logs, Gets project's usage api counts, Gets project's usage api requests count, Scrape a project's metrics."
}
