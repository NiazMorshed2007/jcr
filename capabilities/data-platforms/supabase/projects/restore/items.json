[
  {
    "id": "v1-cancel-a-project-restoration",
    "name": "Cancels the given project restoration",
    "description": "Cancels the given project restoration POST /v1/projects/{ref}/restore/cancel",
    "context": "POST /v1/projects/{ref}/restore/cancel\nCancels the given project restoration\nRequired: ref (path) — Project ref"
  },
  {
    "id": "v1-list-available-restore-versions",
    "name": "Lists available restore versions for the given project",
    "description": "Lists available restore versions for the given project GET /v1/projects/{ref}/restore",
    "context": "GET /v1/projects/{ref}/restore\nLists available restore versions for the given project\nRequired: ref (path) — Project ref"
  },
  {
    "id": "v1-restore-a-project",
    "name": "Restores the given project",
    "description": "Restores the given project POST /v1/projects/{ref}/restore",
    "context": "POST /v1/projects/{ref}/restore\nRestores the given project\nRequired: ref (path) — Project ref"
  }
]
