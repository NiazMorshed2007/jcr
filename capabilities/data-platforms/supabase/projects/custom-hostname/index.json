{
  "id": "custom-hostname",
  "name": "Custom hostname",
  "description": "Custom hostname capabilities. Includes Activates a custom hostname for a project., Attempts to verify the DNS configuration for project's custom hostname configuration, Deletes a project's custom hostname configuration, Gets project's custom hostname config, Updates project's custom hostname configuration."
}
