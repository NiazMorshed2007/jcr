[
  {
    "id": "v1-bulk-create-secrets",
    "name": "Bulk create secrets",
    "description": "Bulk create secrets Creates multiple secrets and adds them to the specified project. POST /v1/projects/{ref}/secrets",
    "context": "POST /v1/projects/{ref}/secrets\nCreates multiple secrets and adds them to the specified project.\nRequired: ref (path) — Project ref; body (body)"
  },
  {
    "id": "v1-bulk-delete-secrets",
    "name": "Bulk delete secrets",
    "description": "Bulk delete secrets Deletes all secrets with the given names from the specified project DELETE /v1/projects/{ref}/secrets",
    "context": "DELETE /v1/projects/{ref}/secrets\nDeletes all secrets with the given names from the specified project\nRequired: ref (path) — Project ref; body (body)"
  },
  {
    "id": "v1-list-all-secrets",
    "name": "List all secrets",
    "description": "List all secrets Returns all secrets you've previously added to the specified project. GET /v1/projects/{ref}/secrets",
    "context": "GET /v1/projects/{ref}/secrets\nReturns all secrets you've previously added to the specified project.\nRequired: ref (path) — Project ref"
  }
]
