[
  {
    "id": "v1-create-project-claim-token",
    "name": "Creates project claim token",
    "description": "Creates project claim token POST /v1/projects/{ref}/claim-token",
    "context": "POST /v1/projects/{ref}/claim-token\nCreates project claim token\nRequired: ref (path) — Project ref"
  },
  {
    "id": "v1-delete-project-claim-token",
    "name": "Revokes project claim token",
    "description": "Revokes project claim token DELETE /v1/projects/{ref}/claim-token",
    "context": "DELETE /v1/projects/{ref}/claim-token\nRevokes project claim token\nRequired: ref (path) — Project ref"
  },
  {
    "id": "v1-get-project-claim-token",
    "name": "Gets project claim token",
    "description": "Gets project claim token GET /v1/projects/{ref}/claim-token",
    "context": "GET /v1/projects/{ref}/claim-token\nGets project claim token\nRequired: ref (path) — Project ref"
  }
]
