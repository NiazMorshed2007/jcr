{
  "id": "templates",
  "name": "Templates",
  "description": "Templates capabilities. Includes Create a template, Duplicate a template, Publish a template, Remove an existing template, Retrieve a list of templates, Retrieve a single template, Update an existing template."
}
