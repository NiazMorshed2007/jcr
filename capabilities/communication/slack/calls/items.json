[
  {
    "id": "calls.add",
    "name": "Registers a new Call.",
    "description": "Registers a new Call. POST /api/calls.add",
    "context": "POST /api/calls.add\nRegisters a new Call.\nRequired: body (body)"
  },
  {
    "id": "calls.end",
    "name": "Ends a Call.",
    "description": "Ends a Call. POST /api/calls.end",
    "context": "POST /api/calls.end\nEnds a Call.\nRequired: body (body)"
  },
  {
    "id": "calls.info",
    "name": "Returns information about a Call.",
    "description": "Returns information about a Call. POST /api/calls.info",
    "context": "POST /api/calls.info\nReturns information about a Call.\nRequired: body (body)"
  },
  {
    "id": "calls.participants.add",
    "name": "Registers new participants added to a Call.",
    "description": "Registers new participants added to a Call. POST /api/calls.participants.add",
    "context": "POST /api/calls.participants.add\nRegisters new participants added to a Call.\nRequired: body (body)"
  },
  {
    "id": "calls.participants.remove",
    "name": "Call the calls.participants.remove method",
    "description": "Call the calls.participants.remove method POST /api/calls.participants.remove",
    "context": "POST /api/calls.participants.remove\nCall the calls.participants.remove method\nRequired: body (body)"
  },
  {
    "id": "calls.update",
    "name": "Updates information about a Call.",
    "description": "Updates information about a Call. POST /api/calls.update",
    "context": "POST /api/calls.update\nUpdates information about a Call.\nRequired: body (body)"
  }
]
