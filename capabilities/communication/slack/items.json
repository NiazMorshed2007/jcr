[
  {
    "id": "agents.sessions.rename",
    "name": "Rename an agent session.",
    "description": "Rename an agent session. POST /api/agents.sessions.rename",
    "context": "POST /api/agents.sessions.rename\nRename an agent session.\nRequired: body (body)"
  },
  {
    "id": "agents.sessions.setStatus",
    "name": "Set an agent session's lifecycle status, creating the session if needed.",
    "description": "Set an agent session's lifecycle status, creating the session if needed. POST /api/agents.sessions.setStatus",
    "context": "POST /api/agents.sessions.setStatus\nSet an agent session's lifecycle status, creating the session if needed.\nRequired: body (body)"
  },
  {
    "id": "api.test",
    "name": "Checks API calling code.",
    "description": "Checks API calling code. POST /api/api.test",
    "context": "POST /api/api.test\nChecks API calling code.\nOptional: body (body)"
  },
  {
    "id": "blocks.validate",
    "name": "Validates blocks, messages, and views Block Kit JSON payloads.",
    "description": "Validates blocks, messages, and views Block Kit JSON payloads. POST /api/blocks.validate",
    "context": "POST /api/blocks.validate\nValidates blocks, messages, and views Block Kit JSON payloads.\nOptional: body (body)"
  },
  {
    "id": "bots.info",
    "name": "Gets information about a bot user.",
    "description": "Gets information about a bot user. POST /api/bots.info",
    "context": "POST /api/bots.info\nGets information about a bot user.\nOptional: body (body)"
  },
  {
    "id": "dialog.open",
    "name": "Open a dialog with a user.",
    "description": "Open a dialog with a user. POST /api/dialog.open",
    "context": "POST /api/dialog.open\nOpen a dialog with a user.\nRequired: body (body)"
  },
  {
    "id": "emoji.list",
    "name": "Lists custom emoji for a team.",
    "description": "Lists custom emoji for a team. POST /api/emoji.list",
    "context": "POST /api/emoji.list\nLists custom emoji for a team.\nOptional: body (body)"
  },
  {
    "id": "entity.presentDetails",
    "name": "Provide information about the entity to be displayed in the flexpane.",
    "description": "Provide information about the entity to be displayed in the flexpane. POST /api/entity.presentDetails",
    "context": "POST /api/entity.presentDetails\nProvide information about the entity to be displayed in the flexpane.\nRequired: body (body)"
  },
  {
    "id": "functions.completeError",
    "name": "Signal the failure to execute a Custom Function.",
    "description": "Signal the failure to execute a Custom Function. POST /api/functions.completeError",
    "context": "POST /api/functions.completeError\nSignal the failure to execute a Custom Function.\nRequired: body (body)"
  },
  {
    "id": "functions.completeSuccess",
    "name": "Signal the successful completion of a Custom Function.",
    "description": "Signal the successful completion of a Custom Function. POST /api/functions.completeSuccess",
    "context": "POST /api/functions.completeSuccess\nSignal the successful completion of a Custom Function.\nRequired: body (body)"
  },
  {
    "id": "migration.exchange",
    "name": "For Enterprise Grid workspaces, map local user IDs to global user IDs.",
    "description": "For Enterprise Grid workspaces, map local user IDs to global user IDs. POST /api/migration.exchange",
    "context": "POST /api/migration.exchange\nFor Enterprise Grid workspaces, map local user IDs to global user IDs.\nRequired: body (body)"
  },
  {
    "id": "openid.connect.token",
    "name": "Exchanges a temporary OAuth verifier code for an access token for {",
    "description": "Exchanges a temporary OAuth verifier code for an access token for { POST /api/openid.connect.token",
    "context": "POST /api/openid.connect.token\nExchanges a temporary OAuth verifier code for an access token for {\nRequired: body (body)"
  },
  {
    "id": "openid.connect.userInfo",
    "name": "Get the identity of a user who has authorized {",
    "description": "Get the identity of a user who has authorized { POST /api/openid.connect.userInfo",
    "context": "POST /api/openid.connect.userInfo\nGet the identity of a user who has authorized {\nOptional: body (body)"
  },
  {
    "id": "rtm.connect",
    "name": "Starts a Real Time Messaging session.",
    "description": "Starts a Real Time Messaging session. POST /api/rtm.connect",
    "context": "POST /api/rtm.connect\nStarts a Real Time Messaging session.\nOptional: body (body)"
  },
  {
    "id": "tooling.tokens.rotate",
    "name": "Exchanges a refresh token for a new app configuration token.",
    "description": "Exchanges a refresh token for a new app configuration token. POST /api/tooling.tokens.rotate",
    "context": "POST /api/tooling.tokens.rotate\nExchanges a refresh token for a new app configuration token.\nRequired: body (body)"
  }
]
