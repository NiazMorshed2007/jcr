[
  {
    "id": "workflows.featured.add",
    "name": "Add featured workflows to a channel.",
    "description": "Add featured workflows to a channel. POST /api/workflows.featured.add",
    "context": "POST /api/workflows.featured.add\nAdd featured workflows to a channel.\nRequired: body (body)"
  },
  {
    "id": "workflows.featured.list",
    "name": "List the featured workflows for specified channels.",
    "description": "List the featured workflows for specified channels. POST /api/workflows.featured.list",
    "context": "POST /api/workflows.featured.list\nList the featured workflows for specified channels.\nRequired: body (body)"
  },
  {
    "id": "workflows.featured.remove",
    "name": "Remove featured workflows from a channel.",
    "description": "Remove featured workflows from a channel. POST /api/workflows.featured.remove",
    "context": "POST /api/workflows.featured.remove\nRemove featured workflows from a channel.\nRequired: body (body)"
  },
  {
    "id": "workflows.featured.set",
    "name": "Set featured workflows for a channel.",
    "description": "Set featured workflows for a channel. POST /api/workflows.featured.set",
    "context": "POST /api/workflows.featured.set\nSet featured workflows for a channel.\nRequired: body (body)"
  }
]
