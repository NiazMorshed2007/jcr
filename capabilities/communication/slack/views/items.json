[
  {
    "id": "views.open",
    "name": "Open a view for a user.",
    "description": "Open a view for a user. POST /api/views.open",
    "context": "POST /api/views.open\nOpen a view for a user.\nRequired: body (body)"
  },
  {
    "id": "views.publish",
    "name": "Publish a static view for a user.",
    "description": "Publish a static view for a user. POST /api/views.publish",
    "context": "POST /api/views.publish\nPublish a static view for a user.\nRequired: body (body)"
  },
  {
    "id": "views.push",
    "name": "Push a view onto the stack of a root view.",
    "description": "Push a view onto the stack of a root view. POST /api/views.push",
    "context": "POST /api/views.push\nPush a view onto the stack of a root view.\nRequired: body (body)"
  },
  {
    "id": "views.update",
    "name": "Update an existing view.",
    "description": "Update an existing view. POST /api/views.update",
    "context": "POST /api/views.update\nUpdate an existing view.\nRequired: body (body)"
  }
]
