{
  "id": "docker-registry",
  "name": "Docker registry",
  "description": "Docker registry capabilities. Includes Create registry, Delete registry, Get registry, Get temporary registry access for pushing snapshots, List registries, Update registry."
}
