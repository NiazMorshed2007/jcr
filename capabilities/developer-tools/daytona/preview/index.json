{
  "id": "preview",
  "name": "Preview",
  "description": "Preview capabilities. Includes Check if sandbox auth token is valid, Check if sandbox is public, Check if the preview warning page is enabled for the sandbox, Check if user has access to the sandbox, Get sandbox ID from signed preview URL token, Get the signing key for a sandbox."
}
