[
  {
    "id": "createSecret",
    "name": "Create secret",
    "description": "Create secret POST /secret",
    "context": "POST /secret\nCreate secret\nRequired: body (body)\nOptional: X-Daytona-Organization-ID (header) — Use with JWT to specify the organization ID"
  },
  {
    "id": "deleteSecret",
    "name": "Delete secret",
    "description": "Delete secret DELETE /secret/{secretId}",
    "context": "DELETE /secret/{secretId}\nDelete secret\nRequired: secretId (path) — ID of the secret\nOptional: X-Daytona-Organization-ID (header) — Use with JWT to specify the organization ID"
  },
  {
    "id": "getSecret",
    "name": "Get secret",
    "description": "Get secret GET /secret/{secretId}",
    "context": "GET /secret/{secretId}\nGet secret\nRequired: secretId (path) — ID of the secret\nOptional: X-Daytona-Organization-ID (header) — Use with JWT to specify the organization ID"
  },
  {
    "id": "listSecrets",
    "name": "List secrets",
    "description": "List secrets This endpoint is deprecated and fails for organizations with more than 1500 secrets. GET /secret",
    "context": "GET /secret\nThis endpoint is deprecated and fails for organizations with more than 1500 secrets. Use `listSecretsPaginated` instead.\nOptional: X-Daytona-Organization-ID (header) — Use with JWT to specify the organization ID"
  },
  {
    "id": "listSecretsPaginated",
    "name": "List secrets with pagination",
    "description": "List secrets with pagination GET /secret/paginated",
    "context": "GET /secret/paginated\nList secrets with pagination\nOptional: X-Daytona-Organization-ID (header) — Use with JWT to specify the organization ID; cursor (query) — Pagination cursor from a previous response; limit (query) — Number of results per page; name (query) — Filter by partial name match; sort (query) — Field to sort by; order (query) — Direction to sort by"
  },
  {
    "id": "updateSecret",
    "name": "Update secret",
    "description": "Update secret PATCH /secret/{secretId}",
    "context": "PATCH /secret/{secretId}\nUpdate secret\nRequired: secretId (path) — ID of the secret; body (body)\nOptional: X-Daytona-Organization-ID (header) — Use with JWT to specify the organization ID"
  }
]
