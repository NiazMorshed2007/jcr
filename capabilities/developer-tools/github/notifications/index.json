{
  "id": "notifications",
  "name": "Notifications",
  "description": "Notifications capabilities. Includes Delete a thread subscription, Get a thread, Get a thread subscription for the authenticated user, List notifications for the authenticated user, Mark a thread as done, Mark a thread as read, Mark notifications as read, Set a thread subscription."
}
