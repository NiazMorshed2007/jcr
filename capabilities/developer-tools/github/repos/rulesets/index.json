{
  "id": "rulesets",
  "name": "Rulesets",
  "description": "Rulesets capabilities. Includes Create a repository ruleset, Delete a repository ruleset, Get a repository rule suite, Get a repository ruleset, Get all repository rulesets, Get repository ruleset history, Get repository ruleset version, List repository rule suites, and 1 more."
}
