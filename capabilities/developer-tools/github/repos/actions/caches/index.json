{
  "id": "caches",
  "name": "Caches",
  "description": "Caches capabilities. Includes Delete a GitHub Actions cache for a repository (using a cache ID), Delete GitHub Actions caches for a repository (using a cache key), List GitHub Actions caches for a repository."
}
