{
  "id": "packages",
  "name": "Packages",
  "description": "Packages capabilities. Includes Delete a package for the authenticated user, Delete a package version for the authenticated user, Get a package for the authenticated user, Get a package version for the authenticated user, List package versions for a package owned by the authenticated user, List packages for the authenticated user's namespace, Restore a package for the authenticated user, Restore a package version for the authenticated user."
}
