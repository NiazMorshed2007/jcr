[
  {
    "id": "codespaces%2Fadd-repository-for-secret-for-authenticated-user",
    "name": "Add a selected repository to a user secret",
    "description": "Add a selected repository to a user secret Adds a repository to the selected repositories for a user's development environment secret. PUT /user/codespaces/secrets/{secret_name}/repositories/{repository_id}",
    "context": "PUT /user/codespaces/secrets/{secret_name}/repositories/{repository_id}\nAdds a repository to the selected repositories for a user's development environment secret. The authenticated user must have Codespaces access to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.\nRequired: secret_name (path) — The name of the secret.; repository_id (path)"
  },
  {
    "id": "codespaces%2Fcodespace-machines-for-authenticated-user",
    "name": "List machine types for a codespace",
    "description": "List machine types for a codespace List the machine types a codespace can transition to use. GET /user/codespaces/{codespace_name}/machines",
    "context": "GET /user/codespaces/{codespace_name}/machines\nList the machine types a codespace can transition to use. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: codespace_name (path) — The name of the codespace."
  },
  {
    "id": "codespaces%2Fcreate-for-authenticated-user",
    "name": "Create a codespace for the authenticated user",
    "description": "Create a codespace for the authenticated user Creates a new codespace, owned by the authenticated user. POST /user/codespaces",
    "context": "POST /user/codespaces\nCreates a new codespace, owned by the authenticated user. This endpoint requires either a `repository_id` OR a `pull_request` but not both. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: body (body)"
  },
  {
    "id": "codespaces%2Fcreate-or-update-secret-for-authenticated-user",
    "name": "Create or update a secret for the authenticated user",
    "description": "Create or update a secret for the authenticated user Creates or updates a development environment secret for a user's codespace with an encrypted value. PUT /user/codespaces/secrets/{secret_name}",
    "context": "PUT /user/codespaces/secrets/{secret_name}\nCreates or updates a development environment secret for a user's codespace with an encrypted value. Encrypt your secret using [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages). For more information, see \"[Encrypting secrets for the REST API](https://docs.github.com/rest/guides/encrypting-secrets-for-the-rest-api).\" The authenticated user must have Codespaces access to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.\nRequired: secret_name (path) — The name of the secret.; body (body)"
  },
  {
    "id": "codespaces%2Fdelete-for-authenticated-user",
    "name": "Delete a codespace for the authenticated user",
    "description": "Delete a codespace for the authenticated user Deletes a user's codespace. DELETE /user/codespaces/{codespace_name}",
    "context": "DELETE /user/codespaces/{codespace_name}\nDeletes a user's codespace. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: codespace_name (path) — The name of the codespace."
  },
  {
    "id": "codespaces%2Fdelete-secret-for-authenticated-user",
    "name": "Delete a secret for the authenticated user",
    "description": "Delete a secret for the authenticated user Deletes a development environment secret from a user's codespaces using the secret name. DELETE /user/codespaces/secrets/{secret_name}",
    "context": "DELETE /user/codespaces/secrets/{secret_name}\nDeletes a development environment secret from a user's codespaces using the secret name. Deleting the secret will remove access from all codespaces that were allowed to access the secret. The authenticated user must have Codespaces access to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.\nRequired: secret_name (path) — The name of the secret."
  },
  {
    "id": "codespaces%2Fexport-for-authenticated-user",
    "name": "Export a codespace for the authenticated user",
    "description": "Export a codespace for the authenticated user Triggers an export of the specified codespace and returns a URL and ID where the status of the export can be monitored. POST /user/codespaces/{codespace_name}/exports",
    "context": "POST /user/codespaces/{codespace_name}/exports\nTriggers an export of the specified codespace and returns a URL and ID where the status of the export can be monitored. If changes cannot be pushed to the codespace's repository, they will be pushed to a new or previously-existing fork instead. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: codespace_name (path) — The name of the codespace."
  },
  {
    "id": "codespaces%2Fget-export-details-for-authenticated-user",
    "name": "Get details about a codespace export",
    "description": "Get details about a codespace export Gets information about an export of a codespace. GET /user/codespaces/{codespace_name}/exports/{export_id}",
    "context": "GET /user/codespaces/{codespace_name}/exports/{export_id}\nGets information about an export of a codespace. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: codespace_name (path) — The name of the codespace.; export_id (path) — The ID of the export operation, or `latest`. Currently only `latest` is currently supported."
  },
  {
    "id": "codespaces%2Fget-for-authenticated-user",
    "name": "Get a codespace for the authenticated user",
    "description": "Get a codespace for the authenticated user Gets information about a user's codespace. GET /user/codespaces/{codespace_name}",
    "context": "GET /user/codespaces/{codespace_name}\nGets information about a user's codespace. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: codespace_name (path) — The name of the codespace."
  },
  {
    "id": "codespaces%2Fget-public-key-for-authenticated-user",
    "name": "Get public key for the authenticated user",
    "description": "Get public key for the authenticated user Gets your public key, which you need to encrypt secrets. GET /user/codespaces/secrets/public-key",
    "context": "GET /user/codespaces/secrets/public-key\nGets your public key, which you need to encrypt secrets. You need to encrypt a secret before you can create or update secrets. The authenticated user must have Codespaces access to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint."
  },
  {
    "id": "codespaces%2Fget-secret-for-authenticated-user",
    "name": "Get a secret for the authenticated user",
    "description": "Get a secret for the authenticated user Gets a development environment secret available to a user's codespaces without revealing its encrypted value. GET /user/codespaces/secrets/{secret_name}",
    "context": "GET /user/codespaces/secrets/{secret_name}\nGets a development environment secret available to a user's codespaces without revealing its encrypted value. The authenticated user must have Codespaces access to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.\nRequired: secret_name (path) — The name of the secret."
  },
  {
    "id": "codespaces%2Flist-for-authenticated-user",
    "name": "List codespaces for the authenticated user",
    "description": "List codespaces for the authenticated user Lists the authenticated user's codespaces. GET /user/codespaces",
    "context": "GET /user/codespaces\nLists the authenticated user's codespaces. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nOptional: per_page (query) — The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"; page (query) — The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"; repository_id (query) — ID of the Repository to filter on"
  },
  {
    "id": "codespaces%2Flist-repositories-for-secret-for-authenticated-user",
    "name": "List selected repositories for a user secret",
    "description": "List selected repositories for a user secret List the repositories that have been granted the ability to use a user's development environment secret. GET /user/codespaces/secrets/{secret_name}/repositories",
    "context": "GET /user/codespaces/secrets/{secret_name}/repositories\nList the repositories that have been granted the ability to use a user's development environment secret. The authenticated user must have Codespaces access to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.\nRequired: secret_name (path) — The name of the secret."
  },
  {
    "id": "codespaces%2Flist-secrets-for-authenticated-user",
    "name": "List secrets for the authenticated user",
    "description": "List secrets for the authenticated user Lists all development environment secrets available for a user's codespaces without revealing their encrypted values. GET /user/codespaces/secrets",
    "context": "GET /user/codespaces/secrets\nLists all development environment secrets available for a user's codespaces without revealing their encrypted values. The authenticated user must have Codespaces access to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.\nOptional: per_page (query) — The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"; page (query) — The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""
  },
  {
    "id": "codespaces%2Fpublish-for-authenticated-user",
    "name": "Create a repository from an unpublished codespace",
    "description": "Create a repository from an unpublished codespace Publishes an unpublished codespace, creating a new repository and assigning it to the codespace. POST /user/codespaces/{codespace_name}/publish",
    "context": "POST /user/codespaces/{codespace_name}/publish\nPublishes an unpublished codespace, creating a new repository and assigning it to the codespace. The codespace's token is granted write permissions to the repository, allowing the user to push their changes. This will fail for a codespace that is already published, meaning it has an associated repository. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: codespace_name (path) — The name of the codespace.; body (body)"
  },
  {
    "id": "codespaces%2Fremove-repository-for-secret-for-authenticated-user",
    "name": "Remove a selected repository from a user secret",
    "description": "Remove a selected repository from a user secret Removes a repository from the selected repositories for a user's development environment secret. DELETE /user/codespaces/secrets/{secret_name}/repositories/{repository_id}",
    "context": "DELETE /user/codespaces/secrets/{secret_name}/repositories/{repository_id}\nRemoves a repository from the selected repositories for a user's development environment secret. The authenticated user must have Codespaces access to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.\nRequired: secret_name (path) — The name of the secret.; repository_id (path)"
  },
  {
    "id": "codespaces%2Fset-repositories-for-secret-for-authenticated-user",
    "name": "Set selected repositories for a user secret",
    "description": "Set selected repositories for a user secret Select the repositories that will use a user's development environment secret. PUT /user/codespaces/secrets/{secret_name}/repositories",
    "context": "PUT /user/codespaces/secrets/{secret_name}/repositories\nSelect the repositories that will use a user's development environment secret. The authenticated user must have Codespaces access to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `codespace` or `codespace:secrets` scope to use this endpoint.\nRequired: secret_name (path) — The name of the secret.; body (body)"
  },
  {
    "id": "codespaces%2Fstart-for-authenticated-user",
    "name": "Start a codespace for the authenticated user",
    "description": "Start a codespace for the authenticated user Starts a user's codespace. POST /user/codespaces/{codespace_name}/start",
    "context": "POST /user/codespaces/{codespace_name}/start\nStarts a user's codespace. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: codespace_name (path) — The name of the codespace."
  },
  {
    "id": "codespaces%2Fstop-for-authenticated-user",
    "name": "Stop a codespace for the authenticated user",
    "description": "Stop a codespace for the authenticated user Stops a user's codespace. POST /user/codespaces/{codespace_name}/stop",
    "context": "POST /user/codespaces/{codespace_name}/stop\nStops a user's codespace. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: codespace_name (path) — The name of the codespace."
  },
  {
    "id": "codespaces%2Fupdate-for-authenticated-user",
    "name": "Update a codespace for the authenticated user",
    "description": "Update a codespace for the authenticated user Updates a codespace owned by the authenticated user. PATCH /user/codespaces/{codespace_name}",
    "context": "PATCH /user/codespaces/{codespace_name}\nUpdates a codespace owned by the authenticated user. Currently only the codespace's machine type and recent folders can be modified using this endpoint. If you specify a new machine type it will be applied the next time your codespace is started. OAuth app tokens and personal access tokens (classic) need the `codespace` scope to use this endpoint.\nRequired: codespace_name (path) — The name of the codespace.\nOptional: body (body)"
  }
]
