{
  "id": "blocks",
  "name": "Blocks",
  "description": "Blocks capabilities. Includes Block a user, Check if a user is blocked by the authenticated user, List users blocked by the authenticated user, Unblock a user."
}
