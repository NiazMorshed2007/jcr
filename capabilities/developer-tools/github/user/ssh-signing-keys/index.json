{
  "id": "ssh-signing-keys",
  "name": "Ssh signing keys",
  "description": "Ssh signing keys capabilities. Includes Create a SSH signing key for the authenticated user, Delete an SSH signing key for the authenticated user, Get an SSH signing key for the authenticated user, List SSH signing keys for the authenticated user."
}
