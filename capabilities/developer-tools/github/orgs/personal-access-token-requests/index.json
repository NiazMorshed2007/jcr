{
  "id": "personal-access-token-requests",
  "name": "Personal access token requests",
  "description": "Personal access token requests capabilities. Includes List repositories requested to be accessed by a fine-grained personal access token, List requests to access organization resources with fine-grained personal access tokens, Review a request to access organization resources with a fine-grained personal access token, Review requests to access organization resources with fine-grained personal access tokens."
}
