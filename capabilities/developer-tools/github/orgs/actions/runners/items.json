[
  {
    "id": "actions%2Fadd-custom-labels-to-self-hosted-runner-for-org",
    "name": "Add custom labels to a self-hosted runner for an organization",
    "description": "Add custom labels to a self-hosted runner for an organization Adds custom labels to a self-hosted runner configured in an organization. POST /orgs/{org}/actions/runners/{runner_id}/labels",
    "context": "POST /orgs/{org}/actions/runners/{runner_id}/labels\nAdds custom labels to a self-hosted runner configured in an organization. Authenticated users must have admin access to the organization to use this endpoint. OAuth tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.\nRequired: org (path) — The organization name. The name is not case sensitive.; runner_id (path) — Unique identifier of the self-hosted runner.; body (body)"
  },
  {
    "id": "actions%2Fcreate-registration-token-for-org",
    "name": "Create a registration token for an organization",
    "description": "Create a registration token for an organization Returns a token that you can pass to the `config` script. POST /orgs/{org}/actions/runners/registration-token",
    "context": "POST /orgs/{org}/actions/runners/registration-token\nReturns a token that you can pass to the `config` script. The token expires after one hour. For example, you can replace `TOKEN` in the following example with the registration token provided by this endpoint to configure your self-hosted runner: ``` ./config.sh --url https://github.com/octo-org --token TOKEN ``` Authenticated users must have admin access to the organization to use this endpoint. OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.\nRequired: org (path) — The organization name. The name is not case sensitive."
  },
  {
    "id": "actions%2Fcreate-remove-token-for-org",
    "name": "Create a remove token for an organization",
    "description": "Create a remove token for an organization Returns a token that you can pass to the `config` script to remove a self-hosted runner from an organization. POST /orgs/{org}/actions/runners/remove-token",
    "context": "POST /orgs/{org}/actions/runners/remove-token\nReturns a token that you can pass to the `config` script to remove a self-hosted runner from an organization. The token expires after one hour. For example, you can replace `TOKEN` in the following example with the registration token provided by this endpoint to remove your self-hosted runner from an organization: ``` ./config.sh remove --token TOKEN ``` Authenticated users must have admin access to the organization to use this endpoint. OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.\nRequired: org (path) — The organization name. The name is not case sensitive."
  },
  {
    "id": "actions%2Fdelete-self-hosted-runner-from-org",
    "name": "Delete a self-hosted runner from an organization",
    "description": "Delete a self-hosted runner from an organization Forces the removal of a self-hosted runner from an organization. DELETE /orgs/{org}/actions/runners/{runner_id}",
    "context": "DELETE /orgs/{org}/actions/runners/{runner_id}\nForces the removal of a self-hosted runner from an organization. You can use this endpoint to completely remove the runner when the machine you were using no longer exists. Authenticated users must have admin access to the organization to use this endpoint. OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.\nRequired: org (path) — The organization name. The name is not case sensitive.; runner_id (path) — Unique identifier of the self-hosted runner."
  },
  {
    "id": "actions%2Fgenerate-runner-jitconfig-for-org",
    "name": "Create configuration for a just-in-time runner for an organization",
    "description": "Create configuration for a just-in-time runner for an organization Generates a configuration that can be passed to the runner application at startup. POST /orgs/{org}/actions/runners/generate-jitconfig",
    "context": "POST /orgs/{org}/actions/runners/generate-jitconfig\nGenerates a configuration that can be passed to the runner application at startup. The authenticated user must have admin access to the organization. OAuth tokens and personal access tokens (classic) need the`admin:org` scope to use this endpoint. If the repository is private, OAuth tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.\nRequired: org (path) — The organization name. The name is not case sensitive.; body (body)"
  },
  {
    "id": "actions%2Fget-runner-version-deprecation-for-org",
    "name": "Get runner version end-of-life schedule for an organization",
    "description": "Get runner version end-of-life schedule for an organization Gets the end-of-life schedule for a specific runner version in an organization. GET /orgs/{org}/actions/runners/deprecations/{version}",
    "context": "GET /orgs/{org}/actions/runners/deprecations/{version}\nGets the end-of-life schedule for a specific runner version in an organization. Returns the runner version and the dates when registration and runtime support will end. Authenticated users must have admin access to the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint.\nRequired: org (path) — The organization name. The name is not case sensitive.; version (path) — The runner version to look up."
  },
  {
    "id": "actions%2Fget-self-hosted-runner-for-org",
    "name": "Get a self-hosted runner for an organization",
    "description": "Get a self-hosted runner for an organization Gets a specific self-hosted runner configured in an organization. GET /orgs/{org}/actions/runners/{runner_id}",
    "context": "GET /orgs/{org}/actions/runners/{runner_id}\nGets a specific self-hosted runner configured in an organization. Authenticated users must have admin access to the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.\nRequired: org (path) — The organization name. The name is not case sensitive.; runner_id (path) — Unique identifier of the self-hosted runner."
  },
  {
    "id": "actions%2Flist-labels-for-self-hosted-runner-for-org",
    "name": "List labels for a self-hosted runner for an organization",
    "description": "List labels for a self-hosted runner for an organization Lists all labels for a self-hosted runner configured in an organization. GET /orgs/{org}/actions/runners/{runner_id}/labels",
    "context": "GET /orgs/{org}/actions/runners/{runner_id}/labels\nLists all labels for a self-hosted runner configured in an organization. Authenticated users must have admin access to the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.\nRequired: org (path) — The organization name. The name is not case sensitive.; runner_id (path) — Unique identifier of the self-hosted runner."
  },
  {
    "id": "actions%2Flist-runner-applications-for-org",
    "name": "List runner applications for an organization",
    "description": "List runner applications for an organization Lists binaries for the runner application that you can download and run. GET /orgs/{org}/actions/runners/downloads",
    "context": "GET /orgs/{org}/actions/runners/downloads\nLists binaries for the runner application that you can download and run. Authenticated users must have admin access to the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.\nRequired: org (path) — The organization name. The name is not case sensitive."
  },
  {
    "id": "actions%2Flist-self-hosted-runners-for-org",
    "name": "List self-hosted runners for an organization",
    "description": "List self-hosted runners for an organization Lists all self-hosted runners configured in an organization. GET /orgs/{org}/actions/runners",
    "context": "GET /orgs/{org}/actions/runners\nLists all self-hosted runners configured in an organization. Authenticated users must have admin access to the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.\nRequired: org (path) — The organization name. The name is not case sensitive.\nOptional: name (query) — The name of a self-hosted runner.; per_page (query) — The number of results per page (max 100). For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\"; page (query) — The page number of the results to fetch. For more information, see \"[Using pagination in the REST API](https://docs.github.com/rest/using-the-rest-api/using-pagination-in-the-rest-api).\""
  },
  {
    "id": "actions%2Fremove-all-custom-labels-from-self-hosted-runner-for-org",
    "name": "Remove all custom labels from a self-hosted runner for an organization",
    "description": "Remove all custom labels from a self-hosted runner for an organization Remove all custom labels from a self-hosted runner configured in an organization. DELETE /orgs/{org}/actions/runners/{runner_id}/labels",
    "context": "DELETE /orgs/{org}/actions/runners/{runner_id}/labels\nRemove all custom labels from a self-hosted runner configured in an organization. Returns the remaining read-only labels from the runner. Authenticated users must have admin access to the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.\nRequired: org (path) — The organization name. The name is not case sensitive.; runner_id (path) — Unique identifier of the self-hosted runner."
  },
  {
    "id": "actions%2Fremove-custom-label-from-self-hosted-runner-for-org",
    "name": "Remove a custom label from a self-hosted runner for an organization",
    "description": "Remove a custom label from a self-hosted runner for an organization Remove a custom label from a self-hosted runner configured in an organization. DELETE /orgs/{org}/actions/runners/{runner_id}/labels/{name}",
    "context": "DELETE /orgs/{org}/actions/runners/{runner_id}/labels/{name}\nRemove a custom label from a self-hosted runner configured in an organization. Returns the remaining labels from the runner. This endpoint returns a `404 Not Found` status if the custom label is not present on the runner. Authenticated users must have admin access to the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.\nRequired: org (path) — The organization name. The name is not case sensitive.; runner_id (path) — Unique identifier of the self-hosted runner.; name (path) — The name of a self-hosted runner's custom label."
  },
  {
    "id": "actions%2Fset-custom-labels-for-self-hosted-runner-for-org",
    "name": "Set custom labels for a self-hosted runner for an organization",
    "description": "Set custom labels for a self-hosted runner for an organization Remove all previous custom labels and set the new custom labels for a specific self-hosted runner configured in an organization. PUT /orgs/{org}/actions/runners/{runner_id}/labels",
    "context": "PUT /orgs/{org}/actions/runners/{runner_id}/labels\nRemove all previous custom labels and set the new custom labels for a specific self-hosted runner configured in an organization. Authenticated users must have admin access to the organization to use this endpoint. OAuth app tokens and personal access tokens (classic) need the `admin:org` scope to use this endpoint. If the repository is private, the `repo` scope is also required.\nRequired: org (path) — The organization name. The name is not case sensitive.; runner_id (path) — Unique identifier of the self-hosted runner.; body (body)"
  }
]
