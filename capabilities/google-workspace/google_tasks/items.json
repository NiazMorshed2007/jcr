[
  {
    "id": "TasklistsDelete",
    "name": "Deletes the authenticated user's specified task list.",
    "description": "Deletes the authenticated user's specified task list. Deletes the authenticated user's specified task list. DELETE /tasks/v1/users/@me/lists/{tasklist}",
    "context": "DELETE /tasks/v1/users/@me/lists/{tasklist}\nDeletes the authenticated user's specified task list. If the list contains assigned tasks, both the assigned tasks and the original tasks in the assignment surface (Docs, Chat Spaces) are deleted.\nRequired: tasklist (path) — Task list identifier.\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasklistsGet",
    "name": "Returns the authenticated user's specified task list.",
    "description": "Returns the authenticated user's specified task list. GET /tasks/v1/users/@me/lists/{tasklist}",
    "context": "GET /tasks/v1/users/@me/lists/{tasklist}\nReturns the authenticated user's specified task list.\nRequired: tasklist (path) — Task list identifier.\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasklistsInsert",
    "name": "Creates a new task list and adds it to the authenticated user's task lists.",
    "description": "Creates a new task list and adds it to the authenticated user's task lists. Creates a new task list and adds it to the authenticated user's task lists. POST /tasks/v1/users/@me/lists",
    "context": "POST /tasks/v1/users/@me/lists\nCreates a new task list and adds it to the authenticated user's task lists. A user can have up to 2000 lists at a time.\nRequired: body (body)\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasklistsList",
    "name": "Returns all the authenticated user's task lists.",
    "description": "Returns all the authenticated user's task lists. Returns all the authenticated user's task lists. GET /tasks/v1/users/@me/lists",
    "context": "GET /tasks/v1/users/@me/lists\nReturns all the authenticated user's task lists. A user can have up to 2000 lists at a time.\nOptional: maxResults (query) — Maximum number of task lists returned on one page. Optional. The default is 1000 (max allowed: 1000).; pageToken (query) — Token specifying the result page to return. Optional.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasklistsPatch",
    "name": "Updates the authenticated user's specified task list.",
    "description": "Updates the authenticated user's specified task list. Updates the authenticated user's specified task list. PATCH /tasks/v1/users/@me/lists/{tasklist}",
    "context": "PATCH /tasks/v1/users/@me/lists/{tasklist}\nUpdates the authenticated user's specified task list. This method supports patch semantics.\nRequired: tasklist (path) — Task list identifier.; body (body)\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasklistsUpdate",
    "name": "Updates the authenticated user's specified task list.",
    "description": "Updates the authenticated user's specified task list. PUT /tasks/v1/users/@me/lists/{tasklist}",
    "context": "PUT /tasks/v1/users/@me/lists/{tasklist}\nUpdates the authenticated user's specified task list.\nRequired: tasklist (path) — Task list identifier.; body (body)\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasksClear",
    "name": "Clears all completed tasks from the specified task list.",
    "description": "Clears all completed tasks from the specified task list. Clears all completed tasks from the specified task list. POST /tasks/v1/lists/{tasklist}/clear",
    "context": "POST /tasks/v1/lists/{tasklist}/clear\nClears all completed tasks from the specified task list. The affected tasks will be marked as 'hidden' and no longer be returned by default when retrieving all tasks for a task list.\nRequired: tasklist (path) — Task list identifier.\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasksDelete",
    "name": "Deletes the specified task from the task list.",
    "description": "Deletes the specified task from the task list. Deletes the specified task from the task list. DELETE /tasks/v1/lists/{tasklist}/tasks/{task}",
    "context": "DELETE /tasks/v1/lists/{tasklist}/tasks/{task}\nDeletes the specified task from the task list. If the task is assigned, both the assigned task and the original task (in Docs, Chat Spaces) are deleted. To delete the assigned task only, navigate to the assignment surface and unassign the task from there.\nRequired: task (path) — Task identifier.; tasklist (path) — Task list identifier.\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasksGet",
    "name": "Returns the specified task.",
    "description": "Returns the specified task. GET /tasks/v1/lists/{tasklist}/tasks/{task}",
    "context": "GET /tasks/v1/lists/{tasklist}/tasks/{task}\nReturns the specified task.\nRequired: task (path) — Task identifier.; tasklist (path) — Task list identifier.\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasksInsert",
    "name": "Creates a new task on the specified task list.",
    "description": "Creates a new task on the specified task list. Creates a new task on the specified task list. POST /tasks/v1/lists/{tasklist}/tasks",
    "context": "POST /tasks/v1/lists/{tasklist}/tasks\nCreates a new task on the specified task list. Tasks assigned from Docs or Chat Spaces cannot be inserted from Tasks Public API; they can only be created by assigning them from Docs or Chat Spaces. A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.\nRequired: tasklist (path) — Task list identifier.; body (body)\nOptional: parent (query) — Parent task identifier. If the task is created at the top level, this parameter is omitted. An assigned task cannot be a parent task, nor can it have a parent. Setting the parent to an assigned task results in failure of the request. Optional.; previous (query) — Previous sibling task identifier. If the task is created at the first position among its siblings, this parameter is omitted. Optional.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasksList",
    "name": "Returns all tasks in the specified task list.",
    "description": "Returns all tasks in the specified task list. Returns all tasks in the specified task list. GET /tasks/v1/lists/{tasklist}/tasks",
    "context": "GET /tasks/v1/lists/{tasklist}/tasks\nReturns all tasks in the specified task list. Doesn't return assigned tasks by default (from Docs, Chat Spaces). A user can have up to 20,000 non-hidden tasks per list and up to 100,000 tasks in total at a time.\nRequired: tasklist (path) — Task list identifier.\nOptional: showCompleted (query) — Flag indicating whether completed tasks are returned in the result. Note that showHidden must also be True to show tasks completed in first party clients, such as the web UI and Google's mobile apps. Optional. The default is True.; showHidden (query) — Flag indicating whether hidden tasks are returned in the result. Optional. The default is False.; dueMin (query) — Lower bound for a task's due date (as a RFC 3339 timestamp) to filter by. Optional. The default is not to filter by due date.; completedMin (query) — Lower bound for a task's completion date (as a RFC 3339 timestamp) to filter by. Optional. The default is not to filter by completion date.; dueMax (query) — Upper bound for a task's due date (as a RFC 3339 timestamp) to filter by. Optional. The default is not to filter by due date.; pageToken (query) — Token specifying the result page to return. Optional.; maxResults (query) — Maximum number of tasks returned on one page. Optional. The default is 20 (max allowed: 100).; showDeleted (query) — Flag indicating whether deleted tasks are returned in the result. Optional. The default is False.; completedMax (query) — Upper bound for a task's completion date (as a RFC 3339 timestamp) to filter by. Optional. The default is not to filter by completion date.; updatedMin (query) — Lower bound for a task's last modification time (as a RFC 3339 timestamp) to filter by. Optional. The default is not to filter by last modification time.; showAssigned (query) — Optional. Flag indicating whether tasks assigned to the current user are returned in the result. Optional. The default is False.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasksMove",
    "name": "Moves the specified task to another position in the destination task list.",
    "description": "Moves the specified task to another position in the destination task list. Moves the specified task to another position in the destination task list. POST /tasks/v1/lists/{tasklist}/tasks/{task}/move",
    "context": "POST /tasks/v1/lists/{tasklist}/tasks/{task}/move\nMoves the specified task to another position in the destination task list. If the destination list is not specified, the task is moved within its current list. This can include putting it as a child task under a new parent and/or move it to a different position among its sibling tasks. A user can have up to 2,000 subtasks per task.\nRequired: task (path) — Task identifier.; tasklist (path) — Task list identifier.\nOptional: parent (query) — Optional. New parent task identifier. If the task is moved to the top level, this parameter is omitted. The task set as parent must exist in the task list and can not be hidden. Exceptions: 1. Assigned and repeating tasks cannot be set as parent tasks (have subtasks), or be moved under a parent task (become subtasks). 2. Tasks that are both completed and hidden cannot be nested, so the parent field must be empty.; previous (query) — Optional. New previous sibling task identifier. If the task is moved to the first position among its siblings, this parameter is omitted. The task set as previous must exist in the task list and can not be hidden. Exceptions: 1. Tasks that are both completed and hidden can only be moved to position 0, so the previous field must be empty.; destinationTasklist (query) — Optional. Destination task list identifier. If set, the task is moved from tasklist to the destinationTasklist list. Otherwise the task is moved within its current list. Recurrent tasks cannot currently be moved between lists.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasksPatch",
    "name": "Updates the specified task.",
    "description": "Updates the specified task. Updates the specified task. PATCH /tasks/v1/lists/{tasklist}/tasks/{task}",
    "context": "PATCH /tasks/v1/lists/{tasklist}/tasks/{task}\nUpdates the specified task. This method supports patch semantics.\nRequired: tasklist (path) — Task list identifier.; task (path) — Task identifier.; body (body)\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "TasksUpdate",
    "name": "Updates the specified task.",
    "description": "Updates the specified task. PUT /tasks/v1/lists/{tasklist}/tasks/{task}",
    "context": "PUT /tasks/v1/lists/{tasklist}/tasks/{task}\nUpdates the specified task.\nRequired: task (path) — Task identifier.; tasklist (path) — Task list identifier.; body (body)\nOptional: fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  }
]
