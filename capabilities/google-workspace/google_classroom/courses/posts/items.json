[
  {
    "id": "CoursesPostsAddOnAttachmentsCreate",
    "name": "Creates an add-on attachment under a post.",
    "description": "Creates an add-on attachment under a post. Creates an add-on attachment under a post. POST /v1/courses/{courseId}/posts/{postId}/addOnAttachments",
    "context": "POST /v1/courses/{courseId}/posts/{postId}/addOnAttachments\nCreates an add-on attachment under a post. Requires the add-on to have permission to create new attachments on the post. This method returns the following error codes: * `PERMISSION_DENIED` for access errors. * `INVALID_ARGUMENT` if the request is malformed. * `NOT_FOUND` if one of the identified resources does not exist.\nRequired: courseId (path) — Required. Identifier of the course.; postId (path) — Optional. Deprecated, use `item_id` instead.; body (body)\nOptional: addOnToken (query) — Optional. Token that authorizes the request. The token is passed as a query parameter when the user is redirected from Classroom to the add-on's URL. This authorization token is required for in-Classroom attachment creation but optional for partner-first attachment creation. Returns an error if not provided for partner-first attachment creation and the developer projects that created the attachment and its parent stream item do not match.; itemId (query) — Identifier of the `Announcement`, `CourseWork`, or `CourseWorkMaterial` under which to create the attachment. This field is required, but is not marked as such while we are migrating from post_id.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "CoursesPostsAddOnAttachmentsDelete",
    "name": "Deletes an add-on attachment.",
    "description": "Deletes an add-on attachment. Deletes an add-on attachment. DELETE /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}",
    "context": "DELETE /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}\nDeletes an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * `PERMISSION_DENIED` for access errors. * `INVALID_ARGUMENT` if the request is malformed. * `NOT_FOUND` if one of the identified resources does not exist.\nRequired: courseId (path) — Required. Identifier of the course.; postId (path) — Optional. Deprecated, use `item_id` instead.; attachmentId (path) — Required. Identifier of the attachment.\nOptional: itemId (query) — Identifier of the `Announcement`, `CourseWork`, or `CourseWorkMaterial` under which the attachment is attached. This field is required, but is not marked as such while we are migrating from post_id.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "CoursesPostsAddOnAttachmentsGet",
    "name": "Returns an add-on attachment.",
    "description": "Returns an add-on attachment. Returns an add-on attachment. GET /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}",
    "context": "GET /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}\nReturns an add-on attachment. Requires the add-on requesting the attachment to be the original creator of the attachment. This method returns the following error codes: * `PERMISSION_DENIED` for access errors. * `INVALID_ARGUMENT` if the request is malformed. * `NOT_FOUND` if one of the identified resources does not exist.\nRequired: attachmentId (path) — Required. Identifier of the attachment.; courseId (path) — Required. Identifier of the course.; postId (path) — Optional. Deprecated, use `item_id` instead.\nOptional: itemId (query) — Identifier of the `Announcement`, `CourseWork`, or `CourseWorkMaterial` under which the attachment is attached. This field is required, but is not marked as such while we are migrating from post_id.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "CoursesPostsAddOnAttachmentsList",
    "name": "Returns all attachments created by an add-on under the post.",
    "description": "Returns all attachments created by an add-on under the post. Returns all attachments created by an add-on under the post. GET /v1/courses/{courseId}/posts/{postId}/addOnAttachments",
    "context": "GET /v1/courses/{courseId}/posts/{postId}/addOnAttachments\nReturns all attachments created by an add-on under the post. Requires the add-on to have active attachments on the post or have permission to create new attachments on the post. This method returns the following error codes: * `PERMISSION_DENIED` for access errors. * `INVALID_ARGUMENT` if the request is malformed. * `NOT_FOUND` if one of the identified resources does not exist.\nRequired: courseId (path) — Required. Identifier of the course.; postId (path) — Optional. Identifier of the post under the course whose attachments to enumerate. Deprecated, use `item_id` instead.\nOptional: itemId (query) — Identifier of the `Announcement`, `CourseWork`, or `CourseWorkMaterial` whose attachments should be enumerated. This field is required, but is not marked as such while we are migrating from post_id.; pageSize (query) — The maximum number of attachments to return. The service may return fewer than this value. If unspecified, at most 20 attachments will be returned. The maximum value is 20; values above 20 will be coerced to 20.; pageToken (query) — A page token, received from a previous `ListAddOnAttachments` call. Provide this to retrieve the subsequent page. When paginating, all other parameters provided to `ListAddOnAttachments` must match the call that provided the page token.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "CoursesPostsAddOnAttachmentsPatch",
    "name": "Updates an add-on attachment.",
    "description": "Updates an add-on attachment. Updates an add-on attachment. PATCH /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}",
    "context": "PATCH /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}\nUpdates an add-on attachment. Requires the add-on to have been the original creator of the attachment. This method returns the following error codes: * `PERMISSION_DENIED` for access errors. * `INVALID_ARGUMENT` if the request is malformed. * `NOT_FOUND` if one of the identified resources does not exist.\nRequired: postId (path) — Required. Identifier of the post under which the attachment is attached.; courseId (path) — Required. Identifier of the course.; attachmentId (path) — Required. Identifier of the attachment.; body (body)\nOptional: updateMask (query) — Required. Mask that identifies which fields on the attachment to update. The update fails if invalid fields are specified. If a field supports empty values, it can be cleared by specifying it in the update mask and not in the `AddOnAttachment` object. If a field that does not support empty values is included in the update mask and not set in the `AddOnAttachment` object, an `INVALID_ARGUMENT` error is returned. The following fields may be specified by teachers: * `title` * `teacher_view_uri` * `student_view_uri` * `student_work_review_uri` * `due_date` * `due_time` * `max_points`; itemId (query) — Identifier of the post under which the attachment is attached.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "CoursesPostsAddOnAttachmentsStudentSubmissionsGet",
    "name": "Returns a student submission for an add-on attachment.",
    "description": "Returns a student submission for an add-on attachment. Returns a student submission for an add-on attachment. GET /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}",
    "context": "GET /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}\nReturns a student submission for an add-on attachment. This method returns the following error codes: * `PERMISSION_DENIED` for access errors. * `INVALID_ARGUMENT` if the request is malformed. * `NOT_FOUND` if one of the identified resources does not exist.\nRequired: attachmentId (path) — Required. Identifier of the attachment.; postId (path) — Optional. Deprecated, use `item_id` instead.; courseId (path) — Required. Identifier of the course.; submissionId (path) — Required. Identifier of the student’s submission.\nOptional: itemId (query) — Identifier of the `Announcement`, `CourseWork`, or `CourseWorkMaterial` under which the attachment is attached. This field is required, but is not marked as such while we are migrating from post_id.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "CoursesPostsAddOnAttachmentsStudentSubmissionsPatch",
    "name": "Updates data associated with an add-on attachment submission.",
    "description": "Updates data associated with an add-on attachment submission. Updates data associated with an add-on attachment submission. PATCH /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}",
    "context": "PATCH /v1/courses/{courseId}/posts/{postId}/addOnAttachments/{attachmentId}/studentSubmissions/{submissionId}\nUpdates data associated with an add-on attachment submission. Requires the add-on to have been the original creator of the attachment and the attachment to have a positive `max_points` value set. This method returns the following error codes: * `PERMISSION_DENIED` for access errors. * `INVALID_ARGUMENT` if the request is malformed. * `NOT_FOUND` if one of the identified resources does not exist.\nRequired: postId (path) — Optional. Deprecated, use `item_id` instead.; submissionId (path) — Required. Identifier of the student's submission.; courseId (path) — Required. Identifier of the course.; attachmentId (path) — Required. Identifier of the attachment.; body (body)\nOptional: itemId (query) — Identifier of the `Announcement`, `CourseWork`, or `CourseWorkMaterial` under which the attachment is attached. This field is required, but is not marked as such while we are migrating from post_id.; updateMask (query) — Required. Mask that identifies which fields on the attachment to update. The update fails if invalid fields are specified. If a field supports empty values, it can be cleared by specifying it in the update mask and not in the `AddOnAttachmentStudentSubmission` object. The following fields may be specified by teachers: * `points_earned`; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  },
  {
    "id": "CoursesPostsGetAddOnContext",
    "name": "Gets metadata for Classroom add-ons in the context of a specific post.",
    "description": "Gets metadata for Classroom add-ons in the context of a specific post. Gets metadata for Classroom add-ons in the context of a specific post. GET /v1/courses/{courseId}/posts/{postId}/addOnContext",
    "context": "GET /v1/courses/{courseId}/posts/{postId}/addOnContext\nGets metadata for Classroom add-ons in the context of a specific post. To maintain the integrity of its own data and permissions model, an add-on should call this to validate query parameters and the requesting user's role whenever the add-on is opened in an [iframe](https://developers.google.com/workspace/classroom/add-ons/get-started/iframes/iframes-overview). This method returns the following error codes: * `PERMISSION_DENIED` for access errors. * `INVALID_ARGUMENT` if the request is malformed. * `NOT_FOUND` if one of the identified resources does not exist.\nRequired: courseId (path) — Required. Identifier of the course.; postId (path) — Optional. Deprecated, use `item_id` instead.\nOptional: attachmentId (query) — Optional. The identifier of the attachment. This field is required for all requests except when the user is in the [Attachment Discovery iframe](https://developers.google.com/workspace/classroom/add-ons/get-started/iframes/attachment-discovery-iframe).; itemId (query) — Identifier of the `Announcement`, `CourseWork`, or `CourseWorkMaterial` under which the attachment is attached. This field is required, but is not marked as such while we are migrating from post_id.; addOnToken (query) — Optional. Token that authorizes the request. The token is passed as a query parameter when the user is redirected from Classroom to the add-on's URL. The authorization token is required when neither of the following is true: * The add-on has attachments on the post. * The developer project issuing the request is the same project that created the post.; fields (query) — Field mask limiting which response fields are returned, e.g. sheets.properties."
  }
]
